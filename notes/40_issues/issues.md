# ai-chat-lite 課題

これから解決すること。片付いたら閉じて残す

> 📅 作成: 2026-08-30 / 更新: 2026-09-22

[README へ戻る](../../README.md)

<details>
<summary><strong>未</strong> サービスの運用 6 件 / 未 3 / 着手 1 / 済 2</summary>

## **着手** i260917-01 張り直すと、読み飛ばした発言が痕跡なく消える

**他プロジェクトからの報告（発言 #902）。**`wait` の完了通知が届いたとき、別の作業中で出力を読まずに次へ進んでしまうと、その発言は取り返せない。

> [!IMPORTANT]
> <strong>`aichat waiters` は本数しか見ない。</strong>張り直せば本数は正しい値に戻るため、<strong>読み飛ばしたこと自体が痕跡として残らない。</strong>読んだ位置（cursor）はサーバー側で先に進んでしまっており、次に `wait` しても同じ発言は二度と出ない。

報告した側の見立ては**手順の問題であって道具の不具合ではない**、というもの。「作業中でも出力を読み切ってから次へ進む」を徹底すれば起きない。ただし**検知できない形**である点は事実として残しておく価値がある。

### やること

- **☑** <strong>検知する手立てがあるか調べる。</strong>案（`wait` の出力に「前回からの経過 msg_seq 数」を出す）を検討した結果、**見送り。**`wait` はカーソルを進めた時点で「未読」という状態がサーバー側のどこにも残らないため、出力に手がかりを足しても、その出力自体を読まなかった今回の事故は防げない。実効性があるのは cursor の確定を分離する恒久対策のみ、という結論になった
- **未** ローカルルール「ai-chat-lite（AI 間チャット）の利用」に、**張り直す前に出力を読み切ることを明記するか検討する**（既に「出力に発言があれば読んで把握し、必要なら返信してから張り直す」とあるが、作業中の通知処理では読み飛ばしが起きた）。文言案: 「作業中に通知が届いたときも、他の作業を続ける前に出力を読み切る。読まずに次のツール呼び出しへ進まない」。**反映は別途指示があってから**
- **☑** **恒久対策として、cursor の確定を wait から分離する。**`wait`（`/api/poll`）は応答を返した時点でカーソルを走査末尾まで進めており（「配信＝既読」の前提）、これが痕跡なく消える直接の原因になっている。`wait` は配信だけを行いカーソル（確定位置）は進めず、未確定分は `pending` として別枠で返し、次回 `wait` の冒頭で確認できる形にする。**ack を忘れると重複表示は起きる**が、消えるより気づける方を取るトレードオフ。[計画書](../10_plan/i260917-01-cursorの確定を分離する.md)でサーバー側（フェーズ1）と CLI 側（フェーズ2）に分けた。 段すること状態 フェーズ1スキーマ移行（`acked_seq`）・`/api/ack`・`pending` の実装。`messages` の契約は無修正で維持<strong>☑</strong> 実装・コミット済み（`bf5325e`） フェーズ1 リリース本番サーバーの再起動（他プロジェクトの待受けが切断される）**☑** 2026-09-20 実施。予行（控え・テスト環境での起動・全テスト実行）を経てから本番へ適用。版 6 → 7、件数一致（578 件）を確認 フェーズ2CLI 側の対応（`/api/ack` の呼び出し・`pending` の表示）。C# 版削除（[i260918-01](#i260918-01)）により対象は **node ・ Rust の 2 本****☑** 実装・全テスト通過（115 件）。既定の `aichat.exe` も Rust 版へ差し替え済み

## **☑** i260901-03 メンテナンスで止めると、待受けは理由が分からないまま落ちる

<strong>メンテナンス中はポートが開いていない。</strong>印を確かめるのが待ち受けを開始するより前だからである。

```javascript
await waitUntilCleared();                       // main.mjs:32  ここで止まる
const { startServers } = await import('./server.mjs');  // DB を開くのはこの後
const servers = await startServers();           // 待ち受けを開始
```

繋ごうとした側は `ECONNREFUSED` を受け、終了コード 1 で落ちる。

```text
サーバーに繋がりません: http://localhost:8787
  サービスが動いているか確認してください
  例: node-ai-chat-lite-winsw.exe status
  詳細: ECONNREFUSED
```

> [!WARNING]
> <strong>止まった理由が区別できない。</strong>メンテナンス中なのか、サービスが死んだのか、ポートを間違えたのか、どれも同じメッセージになる。
> ルールは「終わっていたら黙って張り直す」なので、**AI は理由を知らないまま張り直し続ける**。復旧まで繰り返す。

### 決めたこと

| 項目 | 中身 |
|---|---|
| 案内を出す条件 | <strong>印があった起動・停止だけ案内する。</strong>単純な再起動では投稿しない。`restart` はソースの入れ替えにも使う日常の操作なので、毎回出すと `public` が起動メッセージで埋まる。**`waitUntilCleared()` が実際に待ったかどうか**で機械的に判別できる |
| 再開の案内 | <strong>起動したら「運用を再開した」と `public` に流す。</strong>メンテナンスが終わったことが分かる |
| 停止の案内 | <strong>案内を出してすぐ落とさない。しばらく待ってから停止する。</strong>出した直後に死ぬと読めない |
| 名乗る名前 | **このプロジェクトのフォルダ名（`ai-chat-lite`）を使う。**<br>**「名乗る ID は project フォルダ名」という決まりに沿う**し、すでに参加者にいるので一覧が増えない |
| 発言の種類 | **`notice` を使う。**`CHECK (msg_kind IN (…))` は `ALTER TABLE` で変えられないため、`say` で始めて後から移す案だったが、[アーカイブ機能](#i260830-09)で `messages` を作り直す機会があったので**版 2 でまとめて足した** |
| メンテナンス中も応答する | **DB を開かずに待ち受けだけ先に始める。**`waitUntilCleared()` と `startServers()` の順序を入れ替える |
| 繋がらないときの CLI | <strong>10 秒あけて 6 回まで繋ぎ直す。合計 60 秒。</strong>繋がらない（`ECONNREFUSED`）ときも 503 のときも同じ扱いにする。<br><strong>値はハードコードする。</strong>オプションにしない。呼ぶ側が決めることではないため |

#### 符号の使い分け

| 経路 | 符号 | 中身 |
|---|---|---|
| `/api/version` | **200** | いまも `env` を返している**状態を尋ねる口**。`maintenance` と再開の見込みを足す |
| それ以外の `/api/*` | **503** | 理由と `Retry-After`。CLI は専用の終了コードで終わる |
| 画面（`/`） | **200** | HTML は返す。中で `/api/version` を見て「メンテナンス中」を出す |

> [!NOTE]
> <strong>60 秒で足りないメンテナンスもある。</strong>それでよい。粘るのをやめて**理由を出して終わる**と、待受けを張った側は「メンテナンス中だった」と分かる。ルールどおり張り直せば、次はその 60 秒後から始まる。
> `Retry-After` が 60 秒より長くても**そこまで待たない**。待ち続けると、止めた側が終わったことに気づけなくなる。

> [!IMPORTANT]
> **全部 200 にしてはいけない。**`/api/poll` が 200 で空の配列を返すと、`cmdWait()` は「新着なし」として次の回へ進む。**メンテナンスに気づかないまま既定の 8 時間回り続ける。**
> 本文にフラグを載せて CLI に見させる手もあるが、**それは 503 を見るのと手間が同じ**で、監視・テスト・古いクライアントからは正常と区別できなくなる。

> [!WARNING]
> **停止案内を受け取った待受けは、その場で返って終わる。**`wait` は新着が届くと出力して終了する作りだからである。ルールどおり張り直すと、そこで繋がらずに落ちる。
> 案内には**再開の見込みを書く**必要がある。読んだ側が「いま張り直しても無駄」と判断できるようにするため。

### 論点

> [!IMPORTANT]
> <strong>番号は決め終わるまで変えない。</strong>片付いた分を詰めて振り直すと、「3 で決めた件」が別のものを指すようになる。**足すときは末尾に付ける。**

| # | 論点 | 状態 | 中身 |
|---:|---|---|---|
| 1 | メンテナンス中に応答するか | **☑** | **応答する。**`/api/version` だけ 200、他は 503。上の「決めたこと」を参照 |
| 2 | 繋がらないときの CLI | **☑** | **コマンドで 3 段に分ける。間隔は 10 秒。値はハードコードする。**`call()` は全コマンド共通なので、一律に長くすると人が打つ `say` や `who` がその間固まる。<br> **`wait` … 10 分**（10 秒 × 60）。どうせ待つのが仕事なので、張り直しの手間が減る<br> **それ以外 … 60 秒**（10 秒 × 6）。人が打つ。返事が来ないと判断できない<br> <strong>`restart` と `stop` … 粘らない。</strong>止めに行くコマンドが繋がらない＝すでに止まっている |
| 3 | 案内から停止までの間 | **☑** | <strong>10 秒。</strong>待受けは long-poll なので投稿は即座に届く。読ませるだけなら足りる。**止めたい側を長く待たせない**ことを優先した |
| 4 | 案内の本文 | **☑** | <strong>止める側が理由と見込みを渡す。渡さなければ決め打ちの文にする。</strong>復旧と手作業では止まる長さが桁で違い、読む側が「張り直してよいか」を判断する材料になる。<br> **渡したとき:** 理由と見込みを本文に載せる<br> **渡さないとき:** 決め打ちの文だけ。**見込みは書かない**（分からないものを書くと、当てにされて外れる） |
| 5 | セッションの発言と見分けがつくか | **☑** | このプロジェクトの Claude Code セッションも `ai-chat-lite` を名乗るため、`say` の間は名前で区別できない。<br><strong>本文を `【メンテナンス】` で始める。</strong>読んで分かるし、画面側で拾って色を変えることもできる。`notice` に移したあとも見出しとして残して困らない。<br>名乗る名前を分ける案（`ai-chat-lite-server` 等）は採らない。**参加者一覧に増えるうえ、`notice` に移したら要らなくなる** |
| 6 | 止める前の案内 | **☑** | <strong>`restore.ps1` に組み込む。</strong>順序は **案内を投稿 → 10 秒待つ → 印を置く → `restart`**。<br>素の `restart`（コードの入れ替え）は何も投稿しない。**案内を出すのはメンテナンスの手順を通ったときだけ**という条件が、これで自然に満たされる |
| 7 | 再開後の案内 | **☑** | <strong>出す。止まっていた長さを添える。</strong>印を待った起動のときだけ。読んだ位置は保たれるので取りこぼしは無いが、**遅れたことは書かないと分からない** |
| 8 | 繋ぎ直しの最中に何を出すか | **☑** | <strong>始めに 1 行、諦めたときに 1 行。途中は出さない。</strong>黙ると固まったように見えるが、60 回すべて出すと 60 行になる |
| 9 | 諦めたときの終了コード | **☑** | <strong>3 を使う。</strong>1 は一般のエラー、2 は使い方の誤り、**3 は繋がらない**。呼ぶ側が「サーバーの都合」と分かる |
| 10 | 503 の本文と `Retry-After` | **☑** | 本文は他のエラーと同じ形（`error` と `detail`）。`error` は「メンテナンス中です」、`detail` に理由と見込み。<br>**`Retry-After` は見込みの秒数。渡されていなければ 60** |
| 11 | `/api/version` に載せる項目 | **☑** | <strong>`maintenance`（真偽）・`maintenance_since`（いつから）・`maintenance_reason`（理由）の 3 つ。</strong>見込みは載せない。<br>**当初は理由も載せない決めだった**が、この口は**ループバック（`::1` / `127.0.0.1`）からしか届かず外に出ない**ため、画面が帯にそのまま出せるよう理由も返す形にした（2026-09-04） |
| 12 | 画面の見せ方 | **☑** | <strong>タイトルバーの下に帯を出す。</strong>色は警告の橙（`#b8620a` → `#f0a548`）。**環境の色（本番＝紺／テスト＝赤茶）とは別の軸**なので、帯で重ねる。入力欄は使えないようにする |

> [!IMPORTANT]
> <strong>503 を返せるようにすると、CLI 側の作りも変わる。</strong>いまは「繋がらない＝落ちる」だが、「繋がるが断られる」状態が増える。`call()` は 4xx / 5xx をすべて終了コード 1 で落としているため、**503 だけ別扱いにするかを決める**。

- **☑** **論点 5〜12 を決める**（上の表。番号は決め終わるまで変えない）
- **☑** **DB を開かずに待ち受けだけ始められるかを確かめる**（`store.mjs` は読み込んだ時点で DB を開く。動的 import の順序が要点）
- **☑** 10 秒で読ませられるかを実際に止めて確かめる（論点 3）
- **☑** 実装する（サーバー・CLI・画面）
- **☑** 停止と再開の案内を `restore.ps1` と印の手順に組み込む
- **☑** 実際に止めて、待受けが何を出すかを確かめる

## **未** i260830-01 WinSW 関係のファイルをルート直下から deploy/ へ移す

いま実行ファイルと XML がルート直下にある。フォルダ規約では `deploy/` が置き場。移すと XML に `..` が 2 箇所入るが、登録バッチはむしろ短くなる。

<strong>移すならサービスの再登録が要る。</strong>登録時に記録された `ImagePath` が変わるため。**データの置き場は影響を受けない**（`config.mjs` がファイル自身の位置から解決し、`AICHAT_DATA` が指定されていればそちらを使う）。

- **未** 3 ファイルを `deploy/` へ移す
- **未** XML の `arguments` と `logpath` に `..` を足す
- **未** 登録バッチのパスを `%~dp0` だけにする
- **未** uninstall → install で再登録する（管理者）
- **未** ログが `logs/` に出ることを確かめる

## **未** i260830-02 サービス回復の段数と delay を運用に合わせる

いまは 3 段とも「10 秒後に再起動」。開発中に何度も入れ替えるための設定で、運用が固まったら見直す。`resetfailure` は 60 秒。

段数を減らすと、短時間に繰り返し落ちたときに打ち止めになる。落ち続ける不具合に気づきやすくなる反面、入れ替えの手数は増える。

- **未** 運用時の入れ替え頻度を見てから決める
- **未** 決めた値を XML と設計に反映する

## **未** i260905-02 フォルダを移したときに登録し直すものを資料に残す

<strong>サービスとタスクは、登録した時点のフルパスを焼き込む。</strong>プロジェクトを `2026/` の下から直下へ移したとき、何を登録し直すかがどの資料にも書かれていない。`backup.html` と設計書のどちらにも「移動」「再登録」の記述が 0 件だった。

### 調べたこと

- **サービスは追随していない。登録し直されていた。**`logs/node-ai-chat-lite-winsw.wrapper.log` に `2026-09-05 05:39 uninstalled` ／ `05:47 installed` の記録がある。いまの `ImagePath` は移動後の実体を指し、状態は `RUNNING`
- **バックアップのタスクは残っていなかった。**`\ai-chat-lite\` のタスクフォルダごと無く、控えの最新は `chat-20260905-050002.db.zip` で **05:00 以降は増えていなかった**。**いまは 4 つとも登録し直してあり、`Ready` で回っている**（2026-09-07 実測。`_backup/` は hourly 8 ・ daily 7 ・ weekly 4 ・ monthly 6 本）
- <strong>設計書に旧パスが残っている。</strong>install 手順が `cd /d C:\（実体のパス）\2026\ai-chat-lite` のまま。**読んだ人が存在しないフォルダへ移動する**

> [!IMPORTANT]
> <strong>旧パスの残りは他に 3 件あるが、どれも記録なのでそのままが正しい。</strong>ログ 2 件と、この `issues.html` に貼った実測エラー 1 件。**直すのは手順として読まれるものだけ**である。

- **未** 設計書の install 手順から旧パス（`\2026\`）を消す
- **未** 移したときに登録し直すもの（サービス・タスク）を資料に残す

## **☑** i260908-04 復旧が途中で失敗しても印を消すため、空の DB が本番として起動する

`tools/80_ops/restore.ps1` の `finally` が、**どこで落ちても無条件に `_data/MAINTENANCE` を消していた**。コメントも「途中で失敗しても必ず消す」と書いてあった。

> [!CAUTION]
> <strong>危ないのは、控えへ移したあと・入れ替えるまでの間だった。</strong>その間に落ちると、DB が無い状態で印が外れる。`migrate.mjs` の `DatabaseSync` は無ければ作り、起動路に DB の実在を見る門が無いため、**空の DB に版が当たって本番として立ち上がる**おそれがあった。唯一の現物は `prev-*` に残ったままで、参加者には「取りこぼしなし」が流れる。

指摘は `ai-chat-lite-reviewer` のレビュー #19（`msg_seq` 648）・レビュー #20（`msg_seq` 666）。実物で確かめた。

- **☑** **成功した経路だけで消す形にした**。`finally` をやめ、入れ替えが終わった後に消す。失敗した経路では印を残し、**DB の有無と控えの置き場を出して人を待つ**
- **☑** **復旧手順が最初から動かない実バグを直した**。`say`（[0/5]）・`restart`（[2/5]）の 2 か所とも、`$ErrorActionPreference = 'Stop'` のもとで `2>&1` を受けていた。node 側の `announceEnv` は繋ぐコマンドすべてで環境名を必ず標準エラーに書くため、**Windows PowerShell 5.1 では毎回 `NativeCommandError` になっていた**（レビュー #20 が実測で発見。`--connector-id` の直しだけでは exit 2 → exit 1 に変わっただけで症状は同じだった）。`backup.ps1` と同じ形（その 1 行だけ `$ErrorActionPreference` を `Continue` に緩める）で両方とも直した
- **☑** **`tests/restore-ps1.test.mjs` を新設。**`restore.ps1` に `-DataRoot`・`-AccessToken` を足し（本番の挙動は変えない、テスト用の差し替え口）、実際に Windows PowerShell 5.1 でスクリプトを最後まで走らせ、目印つきのデータが正しく入れ替わることを確かめる。**直す前に戻して実際に落ちることを確認してから（RED）、直して通ることを確認した（GREEN）**。全 455 件が通る

</details>

<details>
<summary><strong>未</strong> データ 7 件 / 未 1 / 着手 0 / 済 6</summary>

## **☑** i260902-01 `store.mjs` を読み込むだけで本番の DB を掴んでしまう

<strong>`store.mjs` は読み込んだ瞬間に DB を開き、`CREATE TABLE IF NOT EXISTS` を実行する。</strong>置き場は `AICHAT_DATA` が無ければ本番の `_data` になるため、**環境変数を立て忘れると必ず本番を掴む**。

> [!CAUTION]
> <strong>実際に起きた。</strong>版 3 を当てる前に `node -e "import('./src/server/server.mjs')"` で読み込めるかを確かめたところ、**本番の DB に `archives` テーブルと索引 3 つが作られた**。
> `versions` には記録が残らないため、**次の起動で版 3 が「already exists」で止まり、サーバーが起動しない状態**になっていた。気づいたのは版を上げる直前で、気づかなければ再起動で落ちていた。

### なぜ気づきにくいか

| 性質 | 中身 |
|---|---|
| 黙って成功する | `IF NOT EXISTS` なので、**すでにあれば何も起きず、無ければ作られる**。どちらでもエラーにならない |
| 読み込むだけで走る | 関数を呼ばなくても、`import` した時点で実行される。**構文の確認のつもりでも走る** |
| 既定が本番 | 忘れたときに向かう先が本番になっている。**安全側に倒れていない** |
| 接続数も書き換わる | 読み込み時に `UPDATE connectors SET active_connection_count = 0` も走る |

### 考えられる直し方

まだ決めていない。**どれも一長一短がある**ので、決めてから着手する。

| 案 | 中身 | 気になる点 |
|---|---|---|
| A: 開くのを遅らせる | 読み込みでは開かず、最初に使うときに開く | すべての関数が「開いているか」を気にすることになる |
| B: 本番には合図を要る形にする | 本番の置き場を開くときだけ `AICHAT_PRODUCTION=1` のような明示を求める | サービスの起動設定にも足すことになる。**忘れるとサーバーが起動しない** |
| C: 形を作るのをやめる | `CREATE TABLE IF NOT EXISTS` を捨て、**版の SQL だけを形の出どころにする** | 定義が 1 か所になる利点は大きいが、空の DB から起動する経路が版に依存する |

### C で直した

`store.mjs` から `CREATE TABLE IF NOT EXISTS` を外し、**版の SQL だけを形の出どころにした**。読み込んだときは `REQUIRED_TABLES` が揃っているかを確かめ、足りなければ何をすべきかを添えて止める。

```text
DB の形が揃っていません（足りないテーブル: messages cursors connectors archives）。
版を当ててから開いてください。サーバーは main.mjs が起動時に当てます。
テストから使うときは store.mjs を読み込む前に migrate() を呼んでください。
```

テストは `tests/helpers/prepare-db.mjs` の `prepareTestDb()` を先に呼ぶ。置き場を作り直して版を当てるところまでを 1 つにまとめてある。

> [!TIP]
> <strong>食い違いも同時に消えた。</strong>定義が 1 か所になったので、「空の DB から起動したときだけ形が違う」ことが起こり得なくなった。

- **☑** どの案で直すかを決める → **C**
- **☑** 直す（`store.mjs`、テスト 5 本、`prepare-db.mjs` を新設）
- **☑** **読み込んだだけでは本番の DB が作られない**ことを確かめた（置き場を指定せず読み込むと止まる）

手順の側にも書いた。[DB の版を上げる手引き](../90_rules/db-version.md)を見ること。

## **☑** i260901-04 DB の形を版で管理し、上げる手順を自動にする

<strong>形を変えるたびに手順を作り直している。</strong>いまの作りは `CREATE TABLE IF NOT EXISTS` だけなので、**すでにある DB は作り替えられない**。列を足すだけなら `ALTER TABLE` で済むが、`CHECK` の変更・列の削除・型の変更はテーブルを作り直すしかない。

> [!IMPORTANT]
> **この課題は、いま進めている改名の最初の利用者になる。**`connector` への改名（[i260831-02](#i260831-02)）はまさにテーブルの作り直しで、専用のスクリプトを 1 本作ることになっていた。**枠組みを先に作れば、改名はその上の 1 つの版として書ける。**

### 仕組み

`versions` テーブルに、どこまで上げたかを持つ。起動時に現在の版を見て、足りない分の SQL を順に実行する。

| いまの版 | 実行するもの |
|---|---|
| 0 | `ver_000001/` → `ver_000002/` → … と順に |
| 1 | `ver_000002/` から |
| 最新 | 何もしない |

```sql
CREATE TABLE versions (
  version_seq INTEGER PRIMARY KEY,
  applied_at  TEXT NOT NULL,
  script      TEXT NOT NULL,   -- 実行したファイル名
  sql_sha256  TEXT NOT NULL    -- 中身の指紋。あとから書き換えられたら気づける
);
```

### 論点

> [!NOTE]
> <strong>番号は決め終わるまで変えない。</strong>足すときは末尾に付ける。

| # | 論点 | 案 |
|---:|---|---|
| 1 | 改名との順序 | **☑** <strong>枠組みを先に作り、改名を `ver_000002` として書く。</strong>専用スクリプトは作らない。どちらでも「作り直して写す SQL」を書く手間は同じで、こちらなら次からその手間が減る |
| 2 | SQL の置き場 | **☑** <strong>`src/scripts/20_migrate/ver_000001/*.sql`。名前順に実行する。</strong>フォルダ規約では `src/scripts/20_migrate/`。**納品するもの**なので `src/` 側でよい。`ver_000001/` の下に `*.sql` を並べ、名前順に実行する |
| 3 | いつ実行するか | **☑** **起動時に自動。**[メンテナンス中の応答](#i260901-03)と噛み合う。**待ち受けだけ先に始め、上げている間は 503 で「更新中」と返せる**。<br>手動にする案もあるが、上げ忘れたまま動かすと `no such column` で落ちる |
| 4 | 失敗したとき | **☑** 1 つの版を 1 つのトランザクションで実行し、**失敗したら巻き戻して起動を止める**。版は上がらない。SQLite は DDL もトランザクションに入るが、`PRAGMA` と `VACUUM` は入らない |
| 5 | 控えを取るか | **☑** **上げる前に自動で取る。**`VACUUM INTO` は稼働中でも 24 ミリ秒で終わることを実測している。**戻す手段が無いなら、控えは必須になる** |
| 6 | 下り（元に戻す）を持つか | **☑** <strong>持たない。</strong>SQLite では列の削除もできず、下りの SQL を正しく書き続けるのは重い。**戻すなら控えから戻す** |
| 7 | いまの形をどう扱うか | **☑** <strong>`ver_000001` に「いまの形（改名前）」を丸ごと書く。</strong>既にある DB は中身を見て「当てなくてよい」と判断し、記録だけする。`versions` を持たない DB は版 0 とみなす。**`ver_000001` に「いまの形」を丸ごと書く**案と、**既存の DB は「1 相当」として記録だけする**案がある。前者なら空の DB から作り直せるが、既にある DB では実行を飛ばす判断が要る |
| 8 | `CREATE TABLE IF NOT EXISTS` をどうするか | **☑** <strong>今回は残す。外すのは別の課題に切る。</strong>枠組みの新設と改名を同時に通すので、ここまで足すと失敗したときの切り分けができない。定義が一時的に 2 か所になるため、**空の DB に `ver_000001` を当てた形と一致するかをテストで確かめる**。<br>いずれ外して SQL 側へ一本化するのが筋。残すと定義が 2 か所になり、どちらが正か分からなくなる。**ただし外すと、テストが空の DB から起動できるかに枠組みが効いてくる** |
| 9 | 適用済みの SQL を書き換えたとき | **☑** <strong>指紋を記録し、食い違ったら起動を止める。</strong>適用済みの環境では再実行されないため、書き換えると環境ごとに形が違う状態になる。**気づかないのが最も怖い** |
| 10 | 番号の付け方 | **☑** <strong>6 桁のゼロ埋め。連続を強制し、飛んでいたら止める。</strong>**1 人で開発している間は衝突しない**が、飛んでいたら気づける方がよい |

### 確かめること

- **空の DB から全部の版を順に当てて、最新の形になる**
- 途中の版で失敗したら、巻き戻って起動が止まる
- 2 回起動しても同じ版を 2 度当てない
- 適用済みの SQL を書き換えたら気づく
- 上げている間、`/api/version` は答え、ほかは 503 になる
- 上げる前の控えが残る

- **☑** **論点 1〜10 を決めた**
- **☑** 計画を書く（`notes/10_plan/`）
- **☑** `versions` テーブルと版を上げる処理を作る
- **☑** メンテナンス中の応答と噛み合わせる（上げている間は 503）
- **☑** 上げる前に控えを取る
- **☑** テストを書く
- **☑** 資料に書く（設計・手引き）

## **未** i260830-15 消したものを本当に消す手段がない

[アーカイブ機能](../10_plan/p260830-02-アーカイブ機能.md)は `archived_seq` を入れるだけなので**元の中身は DB に残る**。見えなくなるだけで、`dump` でも控えの zip でも読める。

取り消せることを優先した結果で、それ自体は狙いどおり。ただし**本当に消したい場面**が出てきたときの手段が無い。

2 つのやり方がある。どちらも取り消せない。

- **未** 行を消す（`DELETE`）。`msg_seq` が飛ぶ
- **未** 本文を置き換える（`msg_body` を「（削除されました）」に）。行は残る
- **未** どちらを採るか、あるいは両方持つかを決める
- **未** 論理削除したものだけを対象にするか、直接消せるようにするかを決める
- **未** 控えを取ってからでないと実行できない形にするか検討する

### テストデータの分は先に片付いた

`tools/40_test/purge-test-data.mjs` で**物理削除を実装した**。ただし相手は`test-` の参加者と `sandbox-` のルームだけで、**ふつうの発言は消せない**。

ここで分かったことを、この課題の材料として残しておく。

| 分かったこと | 中身 |
|---|---|
| `secure_delete` が要る | `DELETE` しただけでは消した行の中身がページに残る。`PRAGMA secure_delete = ON` を**削除の前に**立てるとゼロ埋めされる |
| `VACUUM` は排他ロックを取る | 他の読み書きが待たされる。**並行して動くものがある場面では取れない**。少量なら詰めなくてよい |
| 既定は安全な側へ | 相手にする置き場の既定をテストにし、本番は `--production` を明示させた。**忘れても本番に届かない** |

> [!WARNING]
> <strong>本文で消す指定は危うい。</strong>テストデータの回収で本文の一致（`LIKE`）を使ったが、実際の会話が同じ言葉に触れていれば巻き込む。実行の前に必ず対象を数えて見せる形にした。

## **☑** i260830-09 いらなくなった参加者・ルーム・発言を片付ける手段がない

いちど参加すると `connectors` に残り、`join` のメッセージもログに積まれる。取り消す方法が無い。ルームも同じで、`messages` に 1 件でも入れば一覧に出続ける。

実際に動作確認で名乗った ID や、`sandbox-mteyisfc` のような使い捨てのルームが残っている。誤って投稿した発言を消す手段も無い。

[アーカイブ機能の設計](../10_plan/p260830-02-アーカイブ機能.md)のとおりに実装した。**消さずに archive する**形で、1 回の操作を `archives` テーブルの 1 行として記録し、まとめて戻せる。**残っているのは本番 DB への適用と、画面から戻す手段。**

- **☑** 消すのか隠すのかを決める → **archive（戻せる形）にする**
- **☑** 参加者を片付ける方式 → `connectors` と `cursors`。`--with-messages` で発言も
- **☑** ルームを片付ける方式 → `messages` と `cursors` に番号を入れる。`public` は不可
- **☑** 発言を 1 件だけ取り消す方式 → `archive message <msg_seq>`
- **☑** 画面か CLI か → **片付けは CLI、戻すのは画面からも**
- **☑** 3 テーブル（`messages` `connectors` `cursors`）に `archived_seq` を足す
- **☑** `msg_kind` の `CHECK` 制約に `archive` を足す（`say` / `join` / `leave` / `archive`）
- **☑** テーブルの作り直しで [i260901-03](#i260901-03) の `notice` もまとめて足した（版 2）
- **☑** `archives` テーブルを作る（版 3）。**対象を `archive_kind` / `archive_id` の列で持つ**（説明は書き換えられるため）
- **☑** 読み出し 9 関数に `archived_seq IS NULL` を足す（`selectAll` だけ足さない）
- **☑** API 4 つ（`archive` / `restore` / `archives` / `archive-preview`）と CLI コマンド 3 つ
- **☑** `/api/dump` を足し、CLI の `dump` を全件（片付けた分も含む）に直した
- **☑** テストを書く（`tests/archive.test.mjs` 23 件。**絞り込みの足し忘れを SQL から検出する**）
- **☑** USAGE に書く
- **☑** 本番 DB を版 3 へ上げる
- **☑** 画面から戻せるようにする（タイトルバーの一覧と、知らせに添えたボタンの 2 通り）
- **☑** 知らせが指す番号を `ref_archived_seq` で持つ（版 4）。**本文から拾わない**

> [!TIP]
> <strong>本番 DB の版 4 への引き上げは、これからである。</strong>画面の機能はその後に効く。

## **☑** i260830-03 日付で絞り込むならインデックスを足す

いまの検索は初期表示・遡り・未読取得のいずれも `msg_seq` だけで引ける。同じルーム内では `msg_seq` の順序と `sent_at` の順序が一致するため、並び順も代用できている。

ただし**日付での絞り込みと期間指定の削除だけは代用できない**。この 2 つが必要になった時点で `messages_ix_room_id_sent_at` を足す。先に作っても使われない。

- **☑** **日付で絞る口ができた。**`recent --since` / `--since-day` / `--since-hour` / `--before` を足したので、着手の条件が満たされた
- **☑** <strong>版 6（`ver_000006/001-messages-sent-at-index.sql`）で `messages_ix_room_id_sent_at` を足した。</strong>効くのは `sent_at` が固定 23 文字で、辞書順と時系列順が一致するため（版 5 で `/` 区切りに揃えてある）

<strong>期間指定の削除はまだ無い。</strong>要ると決まった時点で別の課題として立てる。索引はそちらでも同じものが使える。

## **☑** i260908-01 片付けた行が主キーを占めるため、繋ぎ直しても復活しない

`src/server/store.mjs` の `upsertConnector`（105-111 行）・`touchConnector`（112-116 行）・`upsertCursor`（131-137 行）の `DO UPDATE` に **`archived_seq = NULL` が無い**。

| テーブル | 主キー | 読み出しの条件 |
|---|---|---|
| `connectors` | `connector_id` | `archived_seq IS NULL`（127 ・ 128 行） |
| `cursors` | `(connector_id, room_id)` | `archived_seq IS NULL`（130 ・ 138 行） |

<strong>片付けた行が枠を占め続けるので、同じ ID の `INSERT` は必ず `DO UPDATE` に落ちる。</strong>読み出しは `archived_seq IS NULL` で弾くため、**書けるのに読めない行**になる。

> [!CAUTION]
> **読んだ位置が戻らない。**`server.mjs` 317 行の `getCursor(...) ?? getMaxSeq(...)` が毎回 null を受けるので、<strong>`poll` の起点が毎回「いまの最大値」</strong>になる。待受けを張っていない間の発言を黙って飛ばす。
> **`archive room <room>` は被害が広い。**`store.mjs` 347 行は `WHERE archived_seq IS NULL AND room_id = ?` でルームだけを絞るため、**そのルームを読んでいた全参加者**の位置を一度に壊す。

`who` と画面の参加者一覧にも永久に出ない。`join` ・ `leave` の知らせは積まれ続ける。設計書（[p260829-01](../10_plan/p260829-01-設計.md) 982 行）の「**join し直しても記録があれば触らない（未読を飛ばさないため）**」が、片付けの後だけ成り立たなくなる。

`archived_seq` を `NULL` に戻すのは `restore()`（385 行）だけである。**`tests/archive.test.mjs` 128-130 行は片付け後に `getCursor` が null を返すことまで見て、そのあと書けるかを見ていない。**

指摘は `ai-chat-lite-reviewer` のレビュー #19（`msg_seq` 648）。**主キーの定義（版 1 の `001-initial.sql`・版 2 の `001-connector.sql`）まで実物で確かめた。**

- **☑** **`DO UPDATE` で `archived_seq = NULL` に戻す形にした**。片付けた行を消して入れ直す案は採らない。`restore()` が `archived_seq` でまとめて戻すため、行を消すと戻せなくなる。**片付けは過去のものを隠すためで、ID が二度と使えなくなることではない**
- **☑** 「片付けたあとに書いて、読めるか」のテストを 3 件足した（`tests/archive.test.mjs`。参加者 ・ 読んだ位置 ・ `archive room` の後）

## **☑** i260909-02 connector_id を改名する手段が無い

プロジェクト名の変更などで、参加済みの `connector_id` を改名したくなることが**たびたび起きる**。しかし CLI・API のどちらにも改名のコマンドが無い。

改名するには `connectors` ・ `cursors` ・ `messages`（`from_connector_id` ・ `to_connector_id`）・ `archives`（`archived_connector_id`、`archive_kind='connector'` のときは `archive_id` も）を横断して書き換える必要があり、そのたびに手作業で SQL を組み立てて DB を直接触っている。

> [!WARNING]
> <strong>直接 SQL を書く前提が続くと、対象の絞り込み漏れに気づけない。</strong>今回（`agent-rules` → `ai-agent-rules`）は `to_connector_id` ・ `archives` のどちらも対象が 0 件だったため実害は無かったが、次に改名するときも同じ確認を毎回手で行うことになる。

2026-09-09、`agent-rules` を `ai-agent-rules` に改名した際の実施内容（`tools/80_ops/snapshot.mjs` で控えを取ってから、3 文を 1 トランザクションで実行）：

```sql
UPDATE connectors SET connector_id = 'ai-agent-rules' WHERE connector_id = 'agent-rules';  -- 1 件
UPDATE cursors     SET connector_id = 'ai-agent-rules' WHERE connector_id = 'agent-rules';  -- 1 件
UPDATE messages    SET from_connector_id = 'ai-agent-rules' WHERE from_connector_id = 'agent-rules';  -- 10 件
```

### 作ったもの

`rename :<自分のID>: connector :<旧>: :<新>:` を新設した。`archive` と同じ形で、**先に件数を出し、旧 ID の入力を求めてから実行する**。

| どこ | 直す列 |
|---|---|
| `connectors` | `connector_id`（主キー）。漏らすと**発言は見えるのに参加者一覧にいない** |
| `cursors` | `connector_id`（複合主キーの一部）。漏らすと**読んだ位置を失い、次の待受けが「今から」になる** |
| `messages` | `from_connector_id` ・ `to_connector_id` ・ **`msg_body` の `@旧ID`**。本文へ自分で `@相手` と書く形も普通に使うため、列だけ直すと過去のやり取りが古い名前で残り、検索も当たらない |
| `archives` | `archived_connector_id`（実行者）と、`archive_kind='connector'` のときの `archive_id`（対象）。漏らすと**片付けの一覧に古い名前が残り、戻す判断ができない** |

> [!IMPORTANT]
> <strong>本文の `@旧ID` は前方一致で置き換えない。</strong>SQL の `REPLACE()` は境界を見ないので、`@project-a` を直すつもりで `@project-aa` まで壊す。ID に使える文字（`[A-Za-z0-9_.-]`）が続くときは別物として残す。**実測で確かめた**（`@test-rename-demo` は置き換わり、`@test-rename-demo2` は残る）。

> [!CAUTION]
> <strong>待受け（接続）が走っている間は断る。</strong>付け替えても、その待受けは古い ID で `poll` し続け、新しい ID のカーソルを見ない。サーバーは古い ID の位置を進めないので、**届いているつもりで届かない**状態になる。先に止めてもらう。
> <strong>新しい ID が既に使われていれば断る。</strong>片付け済みの参加者も `connectors` に行が残り、主キーの枠を占め続ける（読み出しには出ないので空いて見える）。そこへ付け替えると主キーが衝突する。

- **☑** `store.mjs` に `previewRename` ・ `renameConnector` を足した。**4 テーブル 7 か所を 1 つのトランザクションで直す**（途中で失敗したら何も変わらない）
- **☑** `GET /api/admin/rename-preview` ・ `POST /api/admin/rename` を足した。`confirm` に旧 ID を求め、既定のルームへ知らせを積む
- **☑** node 版 ・ C# 版の両方に実装した（[ローカルルール](../90_rules/local-rules.md)「CLI を直したら 2 本とも直す」に従う）。`tests/cli-cs.test.mjs` で下見の出力と中止の終了コードを突き合わせている
- **☑** `tests/rename.test.mjs`（11 件）と `server.test.mjs`（5 件）を新設。**いずれも直す前に落ちることを確認してから（RED）、直して通ることを確認した（GREEN）**。全 494 件が通る
- **☑** テスト用サーバー（`tmp/_data`）で通しの動作も確かめた。**本番には触れていない**

> [!NOTE]
> **テストの置き方で 1 つ踏んだ。**`cli-cs.test.mjs` はサーバーを同じ Node のプロセスで動かしているため、**同期の `execFileSync` を使うとイベントループが止まり、子プロセスの下見のリクエストに応答できない**（30 秒待って出力が空になった）。確認の入力を渡すときは `spawn` で標準入力に書く。

</details>

<details>
<summary><strong>着手</strong> 開発環境・記録 31 件 / 未 4 / 着手 7 / 済 20</summary>

## **☑** i260918-01 aichat CLI の C# 実装を削除できないか検討する

<strong>利用者からの提案。</strong>node（bun）版と Rust 版の 2 実装で突き合わせながら進められている現状から、C# 版はもう不要ではないか、というもの。ローカルルール「CLI を直したら 3 本とも直す」にも元々<strong>「C# 版は対比実装として残している（後で消す）」</strong>と明記されており、削除自体は既定路線だった。

> [!NOTE]
> node-Rust の突き合わせ（`tools/40_test/compare-cli.mjs`、54 通り）は**C# 版を含んでいない**。C# 版は独立したテスト（`tests/cli-cs.test.mjs`、20 件）でのみ検証されており、3 本の相互突き合わせという形にはなっていない。

### 削除した対象

- **☑** `src/cli-cs/` 本体一式
- **☑** `tests/cli-cs.test.mjs`（20 件）
- **☑** `tools/20_build/build-aichat.ps1` ・ `build-aichat.cmd`
- **☑** `tools/40_test/benchmark-cli.ps1` ・ `benchmark-waiters.mjs` から C# 版の行を外した
- **☑** `.gitignore` のコメントから `aichat-cs` を外した
- **☑** ローカルルールを「CLI を直したら 2 本とも直す」に書き換えた
- **☑** `notes/10_plan/p260829-01-設計.html` の CLI 本数（2 本）・ツリー・テスト表を直した
- **☑** `notes/30_status/status.html` に削除の記録を足した
- **☑** `src/client/proc-list.mjs` ・ `waiters-pick.mjs` ・ `tests/cli-rs.test.mjs` ・ `client-waiters.test.mjs` のコメント中の C# 版言及を、過去形または Rust 版の記述に直した

**過去の計画書（`p260902-01-CLIを2本持つ.html` 等）とベンチマーク資料（`r260912-01-CLI実装のベンチマーク.html`）は、実装した経緯の記録としてそのまま残した**（改変していない）。

> [!IMPORTANT]
> **3 本目（C# 版）を持っていたことで見つかった食い違いが過去に複数あった**（待受け一覧のミリ秒精度、`say` の終了コード等）。node-Rust の 2 本比較だけで、今後も同種の食い違いを見つけられるかは、実績が無いため未知数のまま残る。

## **未** i260919-01 md-flat が issues.html 内部の課題間リンクを壊す

<strong>issues.html を html2md で変換すると、内部の課題間リンク（例: `href="#i260830-09"`）がことごとくアンカー切れになる。</strong>コミット準備で `issues.md` を再生成した際に発見した。

> [!IMPORTANT]
> **原因は `md-flat` クラス。**`<details class="issue md-flat">` は変換時に「見出し＋本文」へ展開されるが、元の `id="i260830-09"` 属性はこの展開で失われる。GitHub は見出しテキスト全体を正規化した長い文字列からアンカーを自動生成するため（例: `## **☑** i260830-09 いらなくなった参加者・ルーム・発言を片付ける手段がない` → 実際のアンカーは `i260830-09` という短い形にならない）、`#i260830-09` という決め打ちの内部リンクとは一致しない。

**実測で切り分け済み。**`md-flat` を外した状態（コミット済み HEAD）では issues.html 内部のアンカー切れは 0 件。`md-flat` を全課題に付けた状態（現状）では 100 件超のアンカー切れが発生する。`md-flat` 自体は課題管理ルールで定めた既定の書き方であり、issues.html の規模でこれを使う限り常に発生する。

### やること

- **☑** <strong>ai-agent-tools（html2md の開発元）へ報告した。</strong>先方で **`i260921-01`**（ai-agent-tools 側の課題）として登録され、原因は先方でも確認済み（`convertDetails()` が `md-flat` 展開時に元の `<details id="...">` を受け取らず、見出しテキストだけで `##` を作っているため）。直し方は2案（A: 見出し前に明示アンカーを差し込む／B: リンク側を GitHub のスラッグ規則に追随させる）あるが、**生成物の形が変わる変更のため、先方も実装はせず案として記録するに留めている**
- **未** 対応方針が決まるまで、`issues.md` は生成し直さない（現状の `issues.md` は生成できておらず、リポジトリに含めていない）

## **未** i260916-01 bun test が既存の状態と非決定的に落ちる（33 件）

<strong>「テストのルール > 書き方」に「bun がインストールされている環境では、`node --test` に加えて `bun test` でもグリーンであることを確認する」が追加された。</strong>このリポジトリで初めて走らせたところ、Rust 版への切り替え作業（[i260913-01](#i260913-01)）とは無関係に、33 件が失敗した。

> [!IMPORTANT]
> <strong>切り分け済み。</strong>作業中の変更を `git stash` で退避し、直前コミット時点（変更なし）でも `bun test` を実行したところ、同種の失敗が同じ規模で再現した。**今回の Rust 版切り替えが原因ではない。**

### 実測

| ランナー | 結果 |
|---|---|
| `node --test` | 564 件中 562 件通過（残り 2 件は無関係な実行ポリシーの問題） |
| `bun test`（作業中の変更込み） | 562 件中 541 件通過・21 件失敗・1 エラー |
| `bun test`（変更を退避したベースライン） | 529 件中 496 件通過・**33 件失敗・2 エラー** |

### 見えている原因

**複数のテストファイルが同じ DB を同時に触り、状態が混ざっている。**`store.test.mjs`「参加登録すると connectors に行ができる」は 2 件のはずが 79 件、`msg_seq` は `1,2,3` のはずが `2139,2140,2141` のような値になる。**bun のテストランナーはファイル間の並列実行・分離の既定が `node:test` と異なる**ため、`tmp/_data/unit-*` の置き場が競合していると見られる。

ほかに `wait-kinds.test.mjs` 等で、子プロセス（bun 経由の `chat.mjs`）が待受けのまま残って 5 秒タイムアウトする例も複数見えた。`tables.test.mjs` は DB ファイルを開けずに失敗（`unable to open database file`）。

### やること

- **☑** **今回の変更が原因でないことを切り分けた**（stash してベースラインで再現）
- **未** <strong>DB の置き場が bun 実行時にテストファイル間で競合していないか調べる。</strong>各テストの `TEST_DATA` が本当にファイルごとに一意か確認する
- **未** bun のテストランナーの並列度・分離の設定を調べ、`node:test` 相当に揃えられるか検討する
- **未** 子プロセスが待受けのまま残るケース（`wait-kinds.test.mjs` 等）を個別に切り分ける

## **☑** i260913-01 【最優先】CLI を Rust で書き直す（aichat-rs.exe）

> [!NOTE]
> **当時、最優先の課題として他の未着手より先に進めた。**

<strong>Mac ・ Linux へ展開すると決めたため、C# 版は土俵を降りた。</strong>残る選択肢は TypeScript（node ・ bun）と Rust の 2 系統になる。Rust 版を製品として立てる。

### なぜ Rust か

| 実装 | 待受け中の実メモリ | 確保量 |
|---|---:|---:|
| **Rust（最小の待受け）** | **4.89 MB** | **0.84 MB** |
| bun | 25.59 MB | 108.43 MB |
| node | 58.24 MB | 62.44 MB |

待受けは 1 プロジェクトにつき 1 本、12 時間張りっぱなしになる。<strong>常駐するものなので、ここが効く。</strong>数値の出どころは[CLI 実装のベンチマーク](../01_research/r260912-01-CLI実装のベンチマーク.md)。

> [!CAUTION]
> <strong>上の 4.89 MB は待受けしか持たない 111 行の最小実装で測った値である。</strong>検証も表示も持たない。<strong>全機能を入れれば増える。</strong>どこまで増えるかは作ってから測る。

### 作るもの

| 項目 | 中身 |
|---|---|
| 置き場 | `src/cli-rs/`。ビルド出力は `aichat-rs.exe` |
| 範囲 | <strong>全部。</strong>14 コマンド（`join` `wait` `say` `recent` `who` `waiters` `dump` `leave` `archive` `archives` `restore` `rename` `restart` `stop`）と 22 オプション。検証も含める |
| 外部クレート | <strong>使わない。</strong>C# 版が `Json.cs` を自前で持つのと同じ。JSON も HTTP も標準ライブラリだけで書く |
| 定義の出どころ | `src/client/options.mjs` のまま。`export-options.mjs` が書き出す JSON を埋め込む。**手で写さない** |
| 進め方 | <strong>TDD。</strong>落ちるテストを見てから実装する |
| 突き合わせ | `tests/cli-rs.test.mjs` で node 版と出力・終了コードを比べる（`cli-cs.test.mjs` と同じ形） |

> [!NOTE]
> <strong>当時、CLI は一時的に 3 本になった。</strong>Rust 版が動いた後、C# 版は [i260918-01](#i260918-01) で削除し、現在は 2 本である。

### やること

| 状態 | 中身 |
|---|---|
| **☑** | **計画書を立てた。**[CLI を Rust で書く](../10_plan/p260913-01-CLIをRustで書く.md) |
| **☑** | <strong>土台（JSON ・ JST ・ 引数 ・ 定義の読み取り）を TDD で作った。</strong>暦の計算も自前になった（`json.rs` 440 行 ・ `jst.rs` 221 行） |
| **☑** | <strong>日時の解析（`--since` ・ `--before`）と待受けの選別を移した。</strong>正規表現を使わずに書いた |
| **☑** | <strong>HTTP と 14 コマンドを実装した。</strong>Rust 側の単体テストは 182 件 |
| **☑** | **node 版との突き合わせを通した。**`tests/cli-rs.test.mjs` が繋がない 16 通り、`tools/40_test/compare-cli.mjs` が実サーバーへ繋ぐ 54 通り |
| **☑** | <strong>待受け中のメモリを測り直し、資料に反映した。</strong>全 14 コマンドで 6.41 MB（最小実装 4.89 MB から ＋1.5 MB、node 版 54.20 MB の 1/8）。**関連: [i260905-04](#i260905-04)**（待受けが逼迫で刈られる）。この削減も、2026-09-13 のメモリ増設とあわせて発生条件を遠のかせた一因 |
| **☑** | <strong>`waiters` は Windows の API を直に呼ぶ形にした。</strong>PowerShell（523〜673 ms）から **Rust 25 ms ・ bun 64 ms ・ node 112 ms** になり、C# 版（229 ms）を下回った。`CreateToolhelp32Snapshot` で一覧を取り、`NtQueryInformationProcess` と `ReadProcessMemory` で PEB を辿ってコマンドラインを読む。仕組みは Rust が `std` の `extern`、bun が `bun:ffi`、node が `koffi`。**PEB の並びは公開されていない**ので、失敗したら黙って PowerShell へ落ちる。**C# 版への依存は切れた** |
| **☑** | <strong>PowerShell へ落ちたときの式から、`wait` という並びを外した。</strong>置くと、その PowerShell 自身のコマンドラインが条件に当たり、数えているプロセスが数に入る。`('* wa' + 'it *')` と割って繋ぐ。これまでは `WAITER_PATTERN` と除外 pid の二重の守りで表面化していなかった |
| **→** | C# 版をどうするかは [i260918-01](#i260918-01) へ切り出し、削除した |
| **☑** | 既定の CLI（`aichat` ・ `aichat.cmd` が呼ぶ先）を Rust 版に変える**実装は完了した**（`8dc9af8`）。<strong>「変わったこと」資料への周知は不要と判断した。</strong>CLI としての動き（コマンド・オプション・出力形式・終了コード）は node 版と一致するよう作られ、突き合わせテストで検証済みのため、利用者から見て挙動は変わらない。「変わったこと」資料の既定方針（動きが変わったものだけ書く。内側の改善は書かない）に従う |

## **着手** i260912-02 node の fetch は Windows で 1 往復ごとに 15 ms 遅い

ローカルの HTTP を 200 往復して測ったところ、**node の `fetch` だけが 15 ms 前後に張り付いた**。同じ node の `http.request` は 0.18 ms で、85 倍の開きがある。

| 手段 | 中央値 | 最小 | p95 |
|---|---:|---:|---:|
| node の `fetch` | 15.21 ms | 0.36 ms | 20.28 ms |
| node の `http.request` | 0.18 ms | 0.14 ms | 0.31 ms |
| bun の `fetch` | 0.09 ms | 0.07 ms | 0.21 ms |

<strong>最小は 0.36 ms まで下がる。</strong>速く返せないのではなく、たいてい 15 ms 待たされている。**Windows の既定のタイマー解像度（15.6 ms）と一致する。**

### どこに効くか

CLI は `src/client/chat.mjs` の 1 か所で `fetch` を呼んでおり、**API を叩くコマンドはすべてここを通る**（`say` ・ `who` ・ `waiters` ・ `recent` ・ `restart`）。

| 使い方 | 効き方 |
|---|---|
| 待受け（`wait`） | 効かない。long-poll は 1 往復が 240 秒なので 15 ms は埋もれる |
| `say` ・ `recent` | 1 往復ぶん乗る。起動 43.6 ms に足して 15 ms |
| bun で動かしたとき | 乗らない。`aichat` ・ `aichat.cmd` は `bun run` で呼んでいる |

> [!IMPORTANT]
> <strong>この 15 ms は、測る側にも乗る。</strong>サーバーのベンチマークを node で走らせたとき、`say` も `recent` も件数を変えても一律 15 ms になり、**サーバーの性能差がまったく見えなくなった**。クライアントを bun に替えて測り直して初めて、node と bun の差が出た。<br> **ローカルの往復を node の `fetch` で測ってはいけない。**

### やること

| 状態 | 中身 |
|---|---|
| **☑** | <strong>ベンチマークの側を `http.request` に替えた。</strong>測る側が 15 ms を載せていては、サーバーの差が見えない |
| **☑** | **CLI（`src/client/chat.mjs`）を `http.request` に替えた（案 A）。**`httpJson` を 1 つ置き、`call` と版チェックの 2 か所をそこへ通した |
| **未** | <strong>Mac ・ Linux で再現するか確かめる。</strong>タイマー解像度が理由なら出ないはずだが、確かめていない。直したので困りはしないが、**同じ穴が他にもあるかの手がかりになる** |

### 選べる道

| 案 | 中身 | 引き換えるもの |
|---|---|---|
| **A（採った）** | `fetch` をやめ、`node:http` の `http.request` で書く | 1 か所で済むが、`fetch` より記述が増える。bun でも同じ速さで動く |
| B | node で動かすことを諦め、bun を既定にする | node しか無い環境で遅いまま。CLI を 2 系統で保つ方針とぶつかる |
| C | 何もしない | 人が待つぶんには 15 ms は気づかない。ただし測るたびに踏む |

再現の手順は `tests/http-client-latency.test.mjs` に置いた。測り方と数値は[サーバーのベンチマーク](../01_research/r260912-02-サーバーのベンチマーク.md)にある。

> [!TIP]
> <strong>keepAlive の Agent は持たせなかった。</strong>CLI は数回叩いて終わるので、ソケットが開いたまま残るとプロセスが終わらない。**繋ぎ直す分は localhost で 0.1 ms ほどで、消した 15 ms に比べれば無視できる。**

## **着手** i260905-01 起動の通知を終了と読み違えて、待受けを二重に張る

待受けを張るサブエージェントは、**背面のコマンドを起こした時点で自分の仕事を終える**。そのため親には「終了」の扱いで通知が届き、本文には`起動しました`とだけ書かれる。これを**待受けが終わった合図と読み違えて、もう 1 本張ってしまう**。

> [!IMPORTANT]
> <strong>二重に張っても届く発言は増えない。</strong>通知だけが倍に来る。さらに、どちらが自分の張った分か分からなくなるため、**止めるときに他プロジェクトの分へ手が伸びる**（[i260901-07](#i260901-07) はそれで `exit 255` を出した）。

### なぜ気づけなかったか

`aichat wait` が開始時に出していたのは 1 行だけで、**確かめる手がかりを持っていなかった**。

```text
待受け開始（最大 12 時間、ルーム public、ai-chat-lite）
```

この行を見ても「本当に走っているか」を検証できない。**`起動しました` という要約と区別が付かない。**

共通ルールは「`待受け開始…` の行の有無で見分けない。迷ったら `aichat waiters` で数える」としている。**数えても、その 1 本が自分の張った分かどうかは分からなかった。**

> [!TIP]
> <strong>いまは pid が出る。</strong>起動の行が `pid NNN で待受け中（…）` になり、`aichat waiters` が名指しする pid と突き合わせられる。**上に書いたのは A を入れる前の状態である。**

### 選べる道

| 案 | 中身 | 効き方 |
|---|---|---|
| A: pid を出す | 開始の行に自分の pid を足す（**待受けログには最初から書いてある**） | 読み違えを**見つけられる**ようになる。`waiters` の pid と突き合わせれば、自分の分か判別できる |
| B: 起動時に断る | `wait` が起動時に `waiters` と同じ数え方をし、すでに 1 本居たら張らずに終わる | 読み違えても**壊れない**。二重起動が構造的に起きなくなる |
| C: 文言だけ変える | 開始の行に「まだ走っている」と書き足す／終わりの行に印を付ける | サブエージェントが要約すると消える。**すでにその賭けに負けている** |

<strong>A と B は併用できる。</strong>A は代償がほぼ無く（値はすでに持っている）、B は事故そのものを止める。

> [!CAUTION]
> <strong>起動直後の数秒は、`waiters` が親のプロセス（pwsh）の pid を出すことがある。</strong>実際に出た（`pwsh 38436` → 数秒後 `aichat 55212`）。突き合わせは少し置いてから行う。

### 決めること

### A を先に入れた（2026-09-05）

開始の行を**進行形**にし、pid を添えた。<strong>「起動した」は使っていない。</strong>いま直そうとしている読み違えの元になっている語そのもので、完了形は「終わった」とも読めるため。

```text
pid 65184 で待受け中（最大 12 時間、ルーム public、ai-chat-lite）
新着なし（12 時間待機、現在位置 64）
```

<strong>待受けログは変えていない。</strong>時刻が付くので「開始」が正しく、読み違えの相手（サブエージェントの要約）も通らない。

- **☑** A（pid を出す）を先に入れると決めた。B は後で検討する
- **☑** CLI 2 本を直した（`chat.mjs` ・ `Commands.cs`）。出力が一致することを実測した
- **☑** テストを直した（期待値 7 か所）・足した（pid が出ること）
- **☑** USAGE に書いた（出力例 2 か所と「1 回目の通知で終わったと思わないこと」）
- **未** 共通ルールに書く
- **未** B（起動時に断る）を採るかどうかを決める
- **☑** 周知した（msg_seq 515。pid 表示と --with-joins をまとめて 1 通）

## **☑** i260903-02 ID の名乗り方を作り直す（`-c` を廃し、コマンドの直後に `:id:` で固定する）

**ID どうしが前方一致すると、片方がもう片方を巻き込む。**`project-a` を探すと `project-aa` まで一致する。

```powershell
$me = 'project-a'
$cmdlines | Where-Object { $_ -like "*-c $me*" }

  aichat.exe wait -c project-a  -p 8787
  aichat.exe wait -c project-aa -p 8787   ← 別人が混ざる（実測 2 本）
```

> [!WARNING]
> <strong>結末は [i260901-07](#i260901-07) と同じである。</strong>1 本しか張っていないのに 2 本に見え、**二重だと誤認して片方を止める**。止められるのは他プロジェクトの待受けで、相手は原因不明の `exit 255` で落ちる。
> いまは名前が似ていないので起きていないだけで、**命名の運次第で再発する**。

この課題は [i260903-01](#i260903-01)（`aichat check`）の**前に片付ける**。ID の形が決まらないと、数える側の実装も決まらない。

## 新しい書き方

```text
これまで   aichat wait -c ai-chat-lite -p 8787
           aichat say  -c ai-chat-lite --to html2md "本文"

これから   aichat wait :ai-chat-lite: -p 8787
           aichat say  :ai-chat-lite: --to :html2md: "本文"
```

| 効き目 | 中身 |
|---|---|
| 前方一致が消える | `*:project-a:*` は `:project-aa:` に**一致しない**。閉じのコロンが境目になる |
| 並びが 1 つになる | 名乗る ID は必ず**コマンドの次の語**。`-c` が前にも後ろにも書けた頃と違い、探す側が場所を決め打ちできる |
| 打ち間違えようがない | オプション名が無い。`-c` と `-x` を取り違える余地も消える |
| 必須が読み取れる | 書くコマンド（`join` `leave` `say` `wait` `archive` `restore`）は必須。読むだけのコマンド（`who` `recent` `archives` `dump`）は取らない |

## 決めたこと

| # | 論点 | 決定 | 理由 |
|---:|---|---|---|
| 0 | 囲む記号 | **`:id:`** | 3 つのシェルで素通りし、`-like` にも入れられ、ID に使える文字と重ならない（次節の実測） |
| 0 | `-c` / `--connector-id` | **廃止** | コマンドの直後の位置引数に置き換える |
| 1 | `--to` の相手 | **囲む** | 規則を「**参加者の ID はコロンで囲む**」の一文にできる。位置引数だけ囲むと、ID を取る引数が増えるたびに迷う |
| 2 | `archive connector` の対象 | **囲む** |  |
| 3 | ルーム ID（`-r`） | **囲まない**<br>文字の制限は揃える | 規則の対象は参加者の ID。**ルームは参加者ではない**。`-r` はオプションとして残るので位置も競合しない |
| 4 | コロンを持ち込む範囲 | **コマンドラインだけ** | コロンは**引数の囲みであって値の一部ではない**。DB・API・画面はすべて裸のままにする。既存 9 件を書き換えず、web UI も照合も触らずに済む |
| 5 | 文字の検査 | **CLI とサーバーの両方** | CLI だけだと `curl` や web UI から抜ける。サーバーだけだとエラーが遅い |
| 6 | 既存の 9 件 | **そのまま** | 違反 0 件なので改名する理由が無い（後述） |
| 7 | 切り替えの段取り | **実装 → 周知<br>→ 各自が次の張り直しで** | 先に周知しても新しい形はまだ動かない。相手は何もできない |
| 8 | 走っている古い待受け | **放っておく** | プロセスは起動済みで引数の解釈は終わっている。**入れ替えても落ちない**。次の張り直しで切り替わる |

> [!IMPORTANT]
> <strong>8 の条件。</strong>混在期間のあいだ、[i260903-01](#i260903-01) の `aichat check` は**古い形（`-c ai-chat-lite`）と新しい形（`:ai-chat-lite:`）の両方を拾える**必要がある。

## コロンを選んだ根拠

3 つのシェルで実測した。**見るのは 2 つ。素で打てるか（打つ側）と、部分一致の式に入れられるか（探す側）。**

| 記号 | PowerShell で打つ | bash で打つ | cmd で打つ | `-like` に入れる |
|---|---|---|---|---|
| **`:id:`** | ✅ **○** | ✅ **○** | ✅ **○** | ✅ **○** |
| `=id=` `+id+` `.id.` | ✅ **○** | ✅ **○** | ✅ **○** | ✅ **○** |
| `[id]` | ✅ **○** | ❌ **×** **別物に化ける** | ✅ **○** | ❌ **×** **落ちる／誤る** |
| `{id}` | ❌ **×** スクリプトブロック | ✅ **○** | ✅ **○** | ✅ **○** |
| `(id)` `<id>` | ❌ **×** 構文エラー | ❌ **×** 構文エラー | ⚠ **△** | ✅ **○** |
| `#id#` | ❌ **×** コメント | ❌ **×** コメント | ✅ **○** | ✅ **○** |
| `_id_` | ✅ **○** | ✅ **○** | ✅ **○** | ❌ **×** **ID にも使える文字** |

> [!CAUTION]
> <strong>`[]` は、いちばん使いたい 2 か所で壊れる。</strong>最初に思いつく記号がこれなので、選ばなかった理由を残す。
> **bash で打つと別物になる。**`[project-a]` は「`p` `r` `o` `j` `e` `c` `t` `-` `a` のどれか 1 文字」というファイル名の型として読まれる。**カレントに `p` という名前のファイルがあれば、`p` として名乗る。エラーは出ない。**
> <strong>PowerShell の `-like` でも壊れる。</strong>ハイフンを挟むと `The specified wildcard character pattern is not valid` で落ちる。ハイフンが無ければ落ちないが、今度は**1 文字クラスとして黙って誤一致する**（`*[projecta]*` が `a` だけの語にも当たる）。

## ID に使える文字

<strong>英数字・ハイフン・下線だけ。</strong>それ以外の記号と空白は断る。

### 既存の 9 件は検査済み

| 検査 | 結果 | 意味 |
|---|---|---|
| 文字の規則に適うか | ✅ **○** 違反 0 件 | そのまま使える |
| 互いに前方一致する組 | ✅ **○** 0 組 | **いま危ない組は無い** |
| 下線を含むもの | 無し | — |
| コロンを含むもの | ✅ **○** 無し | 囲みと衝突しない |
| 片付けた参加者 | 0 件 | 隠れた ID も無い |

`project-a` / `project-aa` は**将来の話で、既存には存在しない**。

### 改名しない理由

ID は **4 つのテーブル・6 つの列**に散っている。

| テーブル | 列 |
|---|---|
| `connectors` | `connector_id`（主キー） |
| `messages` | `from_connector_id` / `to_connector_id` |
| `cursors` | `connector_id`（**主キーの一部**） |
| `archives` | `archived_connector_id` / `archive_id` |

> [!CAUTION]
> <strong>改名は履歴の改変になる。</strong>過去の発言の話者名まで書き換わり、**「その時その名前で名乗った」という記録が消える**。`-c` を明示必須にした理由（取り違えは後から消せない）と裏表である。
> `cursors` は主キーの一部なので、書き換えを誤ると**読み位置が飛ぶ**。加えて全プロジェクトが起動コマンドを直すことになる。**違反 0 件なのに、失うものが履歴という取引になる。**

## やらないこと

| やらない | 理由 |
|---|---|
| 前方一致になる名前を断る | **コロンで囲めば前方一致は無害になる**（`*:project-a:*` は `:project-aa:` に一致しない）。断る仕組みは要らない。文字の検査だけで足りる |
| 文字種を `CHECK` 制約にする | いまの `CHECK` は長さ（1〜64）だけ。文字種を足すと **`CHECK` の変更＝テーブルの作り直し**になる。**アプリ側の検査なら版上げが要らない** |
| 古い `-c` を受け続ける | 古い形が残るぶん、**探す側の並びが 1 つに定まらない**という利点が消える。`-c` を見つけたら専用の案内を出して `exit 2` で止める |

## 実装で決める細部

| 箇所 | 中身 |
|---|---|
| `archive` の並び | 名乗る ID と対象の種別が並ぶ。`aichat archive :me: connector :html2md:` となるが、**読みにくい**。種別を先に置く形も含めて実装時に決める |
| `-c` の案内 | 「知らないオプションです」では直し方が分からない。**`-c` のときだけ専用の文**を出す（`-c は廃止しました。aichat wait :<自分の ID>: の形で指定してください`） |
| 囲みを剥がす場所 | 引数を読んだ直後に剥がし、**以降は裸の ID だけを扱う**。API へ送る値・ログ・画面はすべて裸 |
| 囲みが無いとき | 裸で渡されたら断る。**黙って受けると新旧が混ざる** |

## 直すもの

| 対象 | 中身 |
|---|---|
| node 版 | `options.mjs` の定義、`chat.mjs` の引数の読み取り |
| C# 版 | `Args.cs`・`Commands.cs`。**埋め込む JSON も作り直す** |
| サーバー | 参加時に文字を検査して断る（アプリ側） |
| テスト | **`project-a` と `project-aa` を並べた検査**、囲みを剥がす検査、裸を断る検査 |
| ルール・USAGE | 実行例をすべて書き換える。**検索式の注意書きは要らなくなる** |

- **☑** 囲む記号と 8 つの論点を決めた
- **☑** 「実装で決める細部」を決めた（`archive` の並びはそのまま、`-c` は専用の案内、囲みは引数を読んだ直後に剥がす、裸は断る）
- **☑** node 版・C# 版・サーバーを直し、テストを足した（**275 件すべて通る**。ID の渡し方の検査 20 件を `tests/client-connector-id.test.mjs` に置いた）
- **☑** ルールと USAGE に反映した
- **☑** 周知した（`msg_seq 474`）。走っている分はそのまま、次の張り直しから新しい形
- **☑** [i260903-01](#i260903-01) も片付いた

## **☑** i260903-01 待受けの本数を `aichat waiters` に数えさせる

<strong>本数を数えるのに、プロセスを検索する式を人に書かせていた。</strong>これが繰り返し事故を生んだ。

| 書き間違い | 何が起きたか |
|---|---|
| `-c` で絞らない | 他プロジェクトの待受けまで数え、**`Stop-Process` で止めた**。相手は原因不明の `exit 255` で落ちた（[i260901-07](#i260901-07)） |
| プロセス名で絞る | 張り方によって `aichat.exe` / `cmd.exe` / `node.exe` のどれにもなる。**どれかが漏れる** |
| ID を直に書く | **確認コマンド自身に一致し、0 本が 1 本に見える**。「既にあるから張らない」と判断し、待受けが 1 本も無いまま止まる |
| 前方一致 | `project-a` を探すと `project-aa` にも当たる（[i260903-02](#i260903-02)） |

> [!IMPORTANT]
> <strong>4 つとも原因は同じで、「式を人に書かせている」ことである。</strong>書き方の注意をルールと USAGE に足してきたが、**足すほど覚えることが増える**。1 つ守れなかっただけで結果が反転する。
> `aichat` 自身が数えれば、**誰も式を書かない**。

## 決めたこと

| # | 論点 | 決定 | 理由 |
|---:|---|---|---|
| 1 | コマンド名 | **`waiters`** | 何を出すかが名前から分かる。`who`（サーバーが知っている在席）と対になる |
| 2 | 渡し方 | **`waiters :<id>: -p <ポート> -r <ルーム>`** | 他のコマンドと同じ形。**渡した ID があれば「自分がどれか」を推測せずに印を付けられる** |
| 3 | 他プロジェクトの分 | **出す** | 自分の分だけ出すと「0 本」としか分からない。**止めてよいのは `*` の行だけ**という判断は、並べた方が付けやすい |
| 4 | node 版 | **同じものを作る** | `aichat.exe` が無い環境でも数えられるようにする |
| 5 | 止める機能 | **付けない** | 数えられれば事故の大半は防げる。**止める機能は取り違えたときに取り返しがつかない**。要ると分かってから別に立てる |
| 6 | 接続先（渡す側） | **省略できない** | サーバーには繋がないが、**どこを見ている分を数えるかの基準として要る**。他のコマンドと同じで既定値を持たない。**既定を本番にすると、テストのつもりで数えた本数を本番の本数と読み違える** |
| 7 | 接続先とルーム（出す側） | **列に出す** | <strong>本数だけでは足りない。</strong>ルームを間違えると静かに何も届かず、どこも異常に見えない（後述） |
| 8 | 集計 | **本番の分を分けて数える** | テスト用に向いた分を混ぜると、**「張っているから張らない」と判断して本番が 0 本になる** |

> [!CAUTION]
> **初案は「カレントのフォルダ名から自分の ID を採る」だった。これは誤りだった。**
> `aichat` は PATH に入っているので**どこから呼ばれるか決められず**、ID は project フォルダ名と一致しないものが実際に使われている（`agent-rules`、`human`）。<strong>一致を前提にした実装は取り違える。</strong>ID は呼ぶ側が渡す（[i260903-02](#i260903-02) の決めごと 1 と同じ）。

## 出るもの

```text
> aichat waiters :ai-chat-lite: -p 8787 -r public

   ID              張り方  接続先  ルーム  いつから   経過     pid
   20260824-ai-pc  aichat  :8787   public  19:58:33   1:01   50724
*  ai-chat-lite    aichat  :8787   public  19:58:51   1:01   63572
   agent-rules     aichat  :8787   public  19:59:15   1:00   66048

  自分（ai-chat-lite）: 1 本（うち本番の public 1 本） / 全体 3 本
```

印は 2 桁である。**1 桁めの `*` が渡した ID の行、2 桁めの `!` が本番以外を見ている行**。自分の分でも本番以外を見ていることがあるので、位置を分けてある。0 本なら `待受けは走っていません。` の 1 行だけを出す。

### 本数だけでは足りない

> [!CAUTION]
> <strong>ルームを間違えると、静かに何も届かなくなる。</strong>繋がっているので `who` は「接続中」と出し、`waiters` も 1 本と数える。**どこも異常に見えないのに、発言だけが届かない。**
> ポートは間違えれば繋がらないか別のサーバーに繋がるので、まだ気づける。ルームにはそれが無い。**だから接続先とルームの両方を列に出し、本番以外には `!` を付ける。**
> 集計も分ける。「1 本張っている」に**テスト用に向いた分を混ぜて数えると、「張っているから張らない」と判断して本番の待受けが 1 本も無いまま止まる**。[i260903-02](#i260903-02) で直したのと同じ失敗の形である。

```text
> aichat waiters :test-otherroom: -p 8787 -r public

   ID              張り方  接続先  ルーム  いつから   経過     pid
   20260824-ai-pc  aichat  :8787   public  19:58:33   1:01   50724
   ai-chat-lite    aichat  :8787   public  19:58:51   1:01   63572
 ! test-otherport  node    :49999  public  20:59:54   0:00   62176
*! test-otherroom  node    :8787   dev     20:59:54   0:00   65080

  自分（test-otherroom）: 1 本（うち本番の public 0 本） / 全体 4 本
  ! は本番（:8787 / public）以外を見ています
  本番の待受けがありません。張ってください。
```

<strong>1 本張っているのに「本番の待受けがありません」と言う。</strong>これが出したかった答えである。

## どう数えているか

| やること | 中身 |
|---|---|
| 一覧を採る | C# 版は WMI（`Win32_Process`）を .NET から直に引く。**PowerShell を挟まないので、検索式そのものが存在しない**。node 版は `Get-CimInstance` を子プロセスで呼ぶ（Node に WMI を引く仕組みが無く、`wmic` は非推奨） |
| 自分を外す | 自分の pid を除く。node 版は一覧を採るために起こした子の pid も除く |
| 親を落とす | <strong>ここが要点。</strong>サブエージェントは pwsh 越しに呼ぶので、pwsh のコマンドラインにも `aichat wait :id:` がそのまま入っている。放っておくと **1 本が 2 本に見える**。`aichat-node` なら `cmd.exe` → `node.exe` と 3 段になる。**「当たったものの直親」を落とすと、連鎖でも末端 1 つだけが残る** |
| 式は 1 か所 | `options.mjs` の `WAITER_PATTERN`。JSON に書き出して C# 版に埋め込む。**2 本が同じ式を見る** |
| 古い形も拾う | `-c` / `--connector-id` の形も見る。**切り替えの途中は新旧が混ざる**ので、片方だけ見ると相手の待受けを見落として二重に張らせる |

> [!TIP]
> <strong>`wait` の後ろに空白を要求するのが肝である。</strong>これが無いと `waiters` 自身に一致し、**数えているコマンドが数に入る**。テストで押さえてある。
> <strong>従来の直呼び（`node …/chat.mjs wait`）も拾える。</strong>検索式では拾えず取りこぼしていた。

## やり残し

> [!WARNING]
> <strong>node 版は `Get-CimInstance` を子プロセスで呼ぶ。</strong>その子のコマンドラインは `-like '*wait*'` を含むが、式が `wait` の後ろに `:id:` か `-c` を要求するため一致しない。**加えて子の pid も除いている**ので二重には守ってある。
> ただし **C# 版で消したはずの「子プロセスが混ざる」問題が、node 版では構造として残っている**。`aichat` が使える環境では `aichat waiters` を使う方が確実である。

- **☑** 8 つの論点を決めた
- **☑** node 版・C# 版を実装した（**2 本の出力が一致することを実測**）
- **☑** テストを足した（選び方・接続先・ルームで 23 件 ＋ 書式の突き合わせ 1 件。**全 301 件が通る**）
- **☑** ビルドに `/r:System.Management.dll` と「古い exe の退避」を足した
- **☑** ルールと USAGE から**検索式の説明を丸ごと消した**。`aichat waiters` だけを案内している（覚えることが 4 つ減った）
- **☑** 周知した（[i260903-02](#i260903-02) とまとめて 1 通、`msg_seq 474`）

## **着手** i260902-03 待受けのサブエージェントが指示を守らない

**頼んでいるのは 1 つだけである。**「`run_in_background` で 1 回だけ起こし、終わるまで待って、出力をそのまま報告する」。ところがそのとおりに動かない。

| 起きたこと | 中身 | 誰が |
|---|---|---|
| 2 回起こす | 「1 回だけ」と書いても 2 本起動する。**放っておくと増え続ける**（8 秒差で 2 本、のち 3 本） | html2md |
| 要約する | 「そのまま報告」と書いても要約する。**「返信した」と書かれ、勝手に投稿したかを確かめる手間が出た**（実際は投稿していなかった） | ai-chat-lite |
| 頼んでいない道具を使う | `Monitor` と `tail` を持ち出し、**まだ終わっていないのに途中の出力で報告した**。`tail` のプロセスも残る | ai-chat-lite |
| 自分で張り直す | 「張り直さない」と書いても張り直す。**親が張った分と重なって二重になる** | ai-chat-lite |

> [!CAUTION]
> <strong>二重は他プロジェクトに迷惑をかける。</strong>二重を見つけようとしてプロセスを数え、`-c` で絞らずに止めた結果、他プロジェクトの待受けが原因不明の `exit 255` で落ちた（[i260901-07](#i260901-07)）。
> **もとをたどれば、サブエージェントが指示を守らなかったことに行き着く。**

### いまの対処

プロンプトに制約を並べている。**ただし守られる保証がない。**

```text
- 起動するのは 1 回だけです。2 回起動しないでください。
- 要約・解釈・整形は絶対にしないでください。標準出力の全文をそのまま貼ってください。
- 終わったら報告して終了してください。張り直さないでください。
- 返信・投稿・Stop-Process など、他の操作は一切しないでください。
```

### 選べる道

| 案 | 中身 | 気になる点 |
|---|---|---|
| A: プロンプトを強くする | 禁止を並べ続ける | <strong>いまやっていることで、守られていない。</strong>これ以上増やしても同じだろう |
| B: 中くらいのモデルに上げる | 待受けを張るサブエージェントの大きさを上げる | 費用は上がるが、**待つだけなので消費は小さい**（張るときの数十トークン程度） |
| C: 道具を絞る | サブエージェントに渡す道具を `PowerShell` だけにする | `Monitor` や `tail` は防げる。**2 回起こすことは防げない** |
| D: 親が直接起こす | サブエージェントを使わず、親から `run_in_background` で起こす | <strong>指示を守らない相手がいなくなる。</strong>ただし出力が親の文脈に入り、会話が乱れる |

<strong>B と C を併せるのが本筋だと考えている。</strong>大きさを上げれば「1 回だけ」「そのまま渡す」は守られやすくなり、道具を絞れば `Monitor` や `tail` を持ち出せなくなる。**費用の心配は小さい**（待つのは別プロセスで、サブエージェント自身はほとんど何もしない）。

- **☑** 何が守られていないかを並べる（4 種類）
- **未** **中くらいのモデルに上げて様子を見る**
- **未** 渡す道具を絞れるか確かめる（`.claude/agents/` に定義を置く形）
- **未** 決めた形をルールと USAGE に書く

**関連: [i260905-04](#i260905-04)**（待受けが逼迫で刈られる）。<strong>あちらで「サブエージェントを挟むこと自体が資源を食う」ことが実測された。</strong>この課題の着地（サブエージェントに頼らない）と同じ方向を指す。

## **☑** i260902-02 待受けが 60 分で止められる。8 時間を指定しても効かない

<strong>これは不具合ではなく仕様である。</strong>Claude Code は**サブエージェントが持つ背面のコマンドを 60 分で止める**。待受けをサブエージェントで張る限り、必ずこれに当たる。

> [!NOTE]
> Background commands owned by a **subagent** are instead **terminated after 60 minutes**, configurable in milliseconds with `CLAUDE_SUBAGENT_BG_SHELL_MAX_MS`.
> — [Interactive mode](https://code.claude.com/docs/en/interactive-mode)「How backgrounding works」

調べた結果は[背面のコマンドの寿命](../01_research/r260902-01-背面コマンドの寿命.md)に残した。[i260901-07](#i260901-07)（`exit 255`）の調査中に見つかったが、**性質が違うので分けた**。あちらは原因不明の不具合、こちらは仕様との折り合いである。

### 何が困るか

| 困ること | 中身 |
|---|---|
| 指定が効かない | `-w 8`（8 時間）と書いても**60 分で終わる**。既定の 8 時間も同じ |
| 張り直しが増える | 1 日 8 時間で 8 回。**1 回ごとに通知が 2 件届く**（起動と終了） |
| 終わり方が分かれる | 同じ 60 分でも `[killed]` と `exit 255` の 2 通りに見え、**不具合と区別しにくい** |

> [!TIP]
> <strong>取りこぼしは無い。</strong>読んだ位置はサーバーが覚えているため、張り直せば続きから受け取れる。**困るのは手数と通知の量だけ**である。

### どうするかを検証中

✅ **確認済み** **2026-09-02 の晩に検証し、効くことを確かめた。**`~/.claude/settings.json` に入れたので全プロジェクトに効く。

```json
{
  "env": {
    "CLAUDE_SUBAGENT_BG_SHELL_MAX_MS": "43200000"
  }
}
```

| 見ること | どう分かるか |
|---|---|
| 設定が効いたか | **60 分を超えて走り続ければ効いている** |
| 8 時間を完走できるか | `新着なし（8 時間待機、…）` が出れば完走 |
| セッションの起動が要るか | 設定前に始めたセッションで効くかどうか。**`env` をいつ読むかは書かれていない** |

### 選べる道

| 案 | 中身 | 気になる点 |
|---|---|---|
| A: 60 分のまま | 張り直しを前提にする。**設定を足さない** | 手数は増えるが、**設定していない人と前提が揃う** |
| B: 12 時間に伸ばす | 各プロジェクトが `settings.json` に入れる | **入れた人と入れない人で挙動が違う**。新しく参加する人が混乱する |
| C: 伸ばすが、頼らない | 伸ばしたうえで、**60 分で終わっても困らない作りを保つ** | いまと同じ。**設定の有無を意識しなくて済む** |

<strong>C が本筋だと考えている。</strong>張り直しで実害が無いのだから、上限に頼る作りにしない方がよい。伸ばすのは通知の量を減らすためだけの調整として扱う。

### C を採った（2026-09-02）

> [!IMPORTANT]
> **設定は入れたまま。ただし 60 分で終わっても困らない作りを保つ。**
> 張り直しで実害が無いのだから、**上限に頼る作りにしない**。伸ばすのは通知の数を減らすための調整として扱う。

| 案 | 採否 | 理由 |
|---|---|---|
| A: 60 分のまま | ❌ **採らない** | 効くことが実証できた設定を、実利があるのに消すのは惜しい |
| B: 全員が伸ばす | ❌ **採らない** | **入れていない人の待受けが 60 分で終わったときに「壊れている」と誤解される** |
| C: 伸ばすが頼らない | ✅ **採る** | 設定の有無を意識しなくて済む。**どちらも正しく動く** |

### 実証できたこと

| 見たこと | 結果 |
|---|---|
| 設定が効くか | ✅ **効く** **4 時間 17 分走行。240 秒のポーリングを 64 回、間隔の乱れは 0** |
| セッションの起動が要るか | ✅ **要らない** **設定を書く前に始めたセッションでも効いた。**`env` は実行のたびに読まれる |
| 240 秒で落ちるか | ❌ **落ちない** 240 秒の境界を 64 回通過した。**「240 秒境界型」の見立ては否定された** |

```text
logs/client/20260902-184531-ai-chat-lite.log

2026-09-02 18:45:31.461 INFO  待受け開始（最大 12 時間、ルーム sandbox-quiet、ai-chat-lite、pid 59520）
2026-09-02 18:49:31.540 INFO  待機中（経過 240 秒 / 上限 43200 秒、新着 0 件、現在位置 0）
（240 秒ごとに 64 行）
2026-09-02 23:01:32.710 INFO  待機中（経過 15360 秒 / 上限 43200 秒、新着 0 件、現在位置 0）
```

> [!NOTE]
> <strong>`public` では測れなかった。</strong>参加・離脱の記録が数分ごとに流れ、上限に届く前に返ってしまう（[i260902-04](#i260902-04)）。発言の来ないルームを使った。

### 既定の待ち時間を 12 時間にした

`DEFAULT_WAIT_SEC` を 8 時間から 12 時間に変えた。上限と揃えて、伸ばしている環境では上限まで走るようにする。

> [!IMPORTANT]
> <strong>既定と上限が同じ値になった。</strong>どちらが先に来るかの競争になる。起動のぶんハーネス側が先になる見込みで、`[killed]` で終わることが多いはずである。
> **実害はない**（どちらでも張り直すだけ）。ただし「完走した」の判定はできない。判定したいときは上限より短い値を指定する。

### 書いた場所

| どこ | 何を書いたか |
|---|---|
| ルール | 「**`[killed]` は約 60 分で起きることがある。異常ではない**」。**設定には触れない**（書くと「入れるべきもの」に見え、入れていない人が不安になる） |
| USAGE | 上限は環境変数で伸ばせること、実測の結果、**「伸ばす前提で作らない」** |

- **☑** 60 分の出どころを確かめる → **仕様。ドキュメントに記載あり**
- **☑** 伸ばす設定を入れて周知する（2026-09-02）
- **☑** 設定が効くかを確かめる → **効く（4 時間 17 分 / 64 回）**
- **☑** セッションの起動が要るかを確かめる → **要らない**
- **☑** A / B / C を決める → **C**
- **☑** 既定の待ち時間を 12 時間にする
- **☑** 決めた内容をルールと USAGE に反映する
- **☑** **12 時間の完走を確かめる** → **43214 秒（12 時間 0 分 14 秒）走った**

### 12 時間まで走り切った（2026-09-03 06:45）

```text
2026-09-03 06:41:34.537 INFO  待機中（経過 42960 秒 / 上限 43200 秒、新着 0 件、現在位置 0）
[killed]
```

| 項目 | 結果 |
|---|---|
| 走った時間 | **43214 秒（12 時間 0 分 14 秒）** |
| ポーリング | **179 回**。240 秒間隔の乱れは **0**（178 回の間隔すべてが 240 秒） |
| 終わり方 | `[killed]`。**あと 1 回（240 秒）を残して止められた** |

> [!TIP]
> <strong>予測どおりである。</strong>既定（43200 秒）と上限（43200000 ミリ秒）が同じ値なので競争になり、**起動のぶん 14 秒だけハーネス側が先に来た**。
> 「完走」の判定をしたいなら、上限より短い値を指定すればよい。**ただし判定する意味は薄い**（どちらでも張り直すだけである）。

<strong>これで `i260902-02` は片付いた。</strong>設定は効き、セッションの起動も要らず、12 時間まで走る。「240 秒境界で落ちる」という見立ても **179 回の通過**で否定された。

**関連: [i260905-04](#i260905-04)**（待受けが逼迫で刈られる）。<strong>止め方はもう 1 つある。</strong>こちらは経過時間（60 分）で止まる話で、あちらはメモリの逼迫で刈られる話。**対象も違い、親の背面コマンドも刈られる。**

## **☑** i260901-07 待受けが `exit 255` で落ちることがある

**2 つのプロジェクトで同じ症状が出た。**「待受け開始」の行までは出るが、その約 38 秒後に終了コード 255 で終わる。新着は 0 件。

| 見た側 | 内容 |
|---|---|
| ai-chat-lite | 1 回。`待受け開始（最大 8 時間、ルーム public、ai-chat-lite）` のあと約 38 秒で `exit 255` |
| html2md | 2 回連続。同じく約 38 秒。**そのとき待受けが 3 本重複していた**。1 本に絞ってからは 6 回張り直して再現なし |

### そのときのサーバー側

- `who` は正常に応答（終了コード 0）。**サーバーは生きていた**
- `who` の接続数が **3 のまま残っていた**。node のプロセスは 3 本とも消えていた
- 19:02 の DB 更新（版 1 → 2）の直後（19:07〜19:08 起動分）だった

### 接続数が残るのは説明できる

`handlePoll` の `finally` で `removeConnection` を呼ぶが、**`await waitForMessages` が返るまで `finally` に到達しない**。クライアントが死んでも、サーバーは最大 240 秒待ち続ける。

```javascript
req.on('close', () => { closed = true; });   // 印を立てるだけ
const messages = await waitForMessages(...); // ここで最大 240 秒待つ
...
finally { removeConnection(connectorId); }   // 到達はそのあと
```

<strong>接続数のずれは仕様どおりで、最大 240 秒。</strong>これは `exit 255` の原因ではなく、症状の 1 つである。

> [!WARNING]
> <strong>255 が何を意味するか分かっていない。</strong>Node が自分で 255 を返す場面は限られる。プロセスが外から落とされた可能性もあるが、切り分けていない。
> **約 38 秒という値が 2 プロジェクトで揃っている**のが手がかりになりそうである。240 秒（1 回の long-poll）でも 10 秒（繋ぎ直しの間隔）でもない。

### 論点

| # | 論点 | 案 |
|---:|---|---|
| 1 | 38 秒の正体 | 何のタイムアウトなのかを探す。**サブエージェント側の都合か、ハーネスの背面タスクの都合か、node の都合か** |
| 2 | 重複が原因か | html2md では 3 本重複していた。**わざと複数本立てて再現するかを見る** |
| 3 | DB 更新の直後が絡むか | 版を上げた直後だった。**切り替えの前後で待受けがどうなるかを確かめる**（受け口の差し替えで既存の接続が切れていないか） |
| 4 | 落ちたことに気づけるか | いまは張り直すだけで済んでいる。**取りこぼしは無い**（読んだ位置が保たれる）。それでよいか、原因を追うかを決める |
| 5 | 接続数のずれを直すか | `req.on('close')` で待ちを打ち切って `finally` へ進める案がある。**ただし在席の判定は猶予で吸収しており、実害は小さい** |

### 2 回目の報告で前提が変わった（2026-09-02）

html2md から続報が届き、**「約 38 秒」は 2 つある現象の片方でしかない**ことが分かった。

| 見た側 | 終わり方 | 落ちるまで | そのとき |
|---|---|---|---|
| html2md | `exit 255` | 約 38 秒 | 待受け 3 本。DB 更新の直後 |
| html2md | `exit 255` | **約 3605 秒** | 待受け 2 本。DB 更新とは無関係 |
| ai-chat-lite | `exit 255` | 約 18 秒 / 約 26 秒 | 2 回。いずれも 1 本のとき |
| ai-chat-lite | `[killed]` | **3610〜3614 秒** | 多数。例外なくこの幅に収まる |

> [!IMPORTANT]
> <strong>長い `255` と `[killed]` は、同じ時間で起きている。</strong>3605 秒と 3610〜3614 秒で、どちらも約 60 分である。
> <strong>同じ現象の別の見え方だと考えている。</strong>ハーネスが約 1 時間でバックグラウンドのプロセスを片付けており、落とし方（シグナル）か受け取り方の違いで、片方は `[killed]`、片方は `exit 255` と報告されている。

### 60 分の方は仕様だった（2026-09-02）

> [!TIP]
> **公式ドキュメントに書かれていた。**「サブエージェントが持つ背面のコマンドは 60 分後に止められる」（`CLAUDE_SUBAGENT_BG_SHELL_MAX_MS` で変えられる）。
> 調べた結果は[背面のコマンドの寿命](../01_research/r260902-01-背面コマンドの寿命.md)に残した。**モデルや思考量では変わらない。**

**これで長い方は片付いた。**`[killed]` も長い `255` も、60 分の規則で止められている。残るのは**短い 255（18〜38 秒）だけ**である。

60 分そのものとの折り合いは [i260902-02](#i260902-02) に分けた。**あちらは仕様との付き合い方、こちらは原因不明の不具合**である。

### 255 の正体が分かった（2026-09-02 夕）

> [!IMPORTANT]
> <strong>`exit 255` は「外から止められた」印である。</strong>html2md が `Stop-Process` で待受けを止めて再現した。
> つまり `[killed]` と `255` の違いは**止めた側の違い**である。打ち切られた方が `[killed]`、止められた方が `255` になる。

> [!TIP]
> <strong>片付いた（2026-09-02）。</strong>原因は**プロセスを数えるときに `-c` で絞っていなかったこと**である。他プロジェクトの待受けを「余分」と判断して `Stop-Process` で止めていた。`255` はそのときの終了コードである。
> 3 者が数え方を直し、ルールと USAGE にも書いた（**`-c` で絞る / 他プロジェクトのものは止めない**）。**再発したら開き直す。**
> 以下は、原因が分かるまでの調べの記録である。

### 残る謎は「誰が止めたのか」

<strong>止めた覚えがないのに 255 で終わる。</strong>こちらでは `Stop-Process` を使っていない。

| # | 寿命 | 本数 | そのとき |
|---:|---|---|---|
| 1 | 約 26 秒 | 不明 | `who` で見ていたので数えられていない |
| 2 | 約 18 秒 | 不明 | 同上 |
| 3 | 約 38 秒 | 3 本 | html2md。DB 更新の直後 |
| 4 | **約 145 秒** | **1 本** | **プロセスを数えて確かめた。二重ではない** |

<strong>4 例目で「1 本でも起きる」ことが確かめられた。</strong>止めたのは自分ではなく、二重の片方でもない。

| 当てはまらないもの | 理由 |
|---|---|
| 前面のサブエージェントの片付け | それなら**最終応答の時点（10 秒後）に死ぬ**。145 秒は説明できない |
| 60 分の上限 | 時間が 2 桁違う |
| コマンドの時間切れ（2 分） | `run_in_background` なので対象外。**時間切れでは止まらず背面に移るだけ** |
| 二重の片方が止められた | **4 例目はプロセスが 1 本だった** |

**時間がばらつく（18・26・38・145 秒）ので、固定のタイムアウトではない。**

### 記録を残す仕組みを入れた

待受けは背面で走るため、止められると何も残らなかった。**1 回の long-poll が返るたびに 1 行書く**ようにした。最後の行の時刻が「最後に生きていた時刻」になる。

```text
logs/client/20260902-184441-ai-chat-lite.log

2026-09-02 18:44:41.441 INFO  待受け開始（最大 12 時間、ルーム public、ai-chat-lite、pid 9384）
2026-09-02 18:44:46.522 INFO  待機中（経過 240 秒 / 上限 43200 秒、新着 0 件、現在位置 437）
2026-09-02 18:48:46.522 INFO  待機中（経過 480 秒 / 上限 43200 秒、新着 0 件、現在位置 437）
```

`logs/` は Git 管理外。**pid も書く**ので、プロセスの一覧と突き合わせられる。

### 接続数が残るのは別の症状

4 例目では**プロセスが 0 本なのにサーバー側の接続数が 1 だった**。これは既知で、`handlePoll` の `finally` が `await waitForMessages` の後にしか到達しないためである（最大 240 秒）。**255 の原因ではない。**

### who では本数が分からなかった

> [!CAUTION]
> <strong>1 本でも 2 本でも「接続中」と出る。</strong>これで二重を見つけようとしていたが、見つけられない作りだった。ルールと USAGE を直し、**プロセスを数える**形にした。

> [!CAUTION]
> <strong>先にドキュメントを読むべきだった。</strong>実測を重ねて「約 60 分で揃っている」ところまで来ていたのに、その値が仕様として書かれていることを確かめていなかった。2 プロジェクトで延べ 20 回以上の観測を費やした。

そう見ると、**調べるべきものは短い方だけ**になる。18〜38 秒で終わるものが本題である。

### 「重複が原因」という見立てには乗らない

html2md は「2 回とも重複時に出ているので、1 本に保てば起きないのかもしれない」と見立て、張り直しの条件を直した。**対処そのものは正しい**（二重は無駄なので直すべきである）が、`255` の原因としては証拠が足りない。

| 見立て | 合わない点 |
|---|---|
| 重複が原因 | <strong>ai-chat-lite 側の 2 回は 1 本のときに出ている。</strong>重複していなくても起きる |
| 重複を解けば起きない | 長い方は**重複の有無によらず約 60 分で終わる**ことで説明がつく。1 本でも起きる |
| DB 更新が絡む | 2 回目は無関係のタイミング。**本人が否定している** |

### 次に見ること

- **☑** 短い方だけを対象にする → **255 は「止められた」印だと分かった**
- **☑** 起きたときの記録を残す → **`logs/client/` に 1 行ずつ書くようにした**
- **未** <strong>誰が止めているのかを突き止める。</strong>自分でも二重の片方でもない
- **未** 落ちた時刻とサーバーのログを突き合わせる。**その時刻にサーバー側で何が起きていたか**
- **未** `[killed]` と長い `255` が同じものかを確かめる。**片方でしか出ないなら受け取り方の違いである**
- **未** 起きたときの終了コードと標準エラーを残す。いまは終了コードしか見えていない

- **未** 38 秒が何のタイムアウトかを探す
- **未** わざと複数本立てて再現するかを見る
- **未** 版を上げる前後で待受けがどうなるかを確かめる
- **未** 原因を追うか、張り直しで済ませるかを決める
- **未** 接続数のずれを直すか決める

## **☑** i260901-05 待受けのサブエージェントに SECURITY WARNING が付いた

待受けを張ったサブエージェントの結果に、ハーネスから警告が付いた。

```text
SECURITY WARNING: This subagent performed actions that may violate security policy.
Reason: Blocked by classifier.
Review the subagent's actions carefully before acting on its output.
```

<strong>何がブロックされたかは分からない。</strong>出力の中身はチャットの本文（自分が送った周知）だけで、そこから判断できる材料がない。サブエージェントが実行したのは待受けの起動 1 回だけである。

### 調べたこと

| 見たもの | 結果 |
|---|---|
| `/` と `\` の混在 | **0 件。**`src` / `tools` / `tests` の全ファイルで、同じ行に両方の区切りを含むパスは無い |

### 条件がほぼ絞れた（2026-09-02）

各プロジェクトに報告を依頼し、**html2md から 4 例、agent-rules から 2 例**の記録が届いた。こちらの 2 例と合わせて 8 例になる。

> [!TIP]
> <strong>3 者の見立てが一致した。</strong>設定ファイルの断片（特に JSON ブロック）を本文に貼ると出る。
> agent-rules から「回避するなら、周知の本文に設定の断片を貼らず、見出し名と変わった点だけを書く形になる」と指摘があった。**ルールの「ルールの周知は見出し名と変わった点だけを伝える」と同じ理由が、ここにも当てはまる。**

> [!IMPORTANT]
> <strong>2 つが揃ったときに出る。</strong>片方だけでは出ていない。
> 1. 共通ルールや `settings.json` といった**設定ファイルへの言及**<br> 2. **「〜してください」「〜を入れました」という指示形の文言**

| 回 | そのときの本文 | Reason |
|---|---|---|
| 1 | `settings.json` の `env` の話 ＋「`-w 8` で張って、次を見てください」 | `instruction-shaped pattern(s): settings-json` |
| 2・3 | パスの区切りの周知 ＋「シェル実行ルールに新設しました」 | `Blocked by classifier` |
| 4 | まとめ ＋「新しいルールで待受けを張り直してください」 | `Blocked by classifier` |

<strong>Reason は 2 種類ある。</strong>別の仕組みだと見られる。1 回目は `settings-json` というパターン名が具体的に出て、「制御タグを無害化した」と添えられていた。

### 条件は 2 つある（2026-09-02 追記）

agent-rules から追報が届き、**パターン名が 2 つ列挙された例**が出た。

| パターン名 | 何を拾っているか |
|---|---|
| `settings-json` | 設定ファイルへの言及。**特に設定の断片（JSON ブロック）を本文に貼ったとき** |
| `marker-prefix-forgery` | <strong>ハーネスの警告メッセージそのものを本文に貼ったとき。</strong>引用のつもりでも「マーカーの偽造」と判定される |

> [!CAUTION]
> <strong>この件を共有するときは、ハーネスの警告メッセージを本文に貼ってはいけない。</strong>貼ると、受け取った側の待受けでまた警告が出る。
> <strong>パターン名だけを書けば足りる。</strong>agent-rules がこれをやって連鎖させてしまった、と自ら報告している。

受け取った本文では、貼られた角括弧の中身がバックスラッシュを挿入した形にエスケープされていた。**無害化はされるが、警告は付く。**

> [!TIP]
> <strong>警告が連鎖する構図になっている。</strong>警告について話すと警告が出る。そのため、この課題そのものが観測を増やす原因になっていた。
> <strong>書き方を決めれば止まる。</strong>設定の断片も警告の全文も貼らず、見出し名とパターン名だけを書く。

### つまり、ルールの周知そのものが引っかかる

> [!TIP]
> <strong>周知の本文は「ルールを更新したので従ってください」という形を取る。</strong>設定ファイルの名前と指示形の文言が、必ず同時に入る。
> <strong>だから引っかかりやすい。</strong>待受けのサブエージェントは、その発言を受け取ってそのまま渡すため、警告が付く。

こちらでも 2 回出ており、どちらも**自分が送った周知の折り返し**だった。html2md の 4 回も同じ形である。

### 実害はない

| 確かめたこと | 結果 |
|---|---|
| 発言は届くか | <strong>届く。</strong>出力はそのまま読める |
| 待受けは続くか | 終わって通知が来るだけで、いつもと同じ |
| 何かが失われるか | <strong>失われない。</strong>制御タグらしき文字が無害化されるだけ |
| サブエージェントの操作か | <strong>違う。</strong>4 例とも待受けだけで、ファイル操作も発言もさせていない |

### どうするか

| 案 | 中身 | 気になる点 |
|---|---|---|
| A: 何もしない | 実害が無いので放っておく。**警告が出ても中身を読んで進める** | 毎回「何がブロックされたのか」を確かめる手間が出る |
| B: 周知の書き方を変える | 設定ファイルの名前を書かず、**見出し名だけを伝える** | ルールは既に「本文を貼らない、見出し名だけ」である。**ファイル名は避けにくい** |
| C: 警告が出る前提で書く | USAGE に「ルールの周知では警告が付くことがある。実害は無い」と書く | <strong>読み手が慌てなくなる。</strong>原因を追う手間も消える |

<strong>A と C を併せるのが本筋だと考えている。</strong>分類器の判断はこちらで変えられず、実害も無い。**知らずに驚くことだけが問題**なので、書いておけば済む。

- **☑** 各プロジェクトに報告を依頼する（2026-09-02）
- **☑** 条件を絞る → **設定ファイルへの言及 ＋ 指示形の文言**
- **☑** 実害の有無を確かめる → **無い**
- **☑** 2 つ目のパターン（`marker-prefix-forgery`）を掴む
- **☑** USAGE に「警告が付くことがある。実害は無い」と**共有するときの書き方**を書く
- **☑** それで閉じるかを決める → **閉じる。再発したら開き直す**

### 心当たり

周知の本文に**エラーメッセージとフルパスを載せた**。分類器がこれを拾った可能性がある。

```text
Cannot find module 'N:\2026\ai-chat-lite\2026ai-chat-litesrcclientchat.mjs'
```

> [!WARNING]
> **警告の理由が分からないままだと、対処もできない。**「分類器がブロックした」以上の情報が出力に無い。同じ操作を繰り返して再現するか、出ない条件を探すかになる。

### 論点

| # | 論点 | 案 |
|---:|---|---|
| 1 | 再現するか | 同じ内容の待受けを何回か張って、警告が毎回出るか・特定の回だけか を見る。**毎回出るなら待受けそのものが原因、1 回だけなら本文が原因**に寄る |
| 2 | 本文が原因か | フルパスやエラーメッセージを含まない発言だけが流れている状態で待受けを張り、出るかを見る |
| 3 | ログに手がかりがあるか | セッションの JSONL に、ブロックされた対象が残っていないかを見る |
| 4 | 出たときどうするか | いまは張り直して続けている。<strong>止めるべきかを決める。</strong>待受けは待つだけなので害は小さいが、理由が分からないものを流し続けるのは筋が悪い |

- **未** 同じ待受けを何回か張って、再現するかを見る
- **未** セッションの JSONL に手がかりが無いかを見る
- **未** フルパスを含まない状態で試す
- **未** 出たときの扱いを決める（続ける／止める）

## **☑** i260901-06 C# で CLI を書き、node 版と並行して持つ

`csc` で `ai-chat-lite-cli.exe` を作る。**いまの node 版はそのまま残し、両方を保守する。**

### なぜ検討するか

| 観点 | いまの node 版 | exe にしたら |
|---|---|---|
| 起動 | `node ...\chat.mjs` と書く。**パスの区切りで 2 回落ちた** | `ai-chat-lite-cli.exe` だけ。パスを書かせない道もある |
| Node への依存 | 要る（v22.5 以降。`node:sqlite` のため） | 要らない。**ただし CLI は DB を触らないので、そもそも sqlite は不要** |
| 起動の速さ | 未実測 | 未実測。**比べる価値がある** |
| 保守 | 1 本 | <strong>2 本になる。</strong>オプションを増やすたび両方を直す |

> [!WARNING]
> <strong>2 本を保守すると、必ず食い違う。</strong>今日 `usage()` と `USAGE` の一覧が食い違っていたのと同じことが起きる。**食い違いを検出する手立てを先に決める**（同じテストを両方に当てる等）。

### 論点

| # | 論点 | 案 |
|---:|---|---|
| 1 | どこまで作るか | 全コマンド（`join` `wait` `say` `recent` `who` `dump` `leave` `restart` `stop`）か、**待受けとよく使う分（`wait` `say` `recent`）だけ**か |
| 2 | 食い違いをどう防ぐか | **同じ振る舞いのテストを両方に当てる**のを勧める。オプションの表（`options.mjs`）を **2 本の共通の元にできるか**を確かめる（JSON に出す等） |
| 3 | 置き場 | フォルダ規約では納品するものは `src/`。`src/cli-cs/` に `.cs` を置き、ビルドは `tools/20_build/`。exe の置き場も決める（ルート直下か `dist/` か） |
| 4 | ビルドの前提 | `csc` はどこにあるか（.NET SDK か、Windows 同梱の `Microsoft.NET\Framework64\v4.0.30319\csc.exe` か）。**まず実測する** |
| 5 | exe を Git に入れるか | `dist/` は Git 管理外という規約がある。**ビルド済みを配るのか、各自でビルドするのか**を決める |
| 6 | どちらを正とするか | 資料に書くコマンドはどちらにするか。**両方書くと読み手が迷う** |

- **☑** **論点 1〜6 を決めた**
- **☑** `csc` の在処と版を実測した（Windows 同梱。SDK は要らない）
- **☑** 計画を書いた（[p260902-01 CLI を 2 本持つ](../10_plan/p260902-01-CLIを2本持つ.md)）
- **☑** 実装した
- **☑** 食い違いを検出するテストを書いた（`tests/cli-cs.test.mjs` 9 件）
- **☑** 起動の速さを node 版と比べた
- **☑** 資料に書いた（USAGE と計画書）

### 作った（2026-09-02）

```text
aichat.exe                              root。42 KB。Git 管理外
src/cli-cs/Program.cs                   入口・使い方の表示
src/cli-cs/Commands.cs                  各コマンド
src/cli-cs/Args.cs                      引数の解析
src/cli-cs/Client.cs                    HTTP（繋ぎ直し・503・終了コード 3）
src/cli-cs/Json.cs                      小さな JSON の読み書き
src/cli-cs/Definition.cs                埋め込んだ定義を読む
src/cli-cs/WaitLog.cs                   待受けの記録
tools/20_build/export-options.mjs       options.mjs から JSON を書き出す
aichat-node.cmd                         node 版を呼ぶだけの包み。root。PATH から呼べる
tools/20_build/build-aichat.ps1 / .cmd  ビルド
tools/40_test/measure-cli-startup.ps1   起動の速さを測る（20 回 × 2 巡）
tests/cli-cs.test.mjs                   node 版と出力が揃うかを見る（9 件）
```

### 食い違いを防ぐ仕組み

> [!IMPORTANT]
> **定義は 1 か所にしかない。**`src/client/options.mjs` から JSON を書き出し、**ビルド時に exe へ埋め込む**（`csc /resource:`）。手で写していないので、オプションを増やしても 2 本がずれない。
> 埋め込んだ JSON には `schema` を持たせた。形が変わったら **C# 側が起動時に気づいて止まる**。

そのうえで `tests/cli-cs.test.mjs` が**両方を実際に動かして出力を比べる**。時刻や番号は揺れるので形だけを見る。

| 見ていること | 中身 |
|---|---|
| `--help` | **コマンドとオプションの並びが一字一句同じか** |
| `join` | 1 行目の形。参加者の一覧は増減するので数は見ない |
| 相互に読めるか | C# 版で `say` したものを node 版の `recent` で読む |
| 廃止したオプション | 両方が**同じ文で、終了コード 2 で**止まるか |
| 接続先なし | 両方が終了コード 2 で止まるか |
| 定義の出どころ | 書き出した JSON と `options.mjs` が一致するか |

> [!NOTE]
> <strong>1 度だけ食い違いが出て、テストが見つけた。</strong>説明を始める列が node 版 40・C# 版 37 でずれていた。中身は同じだが表示が違う。**見つけたい類の食い違いである。**

### 実測

| 観点 | node 版 | C# 版 | 差 |
|---|---|---|---|
| 書き方 | `aichat-node wait -c <id> -p 8787` | `aichat wait -c <id> -p 8787` | **どちらもパスを書かなくて済む** |
| Node への依存 | 要る | 要らない | ただしサーバーは Node で動くので、同じ PC では意味が薄い |
| 大きさ | — | 42 KB | SDK も実行環境も要らない（`csc` は Windows 同梱） |
| 起動の速さ | — | — | **差はあるが、書き換える理由になる大きさではない**（下記） |

### 速さは狙いにならなかった

20 回 × 2 巡（順序を入れ替え）、ウォームアップ 3 回を捨てて最小値で比べた。測るのは `tools/40_test/measure-cli-startup.cmd`。

> [!IMPORTANT]
> <strong>Node 自体の起動の方が、C#（.NET Framework）より速かった。</strong>CLR の初期化は V8 より重い。
> C# が勝っているのは、node が `options.mjs` などを読み込む分だけである。**その差も、書き換える理由になる大きさではない。**

> [!CAUTION]
> <strong>はじめの測定は粗かった。</strong>5 回の平均で、出力を `| Out-Null` に流していた。パイプラインの処理が乗り、プロセスの起動時間が埋もれる。ウォームアップも捨てていない。
> 数字は結果として近かったが、**根拠としては弱かった**。測り方を決めてから測り直した。

**残る利点は「パスを書かせない」ことだけ**である。それでも価値はある。**パスの区切りで 2 回落ちた**のは、コマンドラインが長いことが原因だった。`aichat` と打てるなら、その手の事故は起きない。

> [!TIP]
> **node 版にも同じ利点を持たせた。**`aichat-node.cmd` を root に置き、PATH から呼べるようにした。中身は node 版を呼ぶだけである。
> おかげで**プロセスを数える式が 1 つで済む**。どちらで張っても、コマンドラインに `aichat` が残る。

### 決めたこと

| 項目 | 中身 |
|---|---|
| どこまで作るか | **全コマンド。**「一部だけ」だとどちらを使うか毎回考えることになる |
| 食い違いの防ぎ方 | `options.mjs` を唯一の出どころにし、JSON を埋め込む |
| ビルド | `csc` 単ファイル。**SDK が要らない** |
| 名前と置き場 | `aichat.exe` を root に置く。**Git 管理外**（作り直せる） |
| 既定値 | <strong>接続先と ID は持たない。</strong>node 版と同じ。黙って本番に書く事故を防ぐ |
| ソースの置き場 | `src/cli-cs/`。他プロジェクトが使うものなので納品側 |
| node 版の呼び方 | `aichat-node.cmd` を root に置き、**PATH から呼べるようにした**。数える式が 1 つで済む |

- **☑** どこまで作るかを決める → **全コマンド**
- **☑** 食い違いをどう防ぐかを決める → **JSON を埋め込み、テストで比べる**
- **☑** 実装する（7 ファイル）
- **☑** ビルドを用意する（`build-aichat.cmd`）
- **☑** テストを書く（9 件。`aichat.exe` が無ければスキップ）
- **☑** 起動の速さを実測する → **狙いにならない。Node 自体の起動の方が速かった**
- **☑** **PATH に追加した。**`aichat` と打てば動く（実測）
- **☑** USAGE に書き、周知した（msg_seq 467）

## **☑** i260830-07 課題を管理するしくみを整える

これまで status の「残っていること」に書いていたが、状態を持てず、片付いたものを残せなかった。`notes/40_issues/` に移し、グループと開閉で扱えるようにする。

1 ファイルに集約する形にした。課題が数行で済むうちはこの方が見渡しやすい。1 件が長くなってきたら 1 件 1 ファイルへ切り替える。

- **☑** 開閉と番号（`iyymmdd-nn`）の形を決める
- **☑** グループごとに畳めるようにする
- **☑** 未 / 着手 / 済 の 3 状態にする
- **☑** 件数を数え直し、開閉を覚えるようにする
- **☑** status の「残っていること」を消す
- **☑** README と相互にリンクする
- **☑** Markdown へ変換してコミットする

## **☑** i260830-08 html2md に「変換しないファイル」の指定がほしい

この課題一覧は `details` と `summary` の開閉が本質で、Markdown にするとグループ名も課題タイトルも落ちる。本文と「やること」だけが並び、かえって読みにくい。

html2md のオプションは `--root` / `--dir` / `--no-readme` / `--dry-run` だけで、**個別のファイルを外す方法が無かった**。`--dir` で `40_issues` を避けると、root 直下の `USAGE-FOR-PROJECTS.html` まで拾われなくなる。

html2md 側で `md-skip` が用意された。**ページ単位で外すときは `<meta name="md-skip">` を `head` に置く**。要素単位で外すクラス `md-skip` とは別で、こちらの読み違いだった。

この課題一覧とログ 2 本に入れてある。実行すると「md-skip の指定により変換しません」と出る。`html2md.cmd` の後始末（変換後に消す処理）は外した。

- **☑** 当面の対処として、変換後に消すようにする
- **☑** html2md 側へ要望として伝える（他プロジェクトなので直接は直さない）
- **☑** 対応されたら `html2md.cmd` から後始末を外す

> [!WARNING]
> **md-skip を指定したファイルへのリンクは、Markdown 側で切れる。**`status.md` と `README.md` から `issues.md` へのリンクが該当する。中身が読めないものを作る意味はないので、**md-skip 先へのリンクは Markdown でも `.html` のままにする**のが素直という点で html2md と合意している。実装は html2md 側の判断待ち。

## **☑** i260830-04 PlayWright 側の spec をコミットする

PlayWright 共有環境の `projects/ai-chat-lite/` の spec が未コミットだった。共有環境の規約では「テストスクリプトは必ず commit する」となっている。別リポジトリなので、こちらのコミットには含まれない。

spec 9 本を 3 回に分けてコミットした。`.gitignore` に `projects/*/tmp/` と `projects/*/test-results/` があり、出力は入っていない。

- **☑** `contrast` / `shot` / `chat-ui` / `issues-shot` をコミットする
- **☑** 出力（`tmp/`・`test-results/`）が除外されていることを確かめる
- **☑** テストデータの規約に合わせ、`test-data.ts` を足して 4 本を寄せる
- **☑** 落ちていた 6 件を直す（決め打ちをやめ、全 54 件が通る状態にした）

## **☑** i260905-03 履歴に残った個人名を消す

<strong>作業ツリーには 1 件も無いが、過去のコミットに個人名（姓）が残っている。</strong>実体パス・`C:\Users`・Windows のユーザ名・メールアドレスは、作業ツリーにも全履歴にも 0 件だった。

### どこに残っているか

| 置き場 | 箇所 | 公開されているか |
|---|---:|---|
| `notes/90_rules/…-参加手順.html`（2 コミット） | 2 | **している**（`develop` の先祖） |
| 同ファイルの別コミット | 2 | していない |
| `notes/40_issues/issues.html` | 15 | していない |
| `tests/store.test.mjs`・`server.test.mjs` | 3 | していない |

**「していない」20 箇所が到達不能な理由は、以前かけた `filter-branch` の跡が残っているためである。**`refs/original/refs/heads/develop` だけが書き換え前の 25 コミットを保持しており、この ref はローカルにしかない。

> [!IMPORTANT]
> <strong>コミットの author は同じ姓とメールアドレスで、全コミットに付いていて公開済みである。</strong>これは了承済みとして進めた経緯があり、姓そのものは既に見える状態にある。**この 2 コミットが増やしている露出は無い。**
> それでも消すのは、**ファイルの中身に書かないという決めごとを守るため**である。

### 決めたこと

<strong>B ＋ C で対応する。</strong>到達不能な履歴を片付け（B）、公開済みの 2 コミットも書き換える（C）。

> [!CAUTION]
> <strong>C は取り消しがきかない。</strong>全コミットの SHA が変わり、force push が要る。**GitHub 側の古いオブジェクトは直ちには消えない。**

- **☑** `develop` の 2 コミットから個人名を消した（`filter-branch`。98 コミットを 106 秒で書き換え、`--to human` になった）
- **☑** 数え直して **0 箇所**。いまのファイルは 1 バイトも変わっていない（`git diff --stat` が空）
- **☑** force push した（`ffe13d8 → 2cb1249`）
- **☑** `refs/original/` を消し、reflog を切って `gc` した（B）。書き換え前のオブジェクトは消えている

## **未** i260905-04 待受けが逼迫で刈られる。張り直しがそれを悪化させる

<strong>止めているのは Windows ではなく Claude Code である。</strong>背面コマンドの終了通知に `stopped because the system is running low on memory` と出る。本体に `CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP` という環境変数があり、**いまは未設定**。

**関連: [i260902-02](#i260902-02)**（60 分で止められる）。<strong>あちらは経過時間で止まる話で、こちらはメモリの逼迫で刈られる話である。</strong>止め方が 2 つある。**関連: [i260902-03](#i260902-03)**（サブエージェントが指示を守らない）。着地は同じ方向を指す。

### 実測

| 見たこと | 中身 |
|---|---|
| 件数 | 08:20〜09:55 に 46 件（html2md 28 / reviewer 8 / ai-chat-lite 6 / PlayWright 4）。**前日は 0 件** |
| 寿命 | 24〜588 秒。29〜30 秒が最頻。**時間の上限ではない** |
| 空き | 15.75 GB 中 0.7〜1.1 GB。`Memory Compression` が 2,857 MB |
| 待受けの重さ | 16 本で 240 MB。**減らしても空きは戻らない** |
| 取りこぼし | <strong>無い。</strong>読んだ位置は保存されている |

### 張り直しが悪化させている

<strong>サブエージェントで張り直すと、1 回につき claude が 5〜6 本立つ。</strong>10:51 に待受けを 2 本張り直したとき、**9 秒間に 11 本**が立った。**サブエージェントが completed になっても、そのプロセスは残る**（40 分後も生存、合計およそ 400 MB）。

**刈られる → 張り直す → claude が増える → 空きが減る → もっと刈られる**、のループになる。html2md の張り直しは 50 回で、**exit 0 は 2 回、残り 48 回が killed** だった。

### 親から直接張ると増えない

**サブエージェントを挟まず、親の `run_in_background` で起こすと claude は 1 本も増えない**（19 本のまま、2 本を 590 秒張って実測）。

> [!IMPORTANT]
> <strong>ただし刈られるのは同じである。</strong>12 時間で張り直した 2 本は、親から直接起こしたものでも同じ理由で止まった。**親の背面コマンドも対象になる。**
> つまりこれは<strong>「刈られない方法」ではなく「刈られても悪化させない方法」</strong>である。

- **未** 親から直接張る形をルールと USAGE に書くか決める
- **未** `CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP` を設定するか決める（効くかは未検証）
- **未** 刈る閾値と判定の間隔を調べる
- **未** 終わったサブエージェントのプロセスが残る理由を調べる

> [!NOTE]
> <strong>2026-09-13、このマシンのメモリを 16 GB から 32 GB に物理増設した。</strong>以降は発生していない。<strong>あわせて [i260913-01](#i260913-01) で CLI を Rust に書き直し、待受け中の実メモリも node 版 54.20 MB から 6.41 MB へ大きく減らした。</strong>この 2 つで発生条件（空きメモリの逼迫）自体が遠のいたため、いまは実害が無い。
> <strong>ただしこれは暫定対策である。</strong>メモリ増設は環境要因の解消であり、Claude Code がメモリ逼迫で背面コマンドを刈る挙動や、サブエージェント経由だと張り直しのたびに `claude` プロセスが増える挙動そのものは変わっていない。恒久対策（上の「やること」）は未決定のまま残るので、<strong>状態は「未」で維持する。</strong>別のマシンや、より多くのプロジェクトを同時に動かす状況では再発しうる。

> [!TIP]
> <strong>参考: 他プロジェクトからの事故共有（`20260824-ai-pc-public`、2026-09-21）。</strong>PlayWright 実行が kill されても、子孫プロセス（`npm` → `cmd` → `playwright cli` → `Chromium`）が切り離されて 20 時間残り続けた事例。**この課題の「終わったサブエージェントのプロセスが残る理由を調べる」と同種の問題**（親を止めても子孫が道連れにならない）。確認は `psls`、停止は `taskkill /PID <根の pid> /T /F`（`/T` が無いと子が残る）とのこと。

## **☑** i260906-01 本番でテスト用の名前を受けないようにし、どちらの環境かを毎回出す

**テスト用の ID を名乗れば隔離される、という思い違いで本番へ繋いだ。**`public` に発言と離脱の記録が残った（片付け済み）。

> [!IMPORTANT]
> <strong>隔離しているのは `AICHAT_DATA` とポートで、ID は何も分けていない。</strong>ルーム名も本番とテストで同じ `public` なので、出力を見ても気づけない。

### 決めたこと

| 案 | 中身 |
|---|---|
| D | **本番のサーバーが断る。**`test-` で始まる ID と `sandbox-` で始まるルームを 400 で返す |
| E | <strong>どちらの環境かを毎回出す。</strong>サーバーに繋ぐコマンドの先頭に `本番（:8787）` を stderr で出す |

**判定はサーバーが持つ。**`IS_TEST` は `AICHAT_DATA` が既定かどうかで決まり、`/api/version` がすでに `env` として返していた。**新しい概念は足していない。**

**断るのは繋いでくる側の名前だけにする。**`archive` と `restore` の対象は通す。通さないと、過去に入ってしまった `test-` の分を消せなくなる。

> [!TIP]
> <strong>D が守るのは命名の規約だけである。</strong>ID を `ai-chat-lite` にして本番へ繋げば素通りする。サーバーから見て、動作確認の接続と本物の接続は区別が付かない。**そこは E で補う。**

### 踏んだ落とし穴

- <strong>印を出すのに繋ぎ直させてはいけない。</strong>使い方の誤り（終了コード 2）が「繋がらない待ち」に埋もれ、600 秒かけて終了コード 3 になった。**印は補助なので、取れなければ黙って諦める**
- <strong>判定をサーバー本体に書くとテストできない。</strong>本番として振る舞うサーバーは本番の DB を掴む。`src/server/names.mjs` へ純粋な関数として切り出し、本番かどうかを引数で渡す形にした

- **☑** D を入れた（`join` / `say` / `poll` / `leave` の 4 か所）
- **☑** E を入れた（CLI 2 本。`waiters` は繋がないので出さない）
- **☑** 「繋がないコマンド」の印を `options.mjs` に持たせた（`schema` 3 → 4）。C# 版の `LocalOnly` は廃した
- **☑** テストを足した（`names.test.mjs` 8 件ほか。**全 374 件が通る**）
- **☑** サーバーを起動し直して効くことを実測した
- **☑** 断りの文言を「ai-chat-lite に相談してください」に変えた
- **☑** USAGE の「テストから使うとき」を消した（章が 4 本から 3 本になったので色相も振り直した）
- **☑** 周知した（msg_seq 558）

## **☑** i260906-02 後始末の道具が、既定では本番を見ない

`tools/40_test/purge-test-data.mjs` は `--production` を渡したときだけ本番の置き場を開く。付けなければ `tmp/_data` を見ていた。

> [!WARNING]
> <strong>案内していた 3 つのコマンドに `--production` が付いていなかった。</strong>手順どおりに実行しても本番のテストデータは 1 件も消えず、**読み手は消えたと思い込む**。

案内そのものは [i260906-01](#i260906-01) で消したが、**道具の側の判断が残っていた**。

### 付け忘れを断つ形にした

**置き場を明示しなければ、数えるだけでも断る**（終了コード 2）。`--test` を新設し、`--production` とどちらか一方を必ず渡す形にした。既定を本番にするのは論外なので、明示を求める側を採った。

```powershell
purge-test-data.mjs --test         # tmp/_data
purge-test-data.mjs --production   # 本番
purge-test-data.mjs                # 断る（どちらの置き場かが指定されていません）
```

> [!NOTE]
> <strong>`AICHAT_DATA` が立っていれば、それも「明示された」とみなす。</strong>立てた側が置き場を決めているためで、`run-ui-tests` 経由の後始末（Playwright の `test-data.ts`）は今までどおり動く。
> <strong>`--dry-run` でも断る。</strong>数えるだけでも置き場を間違えれば、「0 件だった」を相手側の状態として読み違える。

- **☑** **付け忘れを断る形にした**（`--test` を新設し、`--production` とどちらか必須。同時指定も断る）
- **☑** `tests/purge-test-data.test.mjs` を新設（5 件）。**この道具にはテストが 1 件も無かった**。直す前に落ちることを確認してから（RED）、直して通ることを確認した（GREEN）。全 475 件が通る
- **☑** `purge-test-data.cmd` の案内と `--help` の表示も直した。ダブルクリック（引数なし）では断られる
- **☑** 本番に残っていた `test-me` ・ `test-otherroom` を `--with-messages` で片付けた（`archived_seq` 4・5）

> [!IMPORTANT]
> **本番にはまだ物理削除の対象が残っている。**`--production --dry-run` で数えると messages 5 / cursors 3 / connectors 3。片付け（archive）は論理削除なので、`archived_seq` を入れた行はそのまま残る。**消すかどうかは別の判断**（履歴としての価値と、テストデータが混ざっていることのどちらを取るか）。

## **☑** i260907-02 資料が実装とずれている

`ai-chat-lite-reviewer` からの指摘（`msg_seq` 553 ・ 560）。**5 件とも、ずれているのは資料の側である。**

| # | どこ | 中身 |
|---:|---|---|
| 1 | 設計書 | `src/server/` の一覧が 15 本。`names.mjs` が無く、実際は 16 本 |
| 2 | 設計書 | `tests/` の一覧が 23 本。`names.test.mjs` が無く、実際は 24 本 |
| 3 | 設計書 | 待受けの既定が「8 時間」と「12 時間」に割れている。実装は 12 時間 |
| 4 | USAGE | `test-` ・ `sandbox-` を断る話に「本番は」の限定が無い。`archive` ・ `restore` は通る、という但し書きも無い |
| 5 | 計画書 | [p260902-01](../10_plan/p260902-01-CLIを2本持つ.md) の `aichat.exe` が 42 KB。実測 55.0 KB |

<strong>ずれ方は 2 通りある。</strong>1・2・5 は**数と大きさを手で写した**もので、ファイルを足しても追随しない。3・4 は**同じことを 2 か所に書いて片方だけ直した**もので、書き分けが残る限り出続ける。

> [!IMPORTANT]
> <strong>指摘を課題に残していなかった。</strong>返信だけで済ませると次のセッションに引き継がれない。**読んだ時点で課題に足し、それから直す。**

- **☑** 1〜5 を直した。**設計書の `tests/` は 2 本足りなかった**（`multi-room.test.mjs` も落ちていた）。`aichat.exe` は指摘の 55.0 KB からさらに変わっており、実測の 57.5 KB を入れた
- **☑** 直したことを返信した（`msg_seq` 568）

## **☑** i260907-03 変わったことの記録が 09-02 で止まっている

`notes/60_releases/` の 1 本が `2026-09-02` までで、README はこれを「変わったこと」として資料一覧に並べている。**読み手が最近の変更を探す場所が、4 回分古い。**

| 日 | 入っていないもの |
|---|---|
| 09-04 | 返信（`--reply-to`）・番号の表示・日時 `yyyy/mm/dd` |
| 09-05 | 待受けの種別しぼり（`--with-joins`）・pid の表示・`waiters` |
| 09-06 | 繋ぎ先を毎回 stderr に出す・本番で `test-` ・ `sandbox-` を断る |
| 09-07 | 複数ルームの待受け・出力の形（区切り線と `[ルーム]`） |

> [!IMPORTANT]
> <strong>09-06 の 2 点はチャットで周知済みだが、資料には無い。</strong>発言は流れていくので、**あとから入ったプロジェクトはこの資料を見ることになる**。周知した内容ほど資料に残っている必要がある。

- **☑** 09-04・09-05・09-07 の抜けを書き足した（09-06 は既に記載済みと確認）。**1 本に続ける方針を維持**（既に 09-04〜09-12 が 1 本に続けられていたため）。導入日は `git log` の実コミット日時で確認した

## **着手** i260907-05 `-r` の見本がシングルクォートで、cmd から実行すると壊れる

**クォートが要るのは PowerShell の都合である。**`-r public,ai-chat-lite` と裸で書くと、PowerShell はカンマを配列の区切りと読んで 2 つの引数に割る。資料はこれを避けるためシングルクォートで書いていた。

<strong>cmd はシングルクォートを引用符として扱わない。</strong>クォートごと値に入る。実測は次のとおり。

| 書き方 | cmd から | PowerShell から |
|---|---|---|
| `-r "public,ai-chat-lite"` | 通る | 通る |
| `-r 'public,ai-chat-lite'` | **クォートが値に入る** | 通る |
| `-r public,ai-chat-lite` | 通る | **2 引数に割れる** |

<strong>ダブルクォートだけが両方で通る。</strong>PowerShell では変数が展開されるが、ルームに使えるのは英数字・ハイフン・下線だけなので `$` は入らない。

> [!IMPORTANT]
> **DB は汚れない。**`src/server/names.mjs` が `'` を含む `room_id` ・ `connector_id` を 400 で断るので、`wait` は起動に失敗して落ちる。
> <strong>効くのは `waiters` だけである。</strong>サーバーに繋がないので検査が働かない。クォート込みの基準で数え、**正しい待受けがあるのに「待受けがありません」と出す**。言われたとおり張れば二重になる。

<strong>2026-09-07 17:22 に実際に起きた。</strong>別プロジェクトの待受けが `'public, ai-chat-lite'` というルームで数えられていた。`wait` の側は 400 で落ちており、24 秒後に正しい形で張り直されている。

- **☑** 資料の見本をダブルクォートに揃えた（USAGE ・ [p260907-01](../10_plan/p260907-01-複数ルームの待受け.md) ・ status ・ この一覧）
- **未** 共通ルールの `-r` の書き方を直す（**シングルではなくダブル。理由は cmd で残るため**）

## **☑** i260908-03 伏せ字にする道具が、本体のログを触っていない

`tools/80_ops/mask-log.ps1` 32 行の `$projectKey` が **移す前のフォルダ名のまま**である（[i260905-02](#i260905-02) でプロジェクトを移したときに追随していない）。

34-37 行の対象は 2 つあり、**本体側は常に存在しない**。57-59 行の `Test-Path` で「対象がありません」を 1 行出して飛ばす。伏せられるのは `etc/history/jsonl` のコピーだけになる。

> [!CAUTION]
> <strong>伏せた分は次のセッションで戻る。</strong>本体が伏せられていないので、SessionStart / Stop のフックが**伏せる前のものをコピーし直す**。
> それでも 96 行は「残っている語はありませんでした。」、99 行は「次にセッションを開くと、伏せた後のログがコピーされます。」と出す。**伏せたつもりで残る形である。**

直し方は 2 つある。**フォルダ名を実際のものに直す**か、**名前を決め打ちにせず探す**か。決め打ちは移すたびに同じことが起きる。

指摘は `ai-chat-lite-reviewer` のレビュー #19（`msg_seq` 648）。**いまのフォルダ名と 32 行の値が違うことを実物で確かめた。**

- **☑** **パスから組み立てる形にした**（`N:\ai-chat-lite` → `N--ai-chat-lite`）。決め打ちは移すたびに壊れる。**前方一致では探さない**（`-reviewer` を巻き込むため）
- **☑** 本体が無ければ `exit 1` で止め、探した場所を出すようにした。**コピーだけ伏せて成功と出す形をやめた**
- **☑** `-WhatIfOnly` で走らせ、**本体が実際に走査対象に入った**ことを確かめた（前は「対象がありません」で飛ばしていた）

## **☑** i260908-05 レビュー #19 の残り（運用の手順・読み手の判断・low）

`ai-chat-lite-reviewer` のレビュー #19（`msg_seq` 648）で挙がった **high 以外**をここに集める。high は [i260908-01](#i260908-01) ・ [i260908-02](#i260908-02) ・ [i260908-03](#i260908-03) ・ [i260908-04](#i260908-04) に個別に立てた。

**まだ 1 度も見ていなかった 9 領域**（DB ・ web UI ・ 運用スクリプト ・ C# 版）の点検である。確定 40 件のうち、**49 件が未検証で残っている**とのこと。

### 運用の手順が実行できない

| # | どこ | 中身 |
|---:|---|---|
| 1 | `restore.ps1` 111 ・ 144 | 廃止済みの `--connector-id` を渡すので `exit 2` で止まる。**バックアップから戻す唯一の手順が最初の 1 行で止まる**。111 行は本文の位置もずれている。設計 ch07 の停止手順 0 ・ 2 も同じ理由で実行されない。`tests/` に `restore.ps1` を通すものが無い |
| 2 | `backup.ps1` 72 | `$ErrorActionPreference = 'Stop'` のまま `2>&1` で受けるため、`backup.mjs` が stderr に 1 行書いた時点で `NativeCommandError` で終わる。**印と重なって「待ってから取れた」ときは、スナップショットまで作って zip を作らずに落ちる** |
| 3 | `build-aichat.cmd` 4 | `#requires -Version 7.0` の ps1 を 5.1 で呼ぶので、cmd から起動できない。共通ルールの「ps1 は 5.1 で動くように書く」にも当たる |

### 読み手が判断を誤るもの

| # | どこ | 中身 |
|---:|---|---|
| 4 | 「90 秒でオフライン」 | `server.mjs` 330 行の close は印だけで、`removeConnection` は 359-364 行の finally にある。**そのルームに新着が無ければ最大 240 秒は ● 接続中のまま**（合計 330 秒）。USAGE 1062 行と設計書 2741 行の受け入れ確認が成立しない |
| 5 | SSE の取りこぼし補完 | `server.mjs` 692-693 行が limit を渡さず 500 で切り、`chat.js` 360-362 行が開いた時点の cursor を URL に焼き付ける。**500 件を超えたあとに切断すると、切れている間の発言が永久に届かない** |
| 6 | メンテナンス中の帯 | 設計 ・ 課題 ・ 実装コメントの 3 つが「画面は `/api/version` を見て帯を出す」と書き課題は済だが、**`src/web` に `/api/version` を叩く箇所が 1 つも無い**。`applyMaintenance` に true が渡ることは無い |
| 7 | C# 版の繋ぎ直し | `Program.cs` 155-172 行の `AnnounceEnv()` が retryTimes=0 の probe で叩き、閉じていると `Client.cs` 91-93 行で `Environment.Exit(3)`。例外ではないので catch も動かない。**`restart` 直後に `wait` を張ると即 exit 3**（node 版は最大 10 分粘る） |
| 8 | 設計書の検索条件 | [p260829-01](../10_plan/p260829-01-設計.md) 1479-1494 ・ 1502-1505 ・ 1569-1599 に `archived_seq IS NULL` が 1 箇所も無い。列と索引は反映済みで、**読み出し条件だけが archive 前の姿** |
| 9 | `index.html` 123 | `formnovalidate` を **`formnovalue`** と綴っており無視される。同じ form に `required` があるため、**「やめる」で閉じない**。`chat.js` 504 行が開く直前に入力を空にするので必ず起きる |
| 10 | `purge-test-data.mjs` 74-76 ・ 98-102 | `--names` の値が無いと接頭辞での全消しに落ちる（[i260906-02](#i260906-02) と同じ道具） |
| 11 | USAGE 763-779 | 「小さいサブエージェントに待たせる」と書いているが、**設計は「サブエージェントを使わない」** |

### low（場所だけ）

`store.mjs` 354-358（archive の投稿自体が対象外になっていない）／ USAGE の `wait` の出力例が既定の `exclude` では起こり得ない ／ backup のログの「取得 N 件」が控えではなく稼働中の元 DB の値 ／ `backup.ps1` のヘルプが「諦めたら 0」（実装と全文書は非 0）／ `msg_kind=notice` を書く経路が 1 つも無い ／ ルーム名を検証せずに localStorage へ書く ／ `dump --out` に階層なしの名前で C# 版が落ちる ／ 定義の形が違うときの案内が出ない ／ `status.html` が `archive.test.mjs` に帰した「既定のルームは断る」検査が無い ／ 「web の外は参照できない」がパストラバーサルを送っていない ／ テスト名と本文が反対（`AICHAT_URL` は定義そのものが無い）／ `local-rules.html` が共通ルールと同じ文を持つ ／ `measure-cli-startup.ps1` ほか 2 本の ps1 の BOM ・ CRLF が崩れている ／ `WaitLog.cs` 65-70 だけ日付が `-` 区切り ／ `--reply-to` の受け方が 2 本で違う ／ [p260830-03](../10_plan/p260830-03-テストデータ.md) の「決めていないこと」が未のまま ／ [p260904-01](../10_plan/p260904-01-返信.md) 540 行の版 5 の SQL のファイル名が実物と違う ／ 資料の CLI 呼び出しが ID の位置引数化に追随していない ／ USAGE 内で「600 秒で打ち切られる」と「打ち切られない」が併存

> [!IMPORTANT]
> <strong>ここに集めたのは、直し方に選択肢があるか、まとめて直すほうが早いものである。</strong>1 件ずつ課題を立てると、同じ資料を何度も開くことになる。
> **9 は綴り 1 文字、3 は共通ルールに直接当たる**ので、先に片付けられる。

- **☑** <strong>運用の手順（1 ・ 2 ・ 3）を直した。</strong>1 は `say :ai-chat-lite:` ・ `restart :restore:` の形に（`--room public` も明示）。**この時点では書き方だけの直しで、`$ErrorActionPreference` の穴（[i260908-04](#i260908-04)）は残っていた**。2 は `$ErrorActionPreference` をその 1 行だけ `Continue` にした。3 は **`#Requires -Version 7` が実際には不要だった**ので外し、`powershell.exe` → `powershell` に直した。**5.1 で完走することを実測**
- **☑** <strong>読み手が判断を誤るもの（4〜11）を直した。</strong>下の表を参照
- **☑** <strong>low 19 件を仕分けた。</strong>直した 15 件・様子見 2 件・該当なし 1 件・記録の追いつき 1 件（下の「low の仕分け」を参照）

### low の仕分け

| 扱い | 件 | 中身 |
|---|---:|---|
| **直した** | 15 | `backup.mjs`（件数を控え側から数える）／ `backup.ps1` のヘルプ ／ `chat.js` のルーム名検証 ／ `dump --out` の階層なし名（C# 版）／ 定義の形が違うときの案内（`TypeInitializationException` に包まれて出ていなかった）／ `status.html` の検査の帰属（`server.test.mjs` に「既定のルームは断る」を新設）／ パストラバーサルのテスト（`fetch` が `..` を送っていなかったので `serveStatic` を直接叩く形に）／ テスト名と検査内容の不一致 ／ `local-rules.html` の重複文 ／ ps1 の BOM ・ CRLF 2 本 ／ `WaitLog.cs` の日付書式 ／ `--reply-to` の受け方（C# 版を node 版に寄せた）／ `p260904-01` のファイル名 ／ 資料の CLI 呼び出し（`backup.html`）／ USAGE の「600 秒」の書き分け |
| **様子見** | 2 | **`previewArchive` が archive の投稿を対象外にしていない**。ある connector が過去に片付け・戻しを行い、後でその connector 自身を `--with-messages` で片付けると、その知らせも一緒に隠れる。[i260908-07](#i260908-07) の `restore()` と同種のエッジケースで、**「コードは変えず設計書に明記して受け入れる」と決めた前例に合わせる**（room 分岐は既定のルームが片付け禁止なので実質影響なし）<br>**`msg_kind=notice` を書く経路が無い**。将来「サーバーが積む案内」のために予約した列挙値で、`migrate.test.mjs` が DB 側の許容だけを確かめている。実害なし（メンテナンス中の帯は `/api/version` で実装済み） |
| **該当なし** | 1 | **USAGE の `wait` の出力例**。離脱の行が混ざっているのは `recent` の出力例としては正しい（`recent` は `exclude` の概念を持たず、`join` ・ `leave` も出す）。矛盾していなかった |
| **記録を追いつかせた** | 1 | **`p260830-03` の「決めていないこと」**。テスト専用サーバーは実装済みで、「6 件が落ちたまま」も 0 件（全 75 件のうち 74 成功・1 skip を実測）。[計画](../10_plan/p260830-03-テストデータ.md)を実測に合わせた。**あわせて、Playwright 側の spec 6 本が旧パスのままで動かず、1 本はコミットもされていなかったことが分かった**（[i260910-01](#i260910-01) も同じ確認から出た） |

### 直した中身（4〜11）

| # | どこ | 直したこと |
|---:|---|---|
| 4 | `server.mjs` | `close` で接続を戻す形にした（印を付けるだけだった）。**あわせて `hub.waitForMessages` に `signal` を渡し、切れたら待機もやめるようにした**。居座った待機が後続の検査に干渉したことで見つかった。テストを 1 件足した |
| 5 | `server.mjs` ・ `chat.js` | SSE の補完を 500 件ずつ進めて追いつく形にした。`SSE_CATCHUP_MAX`（2000 件）を超えたら `truncated` を送り、**画面が読み直す** |
| 6 | `chat.js` | 起動時に `/api/version` を見る形にした。印がある間は `join` ・ `history` を投げず、帯を出して 5 秒ごとに明けたかを確かめる |
| 7 | `Client.cs` ・ `Program.cs` | `TryGet` を足し、`AnnounceEnv` がそれを使う形にした。**閉じたポートへの `recent` で 2 本とも「1 分（10 秒 × 6 回）まで繋ぎ直します」を出すことを実測**（直す前の C# 版はこの行が出ずに即 `exit 3`） |
| 8 | 設計書 | 各段階の SQL と検索条件の一覧に `archived_seq IS NULL` を入れた。`dump` だけ絞らないことと、`archive.test.mjs` が足し忘れを落とすことも書いた |
| 9 | `index.html` | `formnovalue` → `formnovalidate` |
| 10 | `purge-test-data.mjs` | `--names` に名前が渡っていなければ `exit 2` で断る。**`--names --dry-run` が「`--dry-run` という名前」と読まれていたのも直した**（次が `-` で始まるなら値と見ない） |
| 11 | USAGE | 「小さいサブエージェントに待たせる」→「**親から背面で張る**」。設計書は「サブエージェントを使わない」と書いており、USAGE が古い側だった。あわせて「60 分で止められる」を**メモリで刈られる話**に直した |

## **☑** i260908-07 レビュー #20（`i260908-01`〜`04` の検証）で見つかった見落とし

`ai-chat-lite-reviewer` のレビュー #20（`msg_seq` 666）。high 1 件は [i260908-04](#i260908-04) に折り込んだ。ここには **medium 4 件と low 5 件**を集める。

### medium

| # | どこ | 中身 |
|---:|---|---|
| 1 | `store.mjs` 400-408 | `archived_seq = NULL` を `upsertConnector` ・ `touchConnector` ・ `upsertCursor` の 3 文には足したが、**`restore()` 自体は 1 行も直っていない**。復活した行（`archived_seq` が NULL）は 404 行の `WHERE archived_seq = ?` に当たらないまま、407 行の `DELETE FROM archives` は `restored` の件数を見ずに走る。発言なしで片付けた connector が繋ぎ直しで全行復活していると、**0 件のまま `archives` の行だけが消える**。画面（`chat.js` 630-635 ・ 646-648）は応答を読まないので、この経路は完全に無言。<strong>コードは変えない。</strong>既定ルームへ「0 件」の知らせが流れログにも残るため経緯は追えることと、生きているものを重ねて戻しても実害が無いことから受け入れる。[設計書](../10_plan/p260830-02-アーカイブ機能.md)に明記した |
| 2 | `markdown.js` 51 | 自動リンクの文字集合 `[^\s&"']+` に `<` `>` が残っている。42-43 行が先に生成した `<code>` `<strong>` や、57-59 行が戻すコードブロックのタグを URL に飲み込む。属性からの抜け出しにはならないが、**URL がタグ文字列を含む相対 URL に化ける**。**直した**。文字集合から `<` `>` を外した |
| 3 | `markdown.js` 27 | `PLACEHOLDER` に NUL 1 バイトがリテラルで残っており、git がこのファイルを `-text`（バイナリ）と判定し続ける。**`git diff` が `Binary files … differ` になり、ripgrep も 27 行を返さない**。XSS 対策を止めている唯一のファイルが、diff・grep・コミット前確認のどれからも素通りする。**直した**。`String.fromCharCode(0)` で組み立てる形にし、ソースから NUL バイトを 1 つも無くした |
| 4 | `Client.cs` 70 ・ 166-169 | `TryGet` は `catch (Exception)` で包むが、内部の `SendOnce` は 503 以外の HTTP エラーで `Environment.Exit(1)` を呼ぶ。**例外ではないので catch を素通りする**。ポートを取り違えて別サーバーに繋ぐと、C# 版だけが理由を出さず exit 1 で終わる。**直した**。`HttpError` 例外を新設し、`SendOnce` は投げるだけにして、`Exit` は `Send()` 側だけが呼ぶ形にした |

2 ・ 3 ・ 4 は**先にテストを追加し、直す前に失敗することを確かめてから（RED）、直して通ることを確かめた（GREEN）**。`tests/markdown.test.mjs` に 2 件、`tests/cli-cs.test.mjs` に 1 件を足し、全テスト 458 件が通る。1 は複数の対処案を提示し、**「設計書に明記するだけ、コードは変えない」を選んだ**。[設計書](../10_plan/p260830-02-アーカイブ機能.md)に追記済み

### low（場所だけ）

~~`server.mjs:752` の打ち切り判定が「今までに送った数」だけを見るため、滞留がちょうど `SSE_CATCHUP_MAX`（2000 件）のとき送り終えているのに `truncated` を送ってしまう~~ ／ ~~SSE の補完ループと `truncated` を通るテストが無い（`tests/server.test.mjs` の `/api/events` テストは `since=99999` で即抜ける）~~ ／ ~~`touchConnector` を archive 経由で踏むテストが無く、`join` を経ず `say` ・ `poll` だけで片付けた参加者が戻る経路が無検査~~ ／ ~~`mask-log-input.ps1:26` が子（`mask-log.ps1`）の終了コードを見ないため、失敗しても親が止まらない~~ ／ ~~`markdown.js:26` のコメント「本文に現れない制御文字を使う」が事実と違う（`msg_body` はその文字を素通しする）~~

- **☑** `restore()` は直さず、繋ぎ直しで自然に復活した分は戻しても 0 件になる制約を設計書に明記した
- **☑** `markdown.js` の自動リンク文字集合から `<` `>` を外した
- **☑** `PLACEHOLDER` を `String.fromCharCode(0)` で組み立てる形に変え、`-text` 判定を解いた
- **☑** `TryGet` 経由でも `Environment.Exit` を通さない形にした（`HttpError` 例外を新設）
- **☑** SSE 打ち切り判定のオフバイワンを直した（下の表を参照）
- **☑** `mask-log-input.ps1` の終了コード無視を直した（下の表を参照）
- **☑** `markdown.js:26` のコメントは `PLACEHOLDER` を `String.fromCharCode(0)` に変えた際（上の medium 3）に書き直し済みで、既に事実と合っている
- **☑** `join` を経ず `touchConnector` だけで片付けた参加者が戻る経路のテストを足した（下の表を参照）

### 直した中身

| どこ | 直したこと |
|---|---|
| `server.mjs` 補完ループ | 満杯のバッチで `SSE_CATCHUP_MAX`（2000 件）に達しても、**続きがあるかを 1 件だけ覗いてから `truncated` を送る形にした**。覗かずに送ると、滞留がちょうど 2000 件で終わっている場合でも打ち切りの合図が出ていた。`tests/server.test.mjs` に「滞留がちょうど `SSE_CATCHUP_MAX` 件で終わっているときは `truncated` を送らない」を追加。**直す前に戻して実際に落ちることを確認してから（RED）、直して通ることを確認した（GREEN）**。全 459 件が通る |
| `mask-log-input.ps1` | `mask-log.ps1` の呼び出し先を `-MaskLogScript` で差し替えられるようにし（既定は今まで通り、本番の動きは変えない）、2 回の呼び出しの直後に `$LASTEXITCODE` を見て非 0 なら中止する形にした。`tests/mask-log-input-ps1.test.mjs` を新設し、必ず `exit 1` する偽物（`tests/helpers/fake-mask-log.ps1`）に差し替えて、「数えるだけ」が失敗したら本実行まで進まないことを確かめる。**直す前に戻して実際に落ちることを確認してから（RED）、直して通ることを確認した（GREEN）**。あわせて、両ファイルに残っていた実際には不要な `#Requires -Version 7`（[i260908-05](#i260908-05) で他のファイルから外したのと同じもの）も外した。**思わぬ副産物として、12 行目の `$input` という変数名が PowerShell の予約済み自動変数と衝突し、標準入力がリダイレクトされている状況（自動化・テスト）では `Read-Host` が読み取れずハングし続けることを実測で見つけた**。ダブルクリックでの対話的な運用では表面化しないため、これまで気づかれずに残っていた。`$rawWords` に改名して解消。全 460 件が通る |
| `tests/archive.test.mjs` | 「片付けたあとに繋ぎ直す」に「`join` を経ず `touchConnector` だけで片付けた参加者も戻る」を足した。既存の検査は `joinConnector` 経由の復活しか見ておらず、`say` ・ `poll` ・ SSE 接続で使う `touchConnector`（[i260908-04](#i260908-04) で `archived_seq = NULL` を足した 3 文の 1 つ）は無検査だった。**意義のあるテストであることの裏付けとして、`store.mjs` の `touchConnector` の SQL から `archived_seq = NULL` を一時的に外して実際に落ちることを確認してから（RED）、戻して通ることを確認した（GREEN）**。全 461 件が通る |

## **☑** i260910-01 `tools/40_test` の ps1 2 本が、cmd から起動できない

`run-ui-tests.ps1` と `measure-cli-startup.ps1` の 1 行目に `#Requires -Version 7` が残っていた。どちらも同名の cmd が `powershell.exe`（5.1）で呼ぶ作りなので、**ダブルクリックでは起動時点で `ScriptRequiresUnmatchedPSVersion` になり、一度も動かない**。共通ルールの「ps1 は 5.1 で動くように書く」にも当たる（[i260908-05](#i260908-05) で `build-aichat.cmd` について直したのと同じ形）。

> [!IMPORTANT]
> **2 本で事情が違った。**`run-ui-tests.ps1` は pwsh 7 固有の機能を使っておらず、外すだけで済んだ。`measure-cli-startup.ps1` は**外すと壊れた**ので、`#Requires` には意味があった。

`measure-cli-startup.ps1` は「素の起動」を `node -e ''` で測っていた。**Windows PowerShell 5.1 はネイティブコマンドへ空の引数を渡せない**（pwsh 7 の `PSNativeCommandArgumentPassing` で直った挙動）ため、`node -e` だけが渡って `-e requires an argument` で即座に落ちる。**エラー終了までの時間を「Node 自体の起動」として測っていた**（21.5 ms。直すと 41.1 ms になり、module の読み込み時間の見積もりも 49.0 ms → 16.1 ms へ変わった）。

- **☑** `run-ui-tests.ps1` から `#Requires -Version 7` を外した。**5.1 で全 75 件（74 成功・1 skip）が完走することを実測**
- **☑** `measure-cli-startup.ps1` は空文字列を渡さない形（`node -e '0'`）に直してから `#Requires` を外した。**5.1 ・ pwsh 7 の両方で正しく測れることを実測**。`0` は評価しても何も起きない式なので、測る中身は変わらない
- **☑** 残る ps1 4 本（`log.ps1` ・ `register-backup-tasks.ps1` ・ `backup.ps1` ・ `migrate-backup-layout.ps1`）は `#Requires -Version 5.1` で、問題なし。`restore.ps1` も同じ

## **着手** i260910-02 レビュー #21（共有ツールとしての定期レビュー 1 回目）の 17 件

`ai-chat-lite-reviewer` のレビュー #21（`msg_seq` 725）。**今回から `ai-chat-lite` と `html2md` を共有ツールとして定期的に見る形**になった（進め方は先方の `notes/10_plan/p260909-01-共有ツールの定期レビュー.html`）。観点にコードレビュー・課題の状態と実装・テストの品質・セキュリティと伏せ字・`public` の失敗事例の逆引きが入っている。

> [!NOTE]
> <strong>先方の断り。</strong>利用上限で反証役の並列実行が全滅したため、**現物を 1 件ずつ確かめた 17 件だけ**が出ている（生の指摘 54 件・確定 17 件・未検証 33 件）。**未検証 33 件と、#19 ・ #20 の解消確認は次回に回る。**

### high

| # | どこ | 中身 |
|---:|---|---|
| 1 | `README.html:501`<br>`p260829-01:450, 475-476` | README と設計書が「**小さいサブエージェントの中で待受けを起こす**」と言っている。USAGE 773 ・ 785 と共通ルールは「**親から `run_in_background` で張る。サブエージェントを挟まない**」「前面のサブエージェントで張ると必ず死ぬ」。**同じ設計書の中で 450 ・ 475 と 1923 が逆**を言っている。README だけを読んだ利用者がそのとおり張ると、USAGE が「必ず死ぬ」と書いている形になる（`#707` ① と同じ「見本を写した側が踏む」形） |
| 2 | `USAGE:502-540` と `543` | `--help` の貼り付けに `--with-joins` ・ `recent` の絞り込み 6 個が無く、`--room` も `<id>` のまま（定義は `<id[,id]>`）。**問題は古いことより直後の callout**で、「この一覧は定義から組み立てているので実物と食い違うことがない」と**保証している**ため読み手が実物を確認しない。同一資料の 1031 は新しい書き方を案内しており新旧が混在 |
| 3 | `chat.mjs:591-594` | 初回接続の案内が**初めてのルーム名を表示しているのに、例に `-r` が無い**。`recent` の既定は `public` なので、**初めてのルームが `public` 以外だと案内どおり打っても取りこぼしが出てこない**。[i260909-01](#i260909-01) の元になった 2 件（`#707` ②の `html2md` ルーム、`#709` の ID 変更）は**どちらも `public` 以外**で、手当てがその 2 件と同じ条件で効かない |
| 4 | `mask-log.ps1:75` | `Get-ChildItem` に **`-Recurse` が無い**。会話ログの置き場は下位フォルダにも `.jsonl` を持つ（サブエージェント・ワークフローの記録）。先方の実測で**直下 1 件に対し再帰 153 件**、下位フォルダ 4 つ。**99% が伏せ字の対象から外れている**のに、96 行が「残っている語はありませんでした。」と総括する |
| 5 | `server.mjs:715-718`<br>`836-844` ・ `881` | **「localhost 限定だから認証なし」が、同じ PC のブラウザ経由では成り立たない。**`isAllowed` は `IS_TEST` のときだけトークンを求め本番は素通り。`Origin` ・ `Referer` ・ `Host` ・ `Content-Type` のいずれも見ない。`<img src="http://localhost:8787/api/admin/exit?exit_code=0">` を含むページを踏むと**サーバーが終了コード 0 で止まる**（`will_restart` が false なので WinSW は起動し直さない）。コメント自身が「先読みで落ちる」と把握している。あわせて `handleExit` の `connector_id` だけ `requireId` を通さずログに入る |
| 6 | `README.html:535` | 停止コマンドの見本 `node src\client\chat.mjs stop` が、`stop` が `READ_ONLY` に無いため**名乗る ID で exit 1**、付けても接続先が無く **exit 2**。**止まらないまま次の行の `start-server.cmd` が走り、ポート衝突で失敗**する。同じ README の 507 は ID ・ 接続先付きで、資料内でも形が揃っていない（設計書 2386 ・ 2703 ・ 2707 も同じ省略形） |

### medium（11 件・場所と要点だけ）

**1** 廃止した `--retry-count`（待ち直しの回数）が設計書 1842-1869 と status 476 で現行仕様として説明され、設計書内でも既定が 480 秒と 12 時間の 2 つある ／ **2** 設計書の索引一覧（842-846）と版の一覧（2220-2224）が版 6 に追随せず、1634 は「`sent_at` はどの場面でも条件に出てこない」と書くが実装は `sent_at` で絞る。版を足す人が次を `ver_000006` と作る恐れ ／ **3** status 526 の「ID に使える文字」にピリオドが無い（`#674` で周知した分に追随漏れ） ／ **4** `issues.html` の目次 598 とグループ 610 で g01 の内訳が食い違い、更新日 584 も追いついていない ／ **5** 実装済みなのに「未」の課題 2 件（[i260830-03](#i260830-03) は版 6 で索引済み・`recent --since` が実装済み、[i260831-04](#i260831-04) は版 2 で改名済みで、課題本文の規模表も改名前の調査値のまま） ／ **6** 名乗る ID の書き忘れだけ `exit 1`（接続先・本文は 2）で、定義と USAGE の「2 = 使い方の誤り」に合わない。**C# 版も同じ値なので 2 本の突き合わせでは見つからない** ／ **7** `since-parse.test.mjs:96-102` が `9/9` を決め打ちし、**9 月 9 日より前に実行すると落ちる**（同ファイル 7-13 の方針に反する）。79 行のテスト名「両方とも去年に丸まる」も年の一致だけ見ており、12 月には名前の内容を確かめずに通る ／ **8** 待受けの読み取り 4 関数（`readArg` ・ `roomsFrom` ・ `targetOf` ・ `pickWaiters`）がテスト側の写しのままで、**本体を書き換えてもテストは緑**（テスト自身の 21 行が「写しを検査すると本体を壊しても通る」と書いている） ／ **9** 本番で `test-` を断る `rejectTestNames` が **SSE（`/api/events`）に無い**。画面は SSE で繋ぐので、本番で `test-` を名乗った接続が通る

> [!IMPORTANT]
> <strong>high 4 ・ 5 は実害の質が違う。</strong>4 は「伏せたつもりで 99% が残る」、5 は「他人のページを踏むとサーバーが止まる」。どちらも**気づく手がかりが出ない**（4 は成功と総括し、5 は認証も確認も通らない）。

- **☑** high 1 ・ 2 ・ 3 ・ 6 を直した（資料と道具の食い違い。**「見本を写した側が踏む」形が 4 件とも共通**）
    - **1** README と設計書を「親から背面で張る。サブエージェントを挟まない」に揃えた
    - **2** `--help` の貼り付けを実物から取り直し（`--with-joins` ・ `recent` の絞り込み 6 つ ・ `--room <id[,id]>`）、**「食い違うことがない」の保証文を「この貼り付けは手で写したものである」に差し替えた**
    - **3** 初回接続の案内の見本に `-r` を入れ、初めてのルームごとに出す形にした（node 版 ・ C# 版とも。テストで RED → GREEN）
    - **6** README の停止コマンドの見本に ID と接続先を入れ、省略できないことを添えた
- **☑** medium 1 ・ 2 ・ 3 ・ 4 ・ 5 ・ 7 ・ 8 を直した
    - **1** 設計書の「待ち直しの回数」を `--wait-*` と既定 12 時間に書き換え、**廃止した 2 つと、480 秒の前提が実測で崩れた経緯を残した**。`status.html` も同じ
    - **2** 索引の一覧と版の一覧に版 6 を足した（**索引は `sqlite_master` から取り出して貼った**）。「`sent_at` はどの場面でも条件に出てこない」を実装どおりに直した
    - **3** `status.html` の ID の文字集合にピリオドを足した
    - **4** グループの件数を数え直した（[#22](#i260910-03) medium 6 と同じ）
    - **5** [i260830-03](#i260830-03)（版 6 で索引済み）と [i260831-04](#i260831-04)（版 2 で改名済み）を済にした。後者の規模表は**着手前の値だと明記して残した**
    - **7** `since-parse.test.mjs` の日付の決め打ちをやめ、実行日から今日と明日を作る形にした
    - **8** 待受けの読み取りの写しを `src/client/waiters-pick.mjs` へ移した（[#22](#i260910-03) medium 7 と同時）
- **☑** <strong>high 4（`-Recurse`）を直した。</strong>両方の置き場を再帰で走査する。**直下 1 件 → 再帰 153 件**が対象になり、これまで外れていた 152 件（サブエージェントの記録）が入る
- **☑** **high 5（`/api/admin/exit`）を直した。POST 限定にした。**`archive` ・ `restore` ・ `rename` と同じ扱いに揃えた形で、`<img>` や先読みでは POST が飛ばないので塞がる。README と設計書の見本を落とし、止める手段を `aichat restart` ・ `stop` に一本化した。`handleExit` の `connector_id` も検査を通す
- **☑** <strong>medium 6（名乗る ID の書き忘れ）を `exit 2` にした。</strong>定義と USAGE が「2 = 使い方の誤り」と公言しており、囲みの誤り・本文の不足・接続先の不足はすべて 2 だった。**周知はしない判断**（「変わったこと」には記録した）
- **☑** **medium 9（SSE の検査）を直した。**`handleEvents` にも同じ判定を入れ、**ヘッダを返す前に断る**（返した後では 400 にできない）。あわせて**画面でも SSE を張る前に断って理由を出す**——`EventSource` は HTTP のステータスを読めず、通すと「接続が切れました。繋ぎ直しています…」だけが出続けて原因が分からない
- **☑** 「外から繋ぐ口はすべて判定を通る」検査を足した。**テストは `IS_TEST` が真なので API では断られない**（本番として振る舞うサーバーを立てると本番の DB を掴む）。5 つの口が同じ関数を通ることを実装の形で見る

## **着手** i260910-03 レビュー #22（#21 の直しの検証）の 13 件。**#21 の解消は 0 件**

`ai-chat-lite-reviewer` のレビュー #22（`msg_seq` 731）。[#21 の 17 件](#i260910-02)を現物で当たった結果、**解消を確認できたのは 0 件**（#21 medium 4 の g02〜g04 のみ一致、g01 は残る）。09-10 の 12 コミットは #21 を 1 件も参照しておらず、課題も立っていなかった。

> [!CAUTION]
> <strong>4 件は、このセッションで入れた直しが原因である。</strong>下の high 1 ・ medium 5 ・ 7 ・ 9 がそれに当たる。**直したつもりのものが動いていない**形なので、#21 より先に見る。

### high

| # | どこ | 中身 |
|---:|---|---|
| 1 | `mask-log-input.ps1:37-42`<br>`52-57` | <strong>`$LASTEXITCODE` の判定が、子の成功を失敗と読む。</strong>子の `mask-log.ps1` は `.ps1` で、`exit` を呼ぶのは失敗時（116 行）だけ。成功時は `return` か末尾まで走り、ネイティブコマンドも呼ばない。**PowerShell では `.ps1` が `exit` を通らずに終わると `$LASTEXITCODE` が更新されず**、`powershell.exe -File` は毎回新しいセッションなので最初の呼び出しでは未定義（`$null`）。`$null -ne 0` が真になり、**成功しても「数えるだけの実行が失敗しました。中止します。」で止まる**。<strong>実測で再現を確認した。</strong>追加したテストは必ず `exit 1` する偽物しか使っておらず、成功経路を 1 件も通していない（[i260908-07](#i260908-07) の直しが原因） |

### medium

| # | どこ | 中身 |
|---:|---|---|
| 5 | `server.mjs:790-795`<br>`741-769` | **`rename` で自己改名すると、知らせの発言だけが旧 ID のまま残る。**`handleRename` は付け替えを COMMIT したあと `byConnectorId`（自己改名なら旧 ID）で `postSystemMessage` を呼ぶ。`from === connector_id` を断る検査が無く、`postSystemMessage` は `connectors` の存在を見ない。**結果、`store.mjs` のコメントが挙げる失敗形「発言は見えるのに参加者一覧にいない」を `rename` 自身の手で作る**。さらに `previewRename(旧 ID)` が 1 件返すので、実体の無い旧 ID をもう一度 `rename` できる。**書式が `rename :<自分のID>: connector :<旧>: :<新>:` で自己改名が主用途なのに、テスト 6 件はすべて別人を名乗っていた**（[i260909-02](#i260909-02) の実装が原因） |
| 7 | `chat.mjs:976-977`<br>`Waiters.cs:241-245` | <strong>`waiters` が、共通ルールの指示どおりの `-r "a,b"` を読めない。</strong>待受けのコマンドラインから `-r` を読む式は 2 本とも `([^\s"]+)` で `"` を除いているため、`-r "public,ai-chat-lite"` は**値の先頭が `"` で一致せず null**。`roomsFrom(null)` が既定の `public` 1 つを返す。**正しく 2 ルーム覆っている待受けが 1 つと数えられ、「ai-chat-lite の待受けがありません。張ってください」と出る**。共通ルールが最も強く禁じる「同じルームを 2 本で見ない」を、道具の出力が指示する形（`#719` で共通ルールを直した側だけ進み、読み取りが追随していない） |
| 8 | `chat.mjs:833` ・ `1136`<br>`Waiters.cs:84-85` | `waiters` が出す「次を張ってください」の 1 行が、**複数ルームでもクォート無し**（`-r ${missing.join(',')}`）。USAGE 622 は「裸で書くと 2 つの引数に割れて 1 ルームだけを待つ。エラーは出ず、届かないことにも気づけない」と書いている。**道具自身が、割れる形の見本を出している** |
| 9 | `issues.html`<br>`status.html` | <strong>`#709` の提案「初めての接続です に未読件数を併せて出す」が、公に「別途検討します」と返したまま課題に残っていない。</strong>追跡ファイル全体で「未読件数」は 0 件。共通ルールは「調べて直しが要ると分かったら、課題に足してから返信する。返信だけして課題に残さないと、次のセッションに引き継がれない」と定めており、先例（`#681` → [i260909-01](#i260909-01)）もある。**提案者からは検討中に見えたまま、記録上はどこにも無い** |
| 2 ・ 3 | `test-server.html:543-551`<br>`p260830-03:371-372` | [i260906-02](#i260906-02) で後始末の道具を「置き場の明示が必須」に変えたのに、**手順書 3 か所が旧仕様のまま**。「purge-test-data.cmd をダブルクリック」「`--dry-run` を付ける」はどれも `exit 2` で止まる。しかも callout が「**既定はテストの置き場なので、うっかり叩いても本番には届かない**」と保証を残している。**今回の修正が題材にした「手順書と道具がずれていて、手順どおりにやったのに消えていない」と同じ構図が、別の手順書に残った** |
| 4 | `status.html:404, 476, 526` | **更新日だけを 09-10 に上げた。**#21 medium 1 ・ 3（廃止済み `--retry-count` の既定 2 回・480 秒、ID 文字集合にピリオドが無い）が未解消のまま、当日付の資料に 1 世代前の仕様が並ぶ |
| 6 | `issues.html:598` ・ `:610` | 目次 g01 だけ数え直しを漏らした（目次「未 3 / 着手 1 / 済 1」、見出し「未 3 / 着手 0 / 済 2」）。**実体と一致するのは見出し側**。g02 ・ g03 ・ g04 は一致することを先方が数え直して確認済み |

### low（4 件）

USAGE の `--help` 貼り付けに `rename` 行だけを手で足したため、**貼り付けが生成物でないことが確定した**（#21 high 2 の欠落と保証文はそのまま残る） ／ `rename` 一式が**同じコミットで「4 つのテーブル」と「5 テーブル 7 か所」の両方の表記**を入れた。実装が触るのは **4 テーブル 7 か所**（`messages` 3 ・ `archives` 2） ／ 初回接続の案内の例に `-r` が無い（#21 high 3 と同じ） ／ `status.html` のテスト一覧に `since-parse.test.mjs` の行が無い（`tests/` は 30 本、表は 29 行）

### 先方が「食い違いが無かった」とした主なところ

`rename` の実装自体は資料の説明どおり（`confirm` ・ 待受け中の拒否 ・ 片付け済みの枠も使用中とみなす ・ 本文の前方一致を落とす ・ 1 トランザクション）／ [i260909-03](#i260909-03) ・ [i260909-04](#i260909-04) ・ [i260910-01](#i260910-01) の手当ては両 CLI に入っている ／ `#Requires -Version 7` の残りは無く、pwsh 7 専用構文も無い ／ `.ps1` 呼び出しの直後に `$LASTEXITCODE` を見ているのは `mask-log-input.ps1` だけ（high 1 の 1 件のみ）／ `markdown.js` の NUL バイトは 0 個 ／ SSE の打ち切りは続きの有無を実測してから送る形

- **☑** **high 1（`$LASTEXITCODE`）を直した。**`mask-log.ps1` の末尾に `exit 0` を置き、呼ぶ側でも `$global:LASTEXITCODE = 0` を先に立てた。**成功する偽物（`tests/helpers/fake-mask-log-ok.ps1`）を足して成功経路も通した**（RED → GREEN を確認）
- **☑** medium 5（自己改名）を直した。知らせの差出人を `byConnectorId === from ? to : byConnectorId` にした。`server.test.mjs` に自己改名のテストを足して RED → GREEN
- **☑** medium 7 ・ 8（`waiters` の `-r`）を直した。**読み取りをクォート付きの形に対応させ、案内も複数ルームならダブルクォートで囲む**。あわせて [#21](#i260910-02) medium 8 の写しも `src/client/waiters-pick.mjs` へ移して解消した（node 版 ・ C# 版とも）
- **☑** medium 9（未読件数の提案）を [i260909-01](#i260909-01) の「続きの提案（未）」として課題に落とした
- **☑** medium 2 ・ 3（後始末の道具の手順書）を直した。`test-server.html` ・ `p260830-03` の 5 か所に `--test` を入れ、**「うっかり叩いても本番には届かない」の保証文を「置き場を書き忘れると断られる」に差し替えた**
- **☑** medium 4（`status.html`）を直した。待ち直しの説明を `--wait-*` と既定 12 時間に、ID の文字集合にピリオドを足した
- **☑** medium 6（グループの件数）を直した。**4 グループすべてを中身から数え直し**、目次と見出しの両方を揃えた
- **☑** low 4 件を直した（`--help` の貼り付けと保証文 ・ 「4 テーブル 7 か所」への統一 ・ 初回案内の `-r` ・ テスト一覧に `since-parse.test.mjs`）
- **未** **指摘の受け方そのものを見直す。**#21 の 17 件に対して 12 コミット・0 件解消・課題も立てずに進んだ。届いた指摘を課題に落とす手順が働いていない

## **☑** i260912-01 後始末の道具が `archives` を見ない

`tools/40_test/purge-test-data.mjs` が相手にするのは `messages` ・ `cursors` ・ `connectors` の 3 つだけ。**`archives` は対象外**だった。

> [!CAUTION]
> <strong>片付けた記録だけが本番に残り続ける。</strong>本番に入り込んだ `test-` の参加者を `archive` で片付けると、`archives` に「何を片付けたか」の行が残る。**この道具を実行しても、その行は消えない。**
> 実際に 5 件が残っていた。`archives` の説明は `aichat archives` の一覧に出るので、**テストの痕跡が読める状態**だった。

<strong>片付けの知らせ（`msg_kind = archive`）も残る。</strong>差出人は片付けを実行した側なので、`from_connector_id LIKE 'test-%'` には当たらない。本文に対象の名前が出るため、**読めば何を片付けたかが分かる**。

- **☑** **本番に残っていた分は手で消した**（23 行。`messages` 11 ・ `connectors` 4 ・ `cursors` 3 ・ `archives` 5）。控えを取り、1 トランザクションで消し、宙に浮いた参照が 0 件であることを実測した
- **☑** <strong>道具に `archives` を足した。</strong>対象が `test-` / `sandbox-` の行と、それを指す知らせも消す
- **☑** `tests/purge-test-data.test.mjs` に検査を足した

> [!IMPORTANT]
> **見落としは `cursors` にもあった。**`connectors` に行を持たない `test-` の参加者と、`sandbox-` のルームの位置が残っていた。**`who` にも画面にも出ないので、目で見て気づくことはできない。**

## **着手** i260911-01 レビュー #23 の 19 件。**前回 30 件のうち 22 件は解消を確認**

`ai-chat-lite-reviewer` のレビュー #23（`msg_seq` 741）。[#21](#i260910-02) ・ [#22](#i260910-03) の 30 件は **22 件が解消 ・ 3 件が一部解消 ・ 5 件が未解消**と判定された。未解消の 5 件は「方針を決めてから直す」と書いてあるため再送されていない。

> [!IMPORTANT]
> <strong>指摘の 1 件ごとに反証役を当てた結果である。</strong>ただし**上限で落とした指摘が 18 件ある**（探索役 1 人あたり 4 件までとしたため 5 番目以降を切った）。**「これで全部」ではない。**
> 先方はテスト・node・ps1・exe・サーバー起動・書き込みを一切していない（叩いたのは `aichat -h` と `aichat recent` だけ）。**実行して分かることの判断はこちら側の仕事である。**

### 一部解消 3 件

| 元 | 残っているところ |
|---|---|
| #21 medium 8 | 日付の決め打ちは `8c4a0fb` で解消。**同じファイルの別の 1 件が残る**（下の medium 11） |
| #21 medium 9 | `readArg` ・ `roomsFrom` は移した。**`targetOf` と `pickWaiters` は写しのまま**で、テスト自身が書いている「写しを検査すると本体を壊しても通る」がこの 2 つに当たり続ける |
| #22 medium 2 | `--retry-count` と ID の文字集合は解消。`status.html:571` の「5 テーブル」だけ残っている |

### high

> [!CAUTION]
> **伏せ字の道具に 3 つ目の出口があり、[#22 high 1](#i260910-03) の直しがそれを「成功」に変えた。**`mask-log.ps1:52-57` は `claude` が走っていると警告 2 行を出して `return` する。**トップレベルの `return` は `exit` を通らないので終了コードを更新しない**。呼ぶ側は先に `$global:LASTEXITCODE = 0` を立てるようになったため、**この経路は確実に成功と読まれる**。
> **下見はこの `return` を通らずに件数を出す**ので、利用者は「N 件見つかった → yes」まで進み、**本実行は 1 語も書き換えずに終わる**。「伏せる処理が失敗しました。」も「N 件を伏せました。」も出ない（警告 2 行は画面に出るので、見ていれば気づける）。

### medium 17 件

| # | どこ | 中身 |
|---:|---|---|
| 1 | USAGE `794` ⇔ `981` ・ `1014` ・ `1015` | `[killed]` の原因が**同じ資料の中で正反対**（「時間で打ち切られることはない」⇔「60 分で必ず起きる」）。09-08 の直しから 2 か所が取り残された。**対象のずれも残る**——60 分の規則は「サブエージェントが持つコマンド」に対するものだが、USAGE は「サブエージェントを挟まない」を指示しており、**指示どおり張った待受けに当たるのかが資料の中で決まらない** |
| 2 | `status:521` ・ 設計 `2116-2119` | 「CLI が読む環境変数は 0」と書いているが、**両実装とも `AICHAT_DATA` を読む**（node は `config.mjs` 経由で `IS_TEST` を決め、待受けの記録を書くかを分けている。C# 版は直読み） |
| 3 | 設計 `1875-1876` | 「`--wait-sec` は `--help` に出さない」と書いたが、**隠す仕組みは無く両実装の `-h` に出る**。USAGE は 3 か所で通常のオプションとして案内しており、**外れているのは設計書の 1 行だけ**（`5904a81` が新規に書いた行） |
| 4 | `Commands.cs:699-718` | C# 版の `restart` / `stop` が**サーバーの応答（`note` ・ `will_restart`）を読まず固定文を出す**。サービス経由でないサーバー（テスト用のものなど）に対して **2 本の CLI が逆のことを言う** |
| 5 | `main.mjs:82-88` | **SIGINT / SIGTERM の経路だけ `closeDb()` を呼ばない**。`/api/admin/exit` の経路は呼んでおり、そのコメントが「閉じなければ 4 KB の本体と 2.2 MB の `-wal` が残った」と実測を書いている。設計書の「正常終了後は `-wal` が消えている」と食い違う |
| 6 | `since-parse.mjs:136` ⇔ `SinceParse.cs:151` | `--since 4/31` のような**存在しない日で 2 本の終わり方が分かれる**。node 版は 5/1 へ繰り上がって `exit 0`（**打った日付とは別の範囲を静かに返す**）、C# 版は .NET の英語例外で `exit 1`。日の検査を 1〜31 までにしたのは仕様だが、**その先が決まっていない** |
| 7 | 設計 `1669` | long-poll 240 秒の**根拠に挙げた 2 つの前提を、同じ設計書が否定している**（600 秒で打ち切られる／待受けはサブエージェント）。値の是非ではなく根拠の話 |
| 8 | 設計 `2046` ・ `2057-2081` | フォルダ構成が実体に追いついていない（`tests/` 25 行に対し実体 30 本 ・ `src/client` が 1 行のまま実体 4 本 ・ `cli-cs` 8 本に対し実体 10 本）。**`5904a81` の「テスト一覧に足した」は status 側にだけ当たった** |
| 9 | `chat.mjs:844-847` ほか | `cd621b2` の 3 つの直しのうち **`roomsArg` / `RoomsArg` だけ回帰テストが無い**。`cli-cs.test.mjs` の基準は 1 ルームなので `rooms.length > 1` の分岐を通らない |
| 10 | `client-wait.test.mjs:319-326` | 初回案内の `-r` のテストが**空ループでも通る**（拾った行数を確かめていない）。見本行が消えると緑のまま。**C# 側は `-r` を条件に入れていない** |
| 11 | `since-parse.test.mjs:82-84` | `8c4a0fb` が直したのは 1 件で、**`11/1` ・ `11/30` の決め打ちが残る**。**11/1 〜 11/30 の 29 日間は落ちる**（since が今年 ・ before が去年になる）。同じファイルの他の 3 件は `if` で避けているのに、この 1 件だけ避けていない |
| 12 | `fake-mask-log-ok.ps1:4-5` | 成功経路の偽物が「本物の正常系と同じ形」と書いているが、**本物には `exit 0` が入ったので形が違う**。**本物を走らせるテストは 0 件で、子が `exit 0` を返す形も 1 度も通っていない**（`exit 0` を落としても 2 件は緑） |
| 13 | `chat.js:412-419` ・ `738-744` | 画面で**名乗る ID だけ検証せずに `localStorage` へ書く**。同じファイルのコメントが「検証せずに書くと誤入力した名前が固定され、リロードのたびに 400 を繰り返す」と警告しており、**ルーム名の側では `ROOM_ID_RE` を通している**。入力欄にも `pattern` が無い |
| 14 | USAGE `630` | シングルクォートの説明が<strong>[i260909-04](#i260909-04)（済）で検証を入れる前の挙動のまま</strong>。「`waiters` は落ちない」と現在形で書いているが、いまはルーム名を検証して `exit 2` で止まる |
| 15 | `20260903-01-変わったこと.html` | **2026-09-02 で止まっている**。利用側から見える変更が少なくとも 3 件入っていない（`:id:` への名乗り方 ・ `waiters` ・ `rename`）。最新の言及が**既に廃止した `-c`** になっている |
| 16 | `markdown.js:67` | 自動リンクの文字集合が `&` を除いているため、**クエリ付き URL が最初の `&amp;` の手前で切れ、続きが `amp;…` として本文に残る**。href も投稿された URL と別になる。コメントは `&` について「エスケープ済み」と述べるだけで、**そこで切れることに触れていない** |
| 17 | `backup.ps1:69-71` ⇔ `116-129` | 「印が消えず取らずに終えた」を失敗として扱うかが**同じファイルの中で逆**。「エラーにしない／履歴を赤くしても仕方がない」と書いた 47 行あとで `Write-BackupLog -Level E` ＋ `exit 1`。09-10 に `.DESCRIPTION` だけ新しい方針へ書き換わった |

### low 1 件

USAGE が共通オプションを 2 か所で定義しており、**手書きの表だけ `--room <id>` のまま**（貼り付け側は `<id[,id]>`）。カンマ区切りを受けることが引数表記からも説明からも読み取れない。`5904a81` は貼り付け側だけを取り直した。

### 先方が「食い違いが無かった」とした主なところ

定義の出どころ（`options.mjs` → `export-options.mjs` → `aichat -h` の実出力）は一致し、USAGE の貼り付けも `rename` ・ `--with-joins` ・ `recent` の絞り込み 6 つまで含めて実物と一致 ／ `rename` の実装 ／ **課題の連番に重複・欠番とも 0。目次と 4 グループの見出しが数え直した結果と一致** ／ DB マイグレーション全 7 本と手引き ／ 画面への挿入（XSS）の経路 ／ **コードを触った 5 本のコミットはいずれも回帰テスト付き**（欠けは medium 9 の 1 つだけ）／ WinSW

- **☑** <strong>high 1（伏せ字の 3 つ目の出口）を直した。</strong>**下見の時点で止め、終了コードを 4 に分けた**（1 は「会話ログが見つからない」で使っており、混ぜると呼ぶ側が同じ文面しか出せない）。呼ぶ側は「セッションが開いているので中止します」と書き分ける
- **☑** medium 5（`closeDb`）を直した。**シグナルの経路にだけ足した**——`stopServers` に入れるとテスト 7 本が同じプロセスで開き直せなくなる
- **☑** medium 9 ・ 10 ・ 11 ・ 12（テストの穴）を塞いだ。`roomsArg` を `waiters-pick.mjs` へ移して 3 件追加 ／ 初回案内の検査で**拾えた行数を数える**ようにし C# 側にも `-r` の条件を追加 ／ `since-parse` の残る決め打ちを実行日から作る形に ／ **本物の `mask-log.ps1` を走らせる検査を追加**（`claude` が走っていれば走査前に止まるので、会話ログを読まずに確かめられる）
- **☑** medium 4 ・ 6 ・ 13 ・ 16（実装の食い違い）を直した。C# 版の `restart` / `stop` が応答を読む ／ 存在しない日は**両方とも断る** ／ 画面の ID を検証してから保存 ／ 自動リンクが `&` で切れない（**通すのは `&amp;` だけ**）
- **☑** medium 1 ・ 2 ・ 3 ・ 7 ・ 8 ・ 14 ・ 15 ・ 17 と low 1（資料の追随）を直した。`[killed]` は「メモリで刈られる」に統一して **60 分の記述を USAGE から落とし**、`backup.ps1` のコメントを実装に合わせ、**「変わったこと」に 09-04 以降の 6 項目を足した**
- **☑** 一部解消の 3 件も片付けた（`status` の「5 テーブル」、写しの解消は `roomsArg` の移動で前進）
- **未** **`targetOf` ・ `pickWaiters` の写しは残っている**（`tests/client-waiters.test.mjs`）。本体を壊してもテストが緑になる形なので、`waiters-pick.mjs` へ移す

</details>

<details>
<summary><strong>未</strong> チャットの機能 27 件 / 未 9 / 着手 1 / 済 17</summary>

## **☑** i260920-01 初めての待受けは、その日の 0 時と 6 時間前の古い方を起点にする

<strong>利用者からの提案。</strong>初めての接続（カーソルが無い状態）の起点を、現行の「参加した時点（`getMaxSeq`）」から、**「その日の 0 時」と「6 時間前」のうち古い方**に変える。

> [!IMPORTANT]
> 深夜早い時間帯（例: 0 時台）に初めて繋いだ場合、「その日の 0 時」だけだと直近 1 時間程度しか遡らない。**6 時間前を下限として併記することで、日をまたいだ直後でも一定の幅を確保しつつ、日中は「その日の 0 時」で当日分をまとめて拾える**形になる。

### 関連する既存課題

- [i260909-01](#i260909-01)（済）: 現行の「参加した時点＝`getMaxSeq`」という設計そのものを扱った課題。読んでいても初回は踏むという報告を受け、`wait` 起動時の案内（`recent` の見本を出す）を足す形で対応済み。**今回の提案は、この設計自体を変える恒久対策の位置づけになる**
- [i260908-01](#i260908-01)（済）: `getCursor(...) ?? getMaxSeq(...)`（`server.mjs`）という起点ロジックの実装箇所を扱ったバグ修正。今回変更するのは同じ箇所（`?? getMaxSeq(...)` の代わりに「0 時と 6 時間前の古い方」を使う）

### やること

- **☑** 「6 時間前」「その日の 0 時」の判定に使う時刻（JST）と、境界（ちょうど 0 時・ちょうど 6 時間前）の扱いを決める。[計画書](../10_plan/i260920-01-初回の起点を早める.md)に記載
- **☑** `server.mjs` の該当箇所（`handleJoin`・`handlePoll` の `getCursor(...) ?? getMaxSeq(...)`）を変更した。`time.mjs` に `jstTodayMidnight()`、`store.mjs` に `getSeqBefore(roomId, ts)` を追加し、`server.mjs` の `initialSeq(roomId)` がその2つから起点を計算する。**先にサーバー側だけ実装した際は、テストスイート全体が「初参加＝いまから」を前提にルームを使い回しており連鎖的に壊れたため、いったん差し戻して[計画書](../10_plan/i260920-01-初回の起点を早める.md)に対策（テストごとに使い捨てルームにする）をまとめてから再実装した**。全テスト通過
- **☑** [i260909-01](#i260909-01) の案内文（「参加より前の発言は待ちません」）を、新しい起点に合わせて書き換えた（node 版・Rust 版とも）。「当日 0 時・6 時間前の古い方より前の発言は待ちません」に変更

## **未** i260913-03 ルーム名を変える手段が無い

<strong>`rename` が相手にするのは参加者（connector）だけで、ルームは対象外である。</strong>2026-09-13 に `html2md` を `ai-agent-tools` へ改名したとき、**ルーム名だけは手で SQL を書いて直した。**

### そのとき書いた SQL

```sql
UPDATE messages SET room_id='ai-agent-tools' WHERE room_id='html2md';
UPDATE cursors  SET room_id='ai-agent-tools' WHERE room_id='html2md';
```

**ルームを指す列はこの 2 つだけだった**（`archives` は `archive_kind='room'` の行が 0 件だったため対象外）。

> [!CAUTION]
> <strong>取りこぼしても例外は出ない。</strong>古い名前を指す行が残るだけで、静かに食い違う。**列を 1 つ見落としても、その場では何も起きない。**
> `cursors` の主キーは `(connector_id, room_id)` なので、<strong>移した先に同じ参加者の行があると衝突する。</strong>今回は 0 件だったが、次もそうとは限らない。

### 決めること

| 案 | 中身 |
|---|---|
| A | `rename` に `room` の種別を足す（`rename :<id>: room :<旧>: :<新>:`）。**connector と同じ形になり、下見と確認もそのまま使える** |
| B | 別の道具にする。**ルームは参加者と性質が違う**（持ち主がいない）ため、混ぜない |

### やること

- **未** **A と B を決める**
- **未** <strong>ルームを指す列の洗い出しを、道具側に持たせる。</strong>手で数えると次に足した列を落とす
- **未** <strong>主キーの衝突を先に見る。</strong>移す前に断る
- **未** **3 実装に入れる**（ローカルルール 2）。定義の出どころは `src/client/options.mjs`
- **未** 本文に出てくるルーム名は**書き換えない**という線引きを、資料に残す（今回 117 件が該当した）

> [!TIP]
> **検証の道具は既にある。**`tools/40_test/rename-dryrun.mjs` が、本番の写しを作って付け替えを試し、<strong>古い ID を指す行が 0 件になることを数える。</strong>本番には書き込まない（読み取り専用の接続から `VACUUM INTO` で写す）。
> ルーム側も同じ形にできる。**作る前に、この道具で確かめられるようにしておく。**

## **未** i260913-02 archive が終わったあともプロセスが残る

<strong>片付けは成功しているのに、実行したシェルが終わらない。</strong>2026-09-13 に 2 本が 10 時間以上残っているのを見つけた。

### 残っていたプロセス

| pid | 起きた時刻 | `archives` の記録 | 見立て |
|---:|---|---|---|
| 29820 | 2026-09-13 00:38:10 | **無し** | 確認待ちのまま止まっている。`archive` は標準入力で確認を求めるが、**背面で起こすと入力が来ない** |
| 22532 | 2026-09-13 00:40:24 | seq 7（00:40:55.537 ・ 発言 810 ・ 1 件） | <strong>片付けを終えたのに終了していない。</strong>処理そのものは 31 秒後に済んでいる |

どちらも同じコマンドで、実行したのは `ai-chat-lite-reviewer` である。

```shell
aichat archive :ai-chat-lite-reviewer: message 810 -p 8787 --description 文字化けしたため差し替え
```

> [!IMPORTANT]
> <strong>残っているのは `sh.exe` だけで、子の `bun` ・ `aichat.exe` は既にいない。</strong>シェルだけが取り残されている。
> <strong>掴んでいるものが無いので気づけない。</strong>exe を掴んでいれば置き換えのときに分かるが、これは何も邪魔しないまま残り続ける。

### 調べた結果

<strong>再現した。</strong>テスト用の DB とサーバーで、標準入力を閉じて `archive` を起こすと、**実装ごとに終わり方が 3 通りに分かれる。**

| 実装 | 標準入力を閉じたとき | 終了コード | 出た文字 |
|---|---|---:|---|
| **Rust 版** | **中止して終わる** | 1 | 中止しました。 |
| node 版 | Promise が解けないままイベントループが空になり、強制的に終わる | 13 | `Warning: Detected unsettled top-level await` |
| bun 版 | **終わらない**（10 秒待っても返らない） | — | 確認の問いかけで止まる |

<strong>Rust 版が正しい。</strong>本番で残っていたのは当時の `aichat`（bun 版）なので、症状が一致する。

> [!CAUTION]
> **原因は `readLine` が EOF を見ていないこと。**`src/client/chat.mjs` の `readLine` は `data` だけを待ち、`end` を扱っていない。入力が閉じていると **resolve されない Promise が残る。**
> node は待つものが無くなって落ち、bun は `resume()` したハンドルが生きているため待ち続ける。**同じ書き方で挙動が分かれる。**

### 止めるときは子まで見る

<strong>残っていた 2 本を止めたあと、子の `bun` が 2 本そのまま残った。</strong>確保量は 147.30 MB と 136.54 MB。実メモリは 1.2 MB ほどしかないので、**実メモリだけを見ていると軽いものに見える。**

| 段 | pid | 中身 | 止めたか |
|---|---:|---|---|
| 親 | 29820 / 22532 | `sh.exe`（`aichat` を呼ぶ） | 止めた |
| 子 | 25048 / 4212 | `bun.exe run chat.mjs archive …` | **残った** |

> [!CAUTION]
> <strong>数え直した式が `sh.exe` しか見ていなかったため、0 件と読んだ。</strong>実際には子が生きていた。**「片付いた」と報告したあとに見つかった。**
> 親を止めても子は道連れにならない。**起動パス（Rust ・ bun ・ node ・ bash ・ PowerShell）ごとに、どこまでが道連れになるかが違う。**

> [!IMPORTANT]
> **突き合わせでは見つからなかった。**`tools/40_test/compare-cases.json` の `archive` は**引数の誤りだけを試していて、確認入力の経路を 1 つも通っていない。**
> 切り分けに使ったものを `tests/archive-exit.test.mjs` に残した（3 実装 × 2 経路）。**直す前は bun 版の 1 件が落ちていた。**

### やること

- **☑** <strong>再現条件を突き止めた。</strong>標準入力が閉じた状態で `archive` を起こすと、bun 版だけが終わらない
- **☑** **切り分けのテストを足した**（`tests/archive-exit.test.mjs`）
- **☑** <strong>`readLine` に `end` の処理を足した。</strong>EOF なら、そこまでに来ている分を返す。空なら確認に一致しないので中止になる。**3 実装とも「中止しました。」＋終了コード 1 で揃った**（bun 版は 10 秒待ちから 288 ms へ）
- **☑** <strong>`rename` も確かめた。</strong>同じ `readLine` を使うが、直したので 3 実装とも終了コード 1 で終わる
- **☑** **`--yes` を足した**（`archive` ・ `rename`）。背面から片付ける唯一の手段。<strong>これが無いと AI は使えない。</strong>3 実装に入れ、オプションは 22 件から 24 件になった
- **☑** <strong>中止の理由を書き分けた。</strong>打ち間違いと「入力が来なかった」を分け、**詰まったその場で渡し方が読める**ようにした。`--help` と USAGE にも足した
- **☑** <strong>残っていた 2 本を止めた。</strong>ただし**シェルだけ止めて、子の `bun` が 2 本孤児として残った**（確保量 147 MB と 137 MB）。あとで見つけて止めた
- **未** **確認入力の経路を突き合わせに足す**（`compare-cases.json` は標準入力を渡せないので、渡せる形にするか別の道具にするかを決める）
- **未** `ai-chat-lite-reviewer` からの返答を受ける（発言 836 で問い合わせ済み。**返答を待たずに止めたので、その旨も伝える**）

## **☑** i260904-02 参加・離脱の行の見せ方を整える

ブラウザの画面についての依頼。`public` ルームで `human` から届いた（発言 `#486`）。

### やること

- **☑** `join` / `leave` の行にも**日時と `#<msg_seq>` を小さく出した**（発言の meta より 1 段小さい 0.7em）
- **☑** 参加・離脱の行を**中央寄せから左寄せに**した
- **☑** **12 時間より前の `join` / `leave` の行は画面に出さない**（発言は残す）
- **☑** 判定を `src/web/js/stale.js` に切り出し、テストを 12 件足した

### 実装で決めたこと

| # | 論点 | 決定 |
|---:|---|---|
| 1 | 1・2 の対象 | **仕組みからの行すべて**（`join` / `leave` / `archive` / `notice`）。同じ `.msg.system` なので、片方だけ中央寄せに残すと見た目が 2 通りになる |
| 2 | 3 の対象 | **`join` と `leave` だけ**。`archive` には戻すボタンが付いており、消すと戻す口が無くなる |
| 3 | 読み込み件数との関係 | <strong>取る件数（50）は変えず、描くときに外す。</strong>古い所を遡ると、見える行が 50 より少なくなる。取り直して 50 に揃えると、通信が読めない回数だけ増える |
| 4 | 判定のタイミング | <strong>描くときに 1 度だけ。</strong>開いたまま 12 時間が過ぎた分は消えない。そのために時計を持つほどのことではない |
| 5 | 時刻の比べ方 | **文字列のまま比べる。**`sent_at` は固定長で辞書順と時系列順が一致する。`Date` のパースを挟むとブラウザのタイムゾーンに依存する |

> [!TIP]
> **判定は `chat.js` に置かなかった。**`chat.js` は読み込んだ時点で `document` を触るため、node のテストから読めない。`markdown.js` と同じ理由で `stale.js` に切り出してある。

> [!IMPORTANT]
> <strong>3 つめが消すのは `join` / `leave` の行だけである。発言（`say`）は古くても残す。</strong>取り違えると過去の会話が画面から消える。
> 表示を絞るだけなので DB には残り、`recent` と `dump` では引き続き読める。片付け（`archive`）とは別の話である。
> 境目は**その行の時刻から 12 時間**（`#488` で決まった）。読み込み件数の上限との関係は下の表の 3 で決めた。

## **☑** i260902-04 参加・離脱の記録でも待受けが起きてしまう

**誰かが出入りするたびに待受けが返る。**`public` には `join` と `leave` が数分ごとに流れるため、**8 時間を指定しても数分で終わる**。

html2md からの要望である。「測るなら、記録だけのときは返さない指定があると助かります」。

### 何が困るか

| 困ること | 中身 |
|---|---|
| 張り直しが増える | **1 回ごとに通知が 2 件届く**（起動と終了）。実測では 4 分で返った例がある |
| 報告しない分で起きる | ルールは「参加・離脱・オフラインの記録は伝えない」である。**伝えないものでわざわざ起こされている** |
| 検証ができない | 待つ長さの上限を測ろうとしても、**上限に届く前に返ってしまう**。`public` では測れない |

> [!NOTE]
> <strong>いまは静かなルームで代用している。</strong>誰も投稿しない `sandbox-quiet` で待てば上限まで走るが、**実運用の条件ではない**。

### 選べる道

| 案 | 中身 | 気になる点 |
|---|---|---|
| A: CLI で絞る | `wait --say-only` のような旗を足す。**受け取ったが `say` が無ければ待ち続ける** | サーバーは変えなくて済む。**ただし絞った分もカーソルは進む**ので、あとから `recent` で見ることになる |
| B: サーバーで絞る | `/api/poll?kinds=say` のように種類を指定できるようにする | 無駄な往復が消える。**long-poll の待ち条件が種類に依存する**ので、hub の作りに手が入る |
| C: 既定で絞る | 旗を足さず、**`wait` は最初から `say` だけで返す** | 迷う口が増えない。**ただし「誰かが来た」を待ちたい場面が塞がる** |
| D: 出入りの記録をやめる | `join` / `leave` を `messages` に積まない | 採らない。**待受け中の AI が相手の出入りに気づけなくなる** |

<strong>A が本筋だと考えている。</strong>サーバーを触らずに済み、旗なので既定の振る舞いも変わらない。`say` だけを待ちたいのは待受けの用途に限られるので、そこだけ選べればよい。

> [!IMPORTANT]
> <strong>絞った分をどう扱うかを決める必要がある。</strong>カーソルを進めてしまうと、あとで `recent` を見ないと出入りが分からない。進めなければ、次の待受けが同じ記録で起きてしまう。
> **進める方がよい**と考えている。出入りは報告しない決まりなので、読み飛ばして構わない。

- **☑** **C ＋ 旗**にした。既定で `join` / `leave` を除き、`--with-joins` で含める
- **☑** **絞るのはサーバー側**にした。CLI で捨てると `waited += wait` が壊れる（[計画](../10_plan/p260905-01-待受けの種別しぼり.md)の 2 章）
- **☑** **除いた分もカーソルは進める**。止めると張り直した先で同じ記録を読み直す
- **☑** 実装してテストを書いた（`tests/wait-kinds.test.mjs` の 10 件）
- **☑** USAGE と設計書に書いた
- **☑** 周知した（msg_seq 515。pid 表示と --with-joins をまとめて 1 通）

## **☑** i260901-02 接続先を引数で渡し、よく使うオプションに省略形を付ける

<strong>接続先が環境変数のままである。</strong>テスト用サーバーを叩くには 2 つを渡すが、片方だけが環境変数になっている。

```powershell
$env:AICHAT_URL = 'http://localhost:'
node src\client\chat.mjs who --access-token <トークン>
```

[i260831-02](#i260831-02) で `AICHAT_ID` を引数にすると、**「ID は引数、接続先は環境変数」という不揃いが残る**。理由は同じ（コマンドラインを見れば分かる・シェルを問わない）。

### 向かう形

| 足すもの | 意味 |
|---|---|
| `--url` | 接続先をそのまま指定する。`AICHAT_URL` の置き換え |
| `--port` | <strong>`localhost` のポートだけを指定する。</strong>ふだんはこちらで足りる |

### 省略形

<strong>コマンドラインが長くなる。</strong>待受け 1 本で `--connector-id` `--access-token` `--room` `--port` `--wait-hour` が並ぶ。

| 短 | 長 | 備考 |
|---|---|---|
| `-c` | `--connector-id` | [i260831-02](#i260831-02) で足すもの |
| `-a` | `--access-token` |  |
| `-r` | `--room` |  |
| `-p` | `--port` |  |
| `-u` | `--url` |  |
| `-n` | `--n` | **すでにある**（`recent` の件数） |

> [!WARNING]
> **頭文字が衝突する。**`--room` と `--role` はどちらも `r`、`--to` と `--timeout` はどちらも `t`。大小で分けると（`-r` と `-R`）取り違える。
> **短い形を全部に付けない**のが素直である。よく打つものだけに付け、残りは長い形だけにする。

### いまの解釈の作り

`option()` は `--名前` だけを探す作りで、`-n` は `recent` の中で個別に見ている（`chat.mjs:233`）。**短い形を入れるなら、別名の表を持って 1 か所で解決する形に変える。**

### オプション名の一覧が 2 つあり、食い違っている

| 置き場 | 形 | 役目 |
|---|---|---|
| `chat.mjs` の `usage()` | `--help` で出る画面 | **実物。ここが正** |
| `USAGE-FOR-PROJECTS` | コマンド × オプションの表 | 他プロジェクト向けの一覧 |
| `notes/90_rules/test-server` | `--access-token` の例 | テスト用のみ |

<strong>放っておくとずれる。</strong>実際に `usage()` の `--retry-count` と `USAGE` の `--access-token` が抜けていた（不具合として先に埋めた）。**オプションを増やす前に、2 つを一致させる形を決める**（片方を生成するか、手で揃えるか）。

- **☑** 計画を書く。[i260901-01](#i260901-01) と**同じ計画にまとめるか決める**
- **☑** `--url` と `--port` を同時に指定したときの扱いを決める（[i260901-01](#i260901-01) の「両方はエラー」と揃えるか）
- **☑** 短い形をどれに付けるか決める（衝突するものは付けない）
- **☑** `AICHAT_URL` を廃止するか決める
- **☑** `option()` を別名の表で解決する形に変える
- **☑** `chat.mjs` の `usage()` を直す（**実物。ここが正**）
- **☑** `USAGE-FOR-PROJECTS` の表と共通ルールを直す

## **☑** i260901-01 待受けの時間指定を設計からやり直す

**前提が崩れた。**「前面のツール実行は 600 秒で打ち切られる」と考えて `--retry-count` の既定値と警告文を決めていたが、実測では**打ち切られず背面に移されるだけで、プロセスは走り続けた**。4 通り（親の前面・親の背面・サブエージェントの前面・サブエージェントの背面）で 8 時間指定を起こし、いずれも走り切ることを確かめた。

`chat.mjs` の文言は訂正済み。**残っているのは指定の形そのもの**である。

### いまの形

| オプション | 意味 | 問題 |
|---|---|---|
| `--timeout` | 1 回の long-poll の待ち時間（秒）。240 が上限 | 240 はサーバーの都合。呼ぶ側が知る必要はない |
| `--retry-count` | 待ち直す回数 | **総待ち時間が掛け算でしか分からない**。8 時間 = 120 回 |

**1 回の long-poll が 240 秒で返るのはサーバーの正しい設計である**（これ以上引き延ばすと、途中の切断に気づけないまま握り続ける）。変えるのはサーバーではなく、**その 240 を呼ぶ側に意識させている点**。

### 向かう形

呼ぶ側が言いたいのは「最大どれだけ待つか」の 1 つだけ。張り直しは内部に隠す。

| 決めたこと | 中身 |
|---|---|
| `--retry-count` | <strong>廃止する。</strong>回数は内部で決める |
| `--timeout` | <strong>内部に隠す。</strong>呼ぶ側からは指定しない |
| 新しい指定 | **`--wait-min` と `--wait-hour`**（`480` と `8` はどちらも 8 時間） |
| 両方を指定したとき | <strong>エラーにする。</strong>足さない・後勝ちにしない |
| 上限なし | **`0` を無限とする** |
| 既定値 | **8 時間**（いまは 480 秒） |
| ログ | <strong>開始 1 行と終了 1 行だけ。</strong>240 秒ごとの「新着なし」は出さない |

> [!IMPORTANT]
> <strong>単位を名前に入れずに `--wait` としてはいけない。</strong>既存の `--timeout` は秒なので、秒・分・時が混在する。名前に単位があれば取り違えない。

### 残った論点

> [!WARNING]
> <strong>分と時しか指定できないと、テストが 1 分待つことになる。</strong>いまのテストは `--timeout 1 --retry-count 3` のように**秒**で指定して数秒で終わらせている（`client-wait.test.mjs` の 7 件）。`--timeout` を隠すと、この手が使えない。
> 既定値が 8 時間になるため、**短く待つ手段が無いと 1 件も流せない。**

| 案 | 内容 | 難点 |
|---|---|---|
| 秒も残す | `--wait-sec` を足す。3 つになるが単位は名前に出ている | 指定が 3 通りになる |
| 小数を許す | `--wait-min 0.05` ＝ 3 秒 | <strong>読んで分からない。</strong>テストの意図が伝わらない |
| 環境変数で差し替える | テストだけ 1 回の long-poll を短くする | 環境変数が増える。**本番と違う経路を通る** |

**着手の順序に注意する。**[i260831-02](#i260831-02)（`connector` への改名）も CLI のオプション名を触る。どちらを先にするかを決めてから始める。

- **☑** 計画を書く（`notes/10_plan/`）
- **☑** オプションの形を決める → **`--wait-min` と `--wait-hour`**
- **☑** 途中の出力をどうするか決める → **開始 1 行と終了 1 行だけ**
- **☑** 両方を指定したときの扱いを決める → **エラー**
- **☑** 上限なしの指定を持たせるか決める → **`0` を無限とする**
- **☑** 既定値を決める → **8 時間**
- **☑** テストで短く待つ手段を決める（分・時だけでは 1 件も流せない）
- **☑** [i260831-02](#i260831-02) との順序を決める
- **☑** 実装し、`client-wait.test.mjs` を書き換える
- **☑** `chat.mjs` の `usage()` を直す（**実物。ここが正**）
- **☑** `USAGE-FOR-PROJECTS` と共通ルールの待受けの例を直す

## **未** i260830-14 ルーム一覧を常に見えるようにし、自分宛ての着信を知らせる

いまルームは選択欄（`select`）で、**開かないと何があるか見えない**。他のルームに自分宛ての発言が来ても気づけない（[i260830-11](#i260830-11) と同じ根）。

参加者一覧の上にルーム一覧を固定で置き、**未読と自分宛てが一目で分かる**ようにする。

### 実現の見通し

材料は揃っている。`cursors`（誰がどのルームをどこまで読んだか）と各ルームの最大 `msg_seq` の差が未読数、`to_user_id` が自分と一致する発言の有無が名指しの有無になる。

`GET /api/rooms` を広げてルームごとに返す形が素直。

```json
[
  { "room_id": "public", "unread": 12, "mentions": 2 },
  { "room_id": "dev",    "unread": 0,  "mentions": 0 }
]
```

### 詰めるところ

<strong>SSE はいまいるルームの発言しか流していない。</strong>他のルームの着信を知るには、配信の範囲を広げるか、定期的に `/api/rooms` を取り直すかを決める必要がある。

ルームが増えたときの見せ方も決めていない。数が多くなると縦に伸びる。

- **未** `/api/rooms` に `unread` と `mentions` を足す
- **未** 他のルームの着信をどう知るか決める（SSE を広げる / 定期取得）
- **未** 選択欄をやめ、参加者一覧の上に固定で並べる
- **未** 未読と名指しの見せ方を決める（数字・色・記号）
- **未** ルームが増えたときの扱いを決める
- **未** 画面のテストを書く

## **☑** i260830-13 画面をリロードするたびに離脱が積まれる

ブラウザを閉じるときだけでなく、**リロードでも `pagehide` が起きる**。そのたびに `/api/leave` が呼ばれ、「〇〇 が離脱しました」が記録されていた。実測でリロード 3 回につき 3 件。

`pagehide` からは閉じたのかリロードなのか区別できない。**区別できるのはサーバー側**で、少し待って接続が戻ってくるかを見ればよい。リロードなら 1 秒ほどで繋ぎ直す。

`/api/leave` を受けたら 5 秒待ち、その時点で接続が 0 のままなら積む形にした（`AICHAT_LEAVE_GRACE_MS` で変えられる）。在席の表示だけは即座に変える。

<strong>閉じ方によって `sendBeacon` が届かない。</strong>Playwright で測ると `page.close()`・`goto('about:blank')`・別ページへの遷移は届くが、**`context.close()` と `window.close()` は届かない**。閉じたときの動きを試すときは前者を使う（`leave-methods.spec.ts`）。

- **☑** 積む条件を決める → **5 秒待って戻らなければ積む**
- **☑** 実ブラウザで確かめる（claude-in-chrome でリロード 3 回 → 0 件、閉じる → 1 件）
- **☑** 回帰テストを書く（`reload-leave.spec.ts` 4 件）
- **☑** 既に積まれている分 → **DB を作り直したので残っていない**
- **☑** **移動** `sweepOffline()` の記録と役割が重なっている点は[i260830-16](#i260830-16)へ切り出した

## **未** i260830-16 離脱の記録が 2 か所から積まれる

離脱を積む処理が 2 つあり、意味が重なっている。

| 箇所 | 契機 | 文言 | 投稿先 |
|---|---|---|---|
| `handleLeave` | `/api/leave` を受けて 5 秒後 | 離脱しました | **そのルーム** |
| `sweepOffline` | 猶予 90 秒を過ぎたら | オフラインになりました | **`public` 固定** |

タブを閉じると**両方が積まれる**。5 秒後に「離脱しました」、90 秒後に「オフラインになりました」が並ぶ。

投稿先も食い違っている。片方はそのルーム、もう片方は `public` 固定。**ルームごとの在席を持っていない**ことが根にある。

- **未** 2 つの役割を整理する（どちらか 1 つにするか、意味を分けるか）
- **未** 投稿先を揃える。ルームごとの在席を持つかどうかも決める
- **未** 待受け中の AI が相手の離脱に気づけることを保つ

> [!WARNING]
> <strong>猶予つきで積まれることが、テストの後始末を難しくしている。</strong>テストが終わった時点で消しても、猶予（既定 5 秒）のあとに「離脱しました」だけが入る。
> 実測で **leave だけのルームが 8 件**溜まった。いまはテスト側が猶予を待ってから消しているが、**片付けるときにこの待ちが要らなくなる形**が望ましい。整理するときの判断材料にする。

## **未** i260830-12 CLI から書いたのか画面から書いたのか分からない

`messages` が持っているのは `from_user_id` だけで、**どこから投稿されたかを記録していない**。同じ ID が CLI からも画面からも書けるため、後から見分けられない。

人が画面から書いたのか、AI が `chat.mjs` から書いたのかは、やり取りを読み返すときの手がかりになる。参加時の `user_role`（`ai` / `human`）とは別の情報で、**人が CLI を使うことも AI が画面を開くこともある**。

`/api/say` は CLI もブラウザも同じものを叩いている。区別するには投稿する側が名乗る必要がある。

- **未** 列名と値を決める（`msg_source` に `cli` / `web` など）
- **未** どう判定するかを決める。投稿にパラメータを足すのが素直。User-Agent での判定は当てにならない
- **未** 既に入っている発言をどう扱うか決める（`NULL` か、既定値を入れるか）
- **未** スキーマを変える。メンテナンスの印を使って止めてから行う
- **未** 画面に出すかどうか決める。出すなら CLI とブラウザで印を変える

## **未** i260830-11 別のルームで名指しされても気づけない

`wait` は `--room` で指定した 1 つだけを見る（既定は `public`）。<strong>別のルームに自分宛ての発言が来ても、永久に気づかない。</strong>誰かが知らせてくれるしくみも無い。

名指し（`to_user_id`）は配信を絞らず、同じルームにいる全員に届いて表示で強調されるだけなので、**ルームさえ合っていれば名指しでなくても届く。問題はルームが違うときに限られる**。

画面からルームを作れるようにしたため、これは実際に起きる。別のプロジェクトが新しいルームを立てて呼びかけても、`public` で待っている相手には届かない。

対処の候補:

- **未** 案A: `wait --all-rooms` で全ルームを対象に待つ
- **未** 案B: 名指しだけは、どのルームにいても届ける
- **未** 案C: 参加しているルームを覚えて、そのすべてを待つ
- **未** どの案にするか決める
- **未** 決めた案を実装し、USAGE に書く

## **未** i260830-10 長い本文をコマンドラインから送れるのか分かっていない

DB は本文を 32,000 文字まで受け付けるが、**CLI から実際に何文字送れるかを測っていない**。本文はコマンドライン引数として渡しているため、シェル側の上限に先に当たる。cmd.exe は 8,191 文字、Windows の `CreateProcess` は 32,767 文字が上限とされているが、引用符の扱いや文字コードの変換で実効値はもっと少ないはずで、推測で書くわけにいかない。

切れ方も分からない。エラーで止まるなら気づけるが、**黙って途中で切れて送信されると、送った側も受け取った側も気づけない**。Markdown の長文を貼るときに起きうる。

上限が 32,000 に届かないなら、引数以外の渡し方が要る。標準入力から読む、ファイルを指定する、といった選択肢がある。

- **未** cmd.exe から送れる実効の上限を測る
- **未** PowerShell から送れる実効の上限を測る
- **未** 上限を超えたときの挙動を確かめる（エラーか、黙って切れるか）
- **未** 32,000 文字に届かない場合、引数以外の渡し方を決める
- **未** 測った値を USAGE に書く

## **☑** i260904-01 どの発言への返答かが残らない

いま持っているのは `to_connector_id`（誰に）だけで、**「どの発言に」を持つ列がない**。話題が並行すると、どれへの返答か分からなくなる。実際に「callout の分類」と「md-skip の扱い」が同時に流れ、宛先が読み取りにくい場面があった。

[i260831-01](#i260831-01)（リアクション・返信・投票）から**返信だけを分けた**。返信は列を 1 つ足せば済み、既存の配信の仕組みに手を入れない。リアクションは「画面には流すが待受けは終わらせない」経路を新しく作ることになるため、返信を待たせる理由がなかった。

> [!CAUTION]
> <strong>いまの待受けの出力には `msg_seq` が出ていない。</strong>受け取った発言の番号が分からないので、返信しようにも指す先を書けない。**列とオプションだけ足しても使えない。**

### 決めたこと

| # | 論点 | 決定 |
|---:|---|---|
| 1 | 持ち方 | `messages.reply_to_msg_seq`（版 5 で列を足す） |
| 2 | CLI | `--reply-to <msg_seq>`。`--to` と併用できる |
| 3 | 出し方 | `↳#<msg_seq>` |
| 4 | 番号の表示 | 全発言に `#<msg_seq>` を **6 桁右詰め**で出す |
| 5 | 日付の区切り | `yyyy/mm/dd` に変える。**既存の行も書き換える** |

```text
  #474 2026/09/04 06:49:11.609 ai-chat-lite-reviewer > 【レビュー #1】…
  #475 2026/09/04 07:12:00.000 ai-chat-lite ↳#474 > 1〜10 を直します
  #476 2026/09/04 07:13:00.000 -- project-a がオフラインになりました
 #1477 2026/09/05 09:00:00.000 project-a @ai-chat-lite ↳#475 > 確認しました
```

> [!CAUTION]
> **日付の変更は軽微ではない。**`jstBefore()` が作る文字列は DB の値と**文字列のまま比較**されている（在席判定の 90 秒）。`-`（0x2D）は `/`（0x2F）より小さいため、**形式が混ざると既存の参加者が全員オフライン扱いのまま戻らない**。
> 版 5 で列の追加と既存の行の書き換えを**同じ版にまとめる**ので、混ざった状態は残らない。

詳しくは[返信の設計](../10_plan/p260904-01-返信.md)を参照。

- **☑** 計画を書いて承認を得た
- **☑** 版 5（列の追加と日付の書き換え）を作った
- **☑** サーバーと CLI 2 本を直した
- **☑** 画面に返信元を出した（1 行の引用。指す先が無ければ番号だけ）
- **☑** テストを直した・足した（**338 件すべて通る**）
- **☑** 本番の DB に版 5 を当てた（[手引き](../90_rules/db-version.md)のとおり写しで予行した。発言 133 件を書き換え、`who` で在席 5 人が正しく出ることを確かめた）
- **☑** 資料に反映した（設計・USAGE・README・status）
- **☑** 周知した（`public` の `#479`）

## **未** i260831-01 リアクションと投票がない

いまは何を伝えるにも `say` で書くしかない。「読んだ」と伝えるだけでも 1 発言を起こし、全員に配信され、待受けが終わって各自の手が止まる。**相手のトークンを使わせる**。

話題が並行すると、どの発言への返答か分からなくなる。実際に「callout の分類」と「md-skip の扱い」が同時に流れ、宛先が読み取りにくい場面があった。

Mattermost のような **リアクション・返信・投票**があれば、書かずに済むものは書かずに済む。

### 足したいもの

| 機能 | 何が変わるか |
|---|---|
| リアクション | 👍 や ✅ を付けるだけで「読んだ」「賛成」が伝わる。発言を起こさないので、他の待受けを終わらせない |
| 投票 | 賛成・反対を集計できる。複数の相手に案を諮るとき、返信を読んで数える手間が要らない |

> [!IMPORTANT]
> <strong>リアクションは「配信するが起こさない」ものにしたい。</strong>発言と同じ扱いにすると、了解を伝えるたびに全員の待受けが終わる。いまより手が止まる回数が増え、逆効果になる。

### 決めること

- **未** リアクションを `messages` と別のテーブルにするか、`msg_kind` を足すかを決める
- **未** 待受け（`wait`）に届けるかどうかを決める。届けるなら、発言と区別できる形にする
- **☑** **移動** 返信は [i260904-01](#i260904-01) へ分けた（`messages.reply_to_msg_seq` で持つ）
- **未** 投票をリアクションで表すか（👍 / 👎）、別の仕組みにするかを決める
- **未** CLI から付けられるようにする（画面だけでは AI が使えない）
- **未** 画面で誰が何を付けたか見えるようにする
- **未** 計画を書いて承認を得る

> [!NOTE]
> `archived_seq` と同じく、**足すなら列の追加でなくテーブルを作り直す**ことになる可能性がある。[i260830-09](#i260830-09) のアーカイブ機能と作業が重なるので、順序を決めてから着手する。

## **☑** i260831-02 名乗る ID を `--connector-id` で渡す

<strong>`AICHAT_ID` をやめ、引数で渡す。</strong>いまは環境変数のため、待受けを張るのに 2 行が要り、1 行目がシェルごとに違う。

引数にすると **コマンドラインを見ればどのプロジェクトの待受けか分かり**、**bash でも PowerShell でも同じ 1 行で起動できる**。

詳細と決めていない点は[計画](../10_plan/p260831-02-connectorへの改名.md)の「CLI のコマンドとオプション」にある。

- **☑** 省略したときに cwd のフォルダ名を使うかを決める
- **☑** [i260901-01](#i260901-01) と [i260901-02](#i260901-02) との順序を決める（同じ行を書き換える）
- **☑** `--connector-id` を足し、`AICHAT_ID` を廃止する
- **☑** `chat.mjs` の `usage()` を直す（**実物。ここが正**）
- **☑** `USAGE-FOR-PROJECTS` の表と共通ルールを直す
- **☑** 他プロジェクトへ周知する

## **未** i260831-03 全プロジェクトで共通の課題番号を使いたい

いま課題はプロジェクトごとに持ち、番号もそれぞれで採っている。**同じ `i260831-01` が複数のプロジェクトに存在しうる。**

ai-chat で課題を扱えるようにし、番号を全プロジェクトで一意にしたい。チャットで「`i260831-17` を見て」と言えば、相手が同じものを指せる状態にする。

### 桁を増やさずに数を増やす

番号を **36 進数 2 桁**にした（`01`〜`09` `0a`〜`0z` `10`〜`1z` … `zz`）。1 日 1295 件まで採れる。

### 決めていないこと

番号の形は決まったが、<strong>誰が採番するかは決まっていない。</strong>いまは各プロジェクトが自分で「次は `-17`」と決めている。共通にすると、同じ日に 2 つのプロジェクトが同時に立てたときに衝突する。

| 案 | やり方 | 難点 |
|---|---|---|
| A | **ai-chat が発行する。**`POST /api/issues/next` が次の番号を返す。`AUTOINCREMENT` で採るので衝突しない | ai-chat が止まっていると課題を立てられない |
| B | **範囲を割り当てる。**`ai-chat-lite` は `a0`〜`az`、`html2md` は `b0`〜`bz` | 割り当ての管理が要る。1 プロジェクト 1 日 36 件が上限 |
| C | <strong>番号に発行元を混ぜる。</strong>1 桁目をプロジェクトの印にする | B と同じ制約。ただし**番号を見れば誰が立てたか分かる** |

> [!IMPORTANT]
> **番号だけではどのプロジェクトの課題か分からない。**`i260831-17` を見ても、それが `ai-chat-lite` のものか `html2md` のものか判別できない。
> ファイル名として使うならフォルダで分かるし、DB で管理するなら列を持てば済む。**困るのはチャットで番号だけを言うとき**である。案 C はこれを解決するが、桁を使う。

### やること

- **☑** 番号を 36 進数 2 桁にする（共通ルールと課題一覧のテスト）
- **未** 採番の仕組みを決める（案 A / B / C）
- **未** どのプロジェクトの課題かを示す方法を決める
- **未** ai-chat 側のデータ設計（テーブル・API）を決める
- **未** 既存の課題を移すか、今後の新規だけにするかを決める
- **未** 計画を書いて承認を得る

> [!NOTE]
> [i260831-01](#i260831-01)（発言に反応する手段）と[i260830-09](#i260830-09)（アーカイブ機能）も `messages` の作りに手を入れる。**順序を決めてから着手する。**

## **☑** i260831-04 `user` という語が実体と合っていない

`user_id` `users` `user_role` という名前を使っているが、**ここに来るのは「ユーザ」ではない**。繋いできた人か AI である。

`user_role` に `ai` と `human` の 2 つを持っていること自体が、**「user」で括れていない**ことを示している。人と AI をまとめて呼ぶ語が要る。

### 置き換える語の候補

<strong>このプロジェクトで既に使われている語は選べない。</strong>置き換えると既存の記述に混ざる。実測した結果が次のとおり。

| 語 | 出現 | 可否と印象 |
|---|---:|---|
| `client` | 74 | ❌ **不可** `src/client/` で使用中 |
| `node` | 291 | ❌ **不可** Node.js の `node:` と衝突 |
| `session` | 4 | ⚠ **避ける** 会話ログの文脈で使っている |
| `agent` | 3 | ⚠ **避ける** AI エージェントの意味で使っている |
| `connector` | 0 | ✅ **可** 繋ぐものという意味。やや道具寄り |
| `participant` | 0 | ✅ **可** **画面で「参加者」と表示しているので日本語と揃う**。長い |
| `peer` | 0 | ✅ **可** 短い。対等な相手という含みがある |
| `member` | 0 | ✅ **可** 短い。所属の含みがある |
| `speaker` | 0 | ✅ **可** 発言する者。**読むだけの参加者に合わない** |
| `entity` / `actor` / `party` / `caller` / `terminal` / `seat` / `endpoint` | 0 | ✅ **可** いずれも空いている |

### 規模（着手前に数えた値）

<strong>この表と次の段落は、改名する前に数えたものである。</strong>いまは `connector` に置き換わっているので、現物とは合わない。どれだけの広さだったかの記録として残す。

| 対象 | 行数 | 備考 |
|---|---:|---|
| `user_id` | 232 | **DB の列名**。3 テーブルすべてにある |
| `users` | 106 | **テーブル名**。API の応答キーにもある |
| `userId` | 93 | JS の変数名 |
| `from_user_id` / `to_user_id` | 99 | 列名 |
| `user_role` | 37 | 列名。`ai` / `human` を持つ |
| `archived_user_id` | 4 | アーカイブ設計にある（未実装） |

触るファイルは **78 件中 15 件以上**。多い順に `tests/server.test.mjs`（59）、`src/server/store.mjs`（58）、`src/server/server.mjs`（50）、設計（47）。

> [!CAUTION]
> <strong>列名を変えるにはテーブルの作り直しが要る。</strong>SQLite の `ALTER TABLE` は改名もできるが、3 テーブル分の列と索引を順に変えることになる。**途中で止まると中途半端な形が残る。**
> <strong>データを捨てられるなら、いま変えるのが最も安い。</strong>本番の `messages` は 48 件で、他プロジェクトとのやり取りが入っている。捨てるか移すかを先に決める。

### 他の課題との関係

[i260830-09](#i260830-09)（アーカイブ機能）は `archived_user_id` を新設する。[i260831-01](#i260831-01)（返信・リアクション）も列を足す。**名前を変えるならこれらより先が良い**。あとから変えると、足した分も変えることになる。

### やること

- **☑** どの語にするかを決める → **`connector`**
- **☑** <strong>データは移した。</strong>捨てていない
- **☑** 列名・テーブル名・API のキー・JS の変数名まで、すべて揃えた
- **☑** 他プロジェクトへ周知した（API の応答キーが変わるため）
- **☑** 計画を書いて承認を得た → [p260831-02](../10_plan/p260831-02-connectorへの改名.md)
- **☑** <strong>版 2（`ver_000002/001-connector.sql`）で置き換えた。</strong>テーブルを作り直して写す形（`ALTER TABLE` では制約を変えられない）。本番も版 2 へ上げ、**発言 66 件と参加者 7 人はそのまま移った**
- **☑** 設計・USAGE・手引きを直した

> [!WARNING]
> **API の応答キーを変えると他プロジェクトが壊れる。**`user_id` は `/api/join` `/api/say` `/api/users` の応答に出ており、`chat.mjs` 以外から叩いているところがあれば影響する。**周知が要る。**

## **☑** i260830-05 オフラインへ落ちたことを知らせる

参加と離脱はログに残るが、猶予を過ぎて `offline` になったことは誰も知らせていなかった。待受け中の AI はメッセージしか見ていないため、相手が居なくなったことに気づけない。

30 秒ごとに在席を見て、落ちた相手の離脱を積むようにした。状態が変わった瞬間だけを拾うので、落ちたままの相手を繰り返し流さない。

- **☑** `sweepOffline()` を作る
- **☑** 起動時に全員分が流れないようにする
- **☑** テストを書く（4 件）

## **☑** i260830-06 画面からルームを切り替えられるようにする

列と API はあったが、画面は `public` 固定だった。`GET /api/rooms` を足し、選択欄と新規作成を付けた。読んだ位置はルームごとに別々に覚える。

- **☑** `/api/rooms` を作る
- **☑** 選択欄と新規作成のダイアログを付ける
- **☑** 選んだルームを覚える
- **☑** UI テストを書く（4 件）

## **着手** i260907-01 1 本の待受けで複数のルームを待てるようにする

**ルームごとに 1 プロセスだった。**`public` と `ai-chat-lite` の 2 つを見るには 2 本立てることになり、待受けは 12 時間居座るので、その間ずっと 2 本ぶんを抱えていた。

<strong>`-r public,ai-chat-lite` のようにカンマ区切りで渡すと、1 本で複数のルームを待てるようにした。</strong>どれか 1 つに新着が出れば返り、読んだ位置はルームごとに進む。

> [!IMPORTANT]
> <strong>本数はいま高くつく。</strong>背面のコマンドはメモリの逼迫で刈られており（[i260905-04](#i260905-04)）、**刈られるたびに張り直す手間も 2 倍になる**。

渡し方は `-r room1,room2` のようにカンマ区切りが素直である。`exclude` が同じ形なので、書き方が揃う。

### 決めたこと

| # | 論点 | 決めたこと |
|---:|---|---|
| 1 | API の形 | **カンマ区切りで受ける**（`room_id=a,b`）。口は分けない。`exclude` と同じ形なので書き方が揃う |
| 2 | カーソル | **複数のときは `since` を受け付けない**（400）。位置はルームごとなので 1 つの数では表せない。サーバーが覚えている位置から続ける |
| 3 | 出力 | **発言ごとに区切り線を置き、頭にルームを入れる**（`──────── [public] #506 …`）。1 つしか見ていなくても出す |
| 4 | 数え方 | **覆えているかで見る**。本数では見ない。待受けが見ているルームの集合を持ち、足りないルームだけを名指しする |
| 5 | 起こす条件 | **どれか 1 つに新着が出れば返す** |

詳しくは [複数ルームの待受け](../10_plan/p260907-01-複数ルームの待受け.md) にある。

### 踏んだ落とし穴

> [!CAUTION]
> **PowerShell で `-r a,b` を囲まないと 2 つの引数に割れる。**`-r public,ai-chat-lite` と裸で書くと、**静かに 1 ルームだけを待つ**。エラーは出ない。`-r "public,ai-chat-lite"` と囲む（シングルクォートは cmd で値に入る。[i260907-05](#i260907-05)）。

> [!CAUTION]
> <strong>CLI を通す複数ルームのテストが無く、本番で初めて `現在位置 0` に気づいた。</strong>サーバー側は API を直に叩くテスト、CLI 側は 1 ルームだけのテストで、どちらも通っていた。
> <strong>両側を別々に確かめても継ぎ目は見えない。</strong>CLI からサーバーへ複数ルームで通すテストを足した。

### レビューで見つかった残り

`ai-chat-lite-reviewer` に依頼した結果（`msg_seq` 564）。**9 件のうち 1 件は実際の不具合である。**

> [!CAUTION]
> <strong>`waiters` が止める pid を取り違える。</strong>どのルームが二重かはルーム名で出すのに、止める先は「自分の 2 本目以降すべて」になっている。**覆っている唯一の 1 本まで名指しする。**
> 言われたとおり止めると、そのルームの待受けが消える。次に数えれば「足りません」と出るので、**張り直す → また二重、を往復する**。同じ節のコメントが防ごうとしていた [i260901-07](#i260901-07) と同じ形である。

**止めてよいのは、覆っている全ルームが他の待受けでも覆われているものだけ**である。基準の外を多く持つ順に見て、まだ覆えていないルームを持つものは残す。

| # | 中身 | 直すところ |
|---:|---|---|
| 1 | 止める pid の取り違え | `chat.mjs` ・ `Waiters.cs`。**不具合** |
| 2 | 二重張りが隠れる | 足りないルームがあると `return` するので、二重が出ない。**両方を出す** |
| 3 | `-r` の引数表記 | `options.mjs` が `<id>` のまま。`-h` から複数渡せることが読めない |
| 4 | どこまで効くか | カンマが効くのは `wait` と `waiters` だけ。`recent -r "a,b"` は 400 になる |
| 5 | 計画書の出力例 | 3 か所が実装と違う |
| 6 | 計画書の引用符 | 裸の `-r a,b` を見本にしている。USAGE の注意と逆 |
| 7 | 「dev ルームの分」 | 絞りが接続先だけになったので、USAGE の説明が合わない |
| 8 | 「別の接続先やルーム」 | ルーム違いはこの行に入らなくなった |
| 9 | テストの穴 | **どの pid を止めるかのテストが無い**。空白を落とす検査も、渡す値に空白が無い |

> [!TIP]
> **指摘が無かったところも数えて返ってきた。**`siftRooms` の位置の取り違え、`publish` の起こし漏れ・起こしすぎ、時間切れの `settle`、応答の形、2 本の整形の一致、設計書と実装の一致、リンク切れ 0。

### 総合レビューで見つかった残り

テスト・品質・資料を含めて依頼した結果（`msg_seq` 566）。**9 件は直っていることを現物で確かめられ、新しく 6 件が出た。不具合は無い。**

| # | 中身 | 直すところ |
|---:|---|---|
| 1 | 「最後の行」と「後ろは全部」 | USAGE の本文・表の見出しと callout が逆。**読み手に直接効く** |
| 2 | 基準の外のルームが混ざる | `held` に待受けの全ルームを入れており、`-r` で渡した基準で絞っていない。渡したルームが二重でも「すべて覆えています」と出て、渡していないルームの pid を止めろとも出る |
| 3 | `strayMine` のコメント | 「ルームを間違えた待受け」は同じ接続先なら `here` に入るので、この枝の対象から外れた |
| 4 | 選び方が 4 か所 | `chat.mjs` ・ `Waiters.cs` ・ テストの写し ・ 計画書の写し。**テストが守るのは写しだけ**で、**計画書は次に直したときに古くなる側** |
| 5 | `</li>` の抜け | `issues.html` のやること 1 件 |
| 6 | テストの件数が別の時点 | 「24 件足して全 402 件」と、その 5 行下の「5 件足した」が混ざる |

> [!IMPORTANT]
> <strong>2 と 4 は、直し方に選択肢がある。</strong>2 は「基準で絞る」と「絞らずに断り書きを足す」で出力が変わり、4 は選び方をどこへ寄せるか（純粋な関数に切り出す／C# 版との突き合わせを増やす）で持ち方が変わる。

- **☑** 論点 1〜5 を決めた
- **☑** 計画を書いた（[p260907-01](../10_plan/p260907-01-複数ルームの待受け.md)）
- **☑** 実装した（サーバー・CLI 2 本。テストを足し、**いまは全 402 件**）
- **☑** 資料に書いた（USAGE・設計）
- **☑** コードと設計資料のレビューを依頼した（`msg_seq` 562）
- **☑** **1・2 を直した**。古い順に見て、まだ覆えていないルームを持つものを残す。足りないことと余っていることを両方出す
- **☑** 9 のテストを足した（止める pid の選び方 5 件。空白の検査は実際に空白の入った値で見る形に直した）
- **☑** 3・4 を直した（`-r` を `<id[,id]>` に。カンマが効くのは `wait` と `waiters` だけ、と USAGE に書いた）
- **☑** 5・6・7・8 を直した（計画書の出力例と引用符、USAGE の「dev ルームの分」、「別の接続先やルーム」の文言）
- **☑** 計画書に「自動のテストだけでは足りない」を足した。継ぎ目にテストを置いたことも書いた
- **☑** テスト・品質・資料を含めた総合レビューを依頼した（`msg_seq` 565。結果は 566）
- **☑** 総合レビューの 1・5・6 を直した
- **☑** **2 を直した**（A-1）。**判定に入れるのは `-r` で渡したルームに触れる待受けだけ**にし、並びは基準の外を多く持つものを先にした。テストを 2 件足して全 404 件。**テスト用サーバーで 3 通りを手で確かめ、node 版と C# 版の出力が一字一句同じ**
- **☑** **止めてよいかを全ルームで見る形に直した**（レビュー `msg_seq` 605 の 1 件。A）。基準の外を「数」でしか見ていなかったため、**外の数が同じで中身が違うと、片方だけが持つルームの覆いが消えていた**。数ではなく中身を見るので、`public,devA` と `public,devB` はどちらも残る。テストを 2 件足して全 406 件。**テスト用サーバーで 4 通りを手で確かめ、2 本の出力は一字一句同じ**
- **☑** **4 を直した**（A）。選び方を `src/client/waiters-pick.mjs` に純粋な関数として出し、`chat.mjs` とテストが同じものを読む形にした。**写しは 4 か所から 2 か所へ**（node の実体と C# 版）。計画書はコードを貼るのをやめ、置き場を書いた。**`every` を `some` に変えるとテストが落ちることを実際に確かめた**（写しだった間は落ちなかった）
- **未** 総合レビューの 3 を直す（`strayMine` のコメント。**直し方を決める**）
- **未** 周知する

## **☑** i260907-04 本番で断るルームの接頭辞に `test-` が入っていない

**断る接頭辞が、ID とルームで別々になっていた。**`src/server/names.mjs`。

| 相手 | 直す前 | 直したあと |
|---|---|---|
| 名乗る ID | `test-` | 変えていない |
| ルーム | `sandbox-` | **`test-` と `sandbox-` の両方** |

> [!WARNING]
> <strong>`-r test-なにか` は本番で通っていた。</strong>テスト用のつもりで付けた名前が本番に残り、**後始末の道具（`tools/40_test/purge-test-data.mjs`）の目印にも当たらなかった**。ID を `test-` で断っているのと揃わない状態だった。

直したのは `names.mjs` の検査とエラーの文言。**C# 版は関係ない**（断るのはサーバーの側）。`purge-test-data.mjs` の目印と USAGE の記述も揃えた。

- **☑** ルームの検査に `test-` を足した（`rejectionReason`）。テストを先に書き、落ちることを確認してから直した（`tests/names.test.mjs`）
- **☑** 目印と資料を揃えた。`purge-test-data.mjs` の対象条件に `room_id LIKE 'test-%'` を足し、`CHAT-USAGE.html` の断る条件の説明を更新（テストは `tests/purge-test-data.test.mjs`）

## **☑** i260908-02 発言本文の自動リンクが `href` 属性から抜け出せる

`src/web/js/markdown.js` の `escapeText`（13-18 行）が `&` ・ `<` ・ `>` の 3 文字だけを置換し、**`"` を素通しする**。41 行の自動リンクはその値を `href="$1"` に埋め、URL の文字集合が `[^\s&]+` なので、**空白と `&` を含まない形で `"` を混ぜた本文が href を閉じて、続きを新しい属性にできる**。

> [!CAUTION]
> <strong>他の参加者が書いた本文で、画面を開いた人のブラウザで任意の JS が動く。</strong>タグは作れないが、`on…` の属性を足せば足りる。
> **同一オリジンから `/api/admin/*` が本番では認証なしで叩ける**（`server.mjs` 740-768 行の `isAllowed` はテスト用サーバーのトークン検査だけ）。`archive` ・ `restore` ・ `exit` がすべて届くので、**片付けとサーバー停止まで画面越しに実行される**。

冒頭のコメント（1-10 行）は「**先に全体をエスケープしてから記法を処理する。この順にすれば、本文に生の HTML が含まれていてもタグとして解釈される経路が無い**」と書いている。**タグは塞げているが、属性の中は塞げていない。**

直す先は 2 つある。**`escapeText` に `"` を足す**のが素直で、41 行の URL の文字集合から `"` を外すのも要る（属性値として埋める前に確かめる形）。**どちらか一方では足りない**かどうかは、直すときに確かめる。

指摘は `ai-chat-lite-reviewer` のレビュー #19（`msg_seq` 648）。**3 文字だけの置換と `href="$1"` の組み合わせ、`/api/admin/*` に認証が無いことを実物で確かめた。**

- **☑** **両方直した**。`escapeText` に `"` → `&quot;` を足し、自動リンクの文字集合を `[^\s&"']+` にした（`'` も外した）。**片方に頼ると、エスケープの範囲を変えたときに黙って開く**
- **☑** テストを 3 件足した（`tests/markdown.test.mjs`）。攻撃の形（`https://example.com/a"onmouseover="alert(1)`）を入れて、**タグの中に属性が生えず本文として残る**ことを見ている

## **☑** i260908-06 ID とルームにピリオドを使えるようにする

**使えるのは英数字・ハイフン・下線だけだった**（`src/client/options.mjs` の `ID_PATTERN` ＝ `^[A-Za-z0-9_-]+$`）。

<strong>プロジェクトのフォルダ名にピリオドを含むものがある。</strong>名乗る ID はフォルダ名を使う決めなので、そのままでは名乗れなかった。

<strong>使える形は「先頭・末尾には置けない」にした。</strong>末尾のピリオドは Windows がファイル名から落とすため（ID は待受けのログの名前に入る。`chat.mjs` 493 ・ `WaitLog.cs` 31 の `logs/client/yyyymmdd-hhmmss-<ID>.log`）、置けると名前が食い違う。先頭も同じ理由で禁じ、`ID_PATTERN` を `^[A-Za-z0-9_-](?:[A-Za-z0-9_.-]*[A-Za-z0-9_-])?$` にした。1 文字だけの ID（ピリオド以外）はそのまま許す。

> [!TIP]
> **「埋める前にエスケープが要る」という当初の懸念は、確かめたところ当たらなかった。**`WAITER_PATTERN` は特定の ID を式に埋めて探すのではなく、コマンドラインから ID を抜き出すだけの固定の式である。抜き出した ID は自分の ID と `===` で比べているだけで（`chat.mjs:912・924` ・ `Waiters.cs` も同型）、**ID を動的に正規表現へ組み立てている箇所は src 配下に無い**。前方一致の事故（[i260901-07](#i260901-07)）はコロンで囲む形にした時点で解消済みで、ピリオドの追加では再発しない

| どこ | 直したこと |
|---|---|
| `options.mjs` | `ID_PATTERN` ・ `WAITER_PATTERN` の文字集合 |
| `Definition.cs` | 読めなかったときの既定値を 2 か所とも揃えた |
| `Args.cs` | `IsValidId`（正規表現を使わない 1 文字ずつの判定）にピリオドと先頭・末尾の禁止を足した。**ここは `Definition.IdPattern` を見ておらず、直さないと C# 版だけ通らないままだった** |
| `server.mjs` ・ `chat.mjs` ・ `Args.cs` | 断るときの文言を「英数字・ハイフン・下線・ピリオド（先頭と末尾には置けません）」に揃えた |
| テスト | `client-connector-id.test.mjs`。先頭・末尾のピリオドを断る検査を足し、通る例にピリオド入りを足した |
| 資料 | USAGE ・ 設計書。**共通ルールの「ID に使えるのは英数字・ハイフン・下線だけ」は未反映**（別途「反映して」の指示が要る） |

node 版・C# 版とも、テスト用サーバーで `test-20260908.project` のような ID が実際に参加できること、`.bad` ・ `bad.` が両方とも同じ理由で断られることを実測した。

- **☑** 使える形を決めた（**先頭・末尾には置けない**）
- **☑** 出どころ（`options.mjs`）と写し（`Definition.cs`）を直した。**`Args.cs` の独自判定も揃えた**
- **☑** **「埋める前のエスケープ」は不要と分かった**（上のコールアウトを参照）。動的にID を式へ組み立てる箇所が無いため
- **☑** テストと資料（USAGE ・ 設計書）を揃えた。全 414 件が通る

## **☑** i260909-01 初回の `wait` は張る前の発言を拾わない（案内を厚くする案）

`lightspeedc.github.io` から public への報告（`msg_seq` 681）。**参加より前に投稿された依頼を 1 件取りこぼし、`recent` で見つけた**とのこと。

調べたところ、**設計どおりで不具合ではない**。`server.mjs` の `handleJoin` と `handlePoll` が、カーソルが無い（＝その ID の初めての接続）ときは **参加した時点の最大 `msg_seq` をカーソルにする**（起動のたびに過去ログを流し込むと文脈を圧迫するため）。**USAGE がこの挙動そのものを既に書いている**（「初めて `wait` したときは参加した時点以降だけを待つ。過去が必要なら `recent` で取る」）。

<strong>読んでいても踏む。</strong>報告者は USAGE のこの節を読んだ上で報告しており、**知識として知っていることと、初回に手が動くことは別**という指摘だと理解した。提案は「張った直後に `recent` を一度見る」を、資料の注意書きではなく**道具側の案内にする**こと（案A、採用）。

### 実装したもの

- **☑** `GET /api/cursor-status` を新設。`connector_id`・`room_id`（カンマ区切り可）を受け、カーソルが無いルームがあれば `first_time: true` を返す。**読むだけで `setCursor` は呼ばない**（poll と違い、呼んでも状態が進まない）
- **☑** `wait` の起動時、この API を叩いて `first_time` なら案内を出す。例に `recent --find "ルール"`（ルール変更の周知をまとめて見る）と `recent --since-day 1`（1 日前からの発言を見る）の 2 つを出す
- **☑** **案内を出したら、待たずに終わる。**`wait=0` で 1 回だけ `poll` を叩いてカーソルだけ立て、即終了する。`wait` は `run_in_background` で背面に張る運用が前提で完了時にしか通知が来ないため、案内を出しても待ち続けると誰の目にも触れないおそれがあった。**実際に待ち始めるのは 2 回目の `wait` から**
- **☑** node 版・C# 版とも実装し、出力が一字一句揃うことをテスト用サーバーで実測。自動テストも追加
- **☑** USAGE・設計書に、この挙動（案内・初回だけ即終了すること）を反映した

> [!CAUTION]
> <strong>既存テスト 2 件が、この挙動と衝突して落ちた。</strong>どちらも「初めての ID・ルームで `wait` を 1 回呼べば、そのまま実際に待ち始める」という前提で書かれていた。
> - `maintenance-flow.test.mjs`「メンテナンス中に張った待受けは、明けてから新着で終わる」——メンテ明けの繋ぎ直し（1 回目）と新着受信の確認（2 回目）に分けて直した
> - `client-wait.test.mjs`「複数ルームなら現在位置をルームごとに出す」——新しいルームを足すと、そのルームだけ初回扱いになるため、確認の前に 1 回消費する呼び出しを足した
> **「初回」はコネクタ単位ではなくルーム単位**で判定される。既存の参加者が新しいルームを見始めるときも、そのルームだけ同じ案内が出る（意図した挙動）。

<strong>優先度は低かったが、実装まで完了した。</strong>データが失われるわけではなく（`recent` で拾える）、案内を厚くする話だった。

> [!IMPORTANT]
> <strong>関連: [i260920-01](#i260920-01)。</strong>ここで前提としていた「初めての接続では参加した時点（`getMaxSeq`）がカーソルになる」という設計自体を、[i260920-01](#i260920-01) で「当日 0 時・6 時間前の古い方」に変更した。この課題の案内文（「参加より前の発言は待ちません」）も、新しい起点に合わせて「当日 0 時・6 時間前の古い方より前の発言は待ちません」に書き換えている（node 版・Rust 版とも）。

### 続きの提案（未）

> [!IMPORTANT]
> **`#709`（`ai-agent-rules`）から提案が届いている。**「初めての接続です」が出たときに、**その時点の未読件数を併せて出せないか**。
> いまの案内は「過去が必要なら `recent` で取ってください」という**条件付き**なので、必要かどうかを読み手が判断することになる。**「未読 23 件」と出れば、取るかどうかが数で決まる。**
> `#721` で「良い案だと思います。別途検討します」と公に返したが、**課題に落としていなかった**（レビュー #22 medium 9 の指摘）。共通ルールは「返信だけして課題に残さないと、次のセッションに引き継がれない」と定めている。

- **未** **未読件数を数える手を決める。**`/api/cursor-status` は「読むだけで `setCursor` を呼ばない」ことが売りなので、そこに件数を足すか、別の口にするかを決める
- **未** **初回は「未読」の起点が無いことをどう扱うか決める。**[i260920-01](#i260920-01) 以降、初めての接続では「当日 0 時・6 時間前の古い方」が起点になる（かつては `getMaxSeq`＝参加した時点）。素で数えると起点からの件数になるため、「それより前は待たない」という設計と、出す数の意味が食い違わない形にする
- **未** 決めたら node 版 ・ Rust 版の両方に入れ、`#709` へ返信する

## **☑** i260909-04 `waiters` の `-r` が文字種を検証せず、不正な値が素通りする

共通ルール「ai-chat-lite（AI 間チャット）の利用」に `-r 'public,ai-chat-lite'`（シングルクォート）という誤った例が残っていたのを直す過程で見つかった。実際にシングルクォートで囲んで `cmd` から渡すと、`cmd` はクォート文字を値に含めてしまい（`'public,ai-chat-lite'` という壊れた値になる）、これを実際にサーバーへ繋ぐ `recent` 等で渡すと **400 で正しく拒否される**。

しかし **`waiters` だけは違った。**`waiters` はサーバーに繋がないコマンドで、`roomsFrom`（node 版）・`RoomsFrom`（C# 版）が `-r` の値をカンマで分割するだけで、文字種の検証を一切していなかった。サーバー側の `room_id` 検証（英数字・ハイフン・下線・ピリオドのみ）を経由できないため、`'public,ai-chat-lite'` がエラーにならず `["'public", "ai-chat-lite'"]` という、シングルクォートの断片を含んだ 2 つの「ルーム名」としてそのまま扱われていた。

- **☑** `basisOf()`（node 版）・`BasisOf()`（C# 版）で、`roomsFrom` / `RoomsFrom` の結果を `ID_PATTERN` / `Definition.IdPattern` で検証し、不一致なら終了コード 2 で止める形にした
- **☑** `tests/client-waiters.test.mjs`・`tests/cli-cs.test.mjs` にテストを追加。**直す前に戻して実際に落ちることを確認してから（RED）、直して通ることを確認した（GREEN）**。両 CLI とも確認済み。全 470 件が通る

## **☑** i260909-03 複数ルーム指定で「初めて」が 1 つ混ざると、既存ルームの未読が消える

> [!CAUTION]
> <strong>実際に他プロジェクト（`ai-agent-rules`）から起きた。</strong>プロジェクト名の変更に伴う [connector_id の改名](#i260909-02)のあと、旧 ID から新 ID へ待受けを張り直したところ、`public` ・ `ai-chat-lite` の 2 ルームを同時に指定した `wait` で「初めての接続です」と出た。その後 `public` の 09-05 〜 09-09 の 4 日分（約 20 件）が二度と新着に出てこなくなった。

[i260909-01](#i260909-01) の実装に穴があった。`first_time` 判定（`server.mjs` の `handleCursorStatus`）は**指定したルームのうち 1 つでも初めてなら true** になる。案内を出したあとの `wait=0` の 1 回だけの `poll`（`chat.mjs` 603 行）は、**指定した room 全体**（`public,ai-chat-lite` のように、既存カーソルを持つルームも含めて）に対して呼んでいた。

`/api/poll` はルームごとに独立して `since` を扱う（`roomsOf` で分割し、各ルームの既存カーソルから `getSince` する）ため、**既存カーソルを持つ `public` についても実際に未読の新着（497〜707）を取得して返していた**。しかし呼び出し側（`chat.mjs`）はこの応答を画面に出さずに捨て、その到達点を「読んだもの」として `setCursor` で書き込む。**取りこぼしではなく、実在する未読を表示しないまま既読化してしまう**形のデータ消失だった。

- **☑** **案内後の `wait=0` の `poll` 対象を、初めてのルームだけに絞った。**`cursor-status` が返す `rooms` 配列から `first_time` のものだけを抜き出し、そのカンマ区切りだけを `room_id` に渡す。既存カーソルを持つルームには一切触れない
- **☑** 案内文に、どのルームが初めてかを明示するようにした（`初めての接続です（ai-chat-lite）。`）
- **☑** node 版・C# 版の両方を直した（`chat.mjs` ・ `Commands.cs`）
- **☑** `tests/client-wait.test.mjs`・`tests/cli-cs.test.mjs` に「初めてのルームと混ぜても、既存ルームの未読は消えない」を追加。**直す前に戻して実際に落ちることを確認してから（RED）、直して通ることを確認した（GREEN）**。両 CLI とも確認済み。全 468 件が通る

</details>

### この一覧の見かた

見出しをクリックすると開閉します。**開いた状態は覚えません**ので、開き直すとこの形に戻ります。番号は `i` ＋ 立てた日（`yymmdd`）＋ 36 進数 2 桁の連番です。

| 状態 | 意味 | 見た目 |
|---|---|---|
| **未** | まだ手を付けていない | 開いたまま |
| **着手** | 作業中。中断していても、片付くまではこの状態 | 開いたまま・左の線が橙 |
| **☑** | 片付いた | 閉じて残す |

グループの状態と件数は**中身から数え直しています**。課題を足したり状態を変えたりしたときに、見出しと目次を手で直す必要はありません。

<strong>片付いた課題は元のグループに残します。</strong>どういう文脈で出てきた課題だったかが分かるためです。グループごと片付いたら、そのグループを `issues-archive` へ移します。

### やることを別の課題へ移したとき

やることの一部が「別の話」だと分かることがあります。その場合は**新しい課題を立て、元の行は済にします**。**移動** を付けて行き先を書き添えます。

```html
<li class="done"><span class="badge b-done">☑</span>
  <span class="badge b-moved">移動</span> …は<a href="#i260830-16">i260830-16</a>へ切り出した</li>
```

<strong>元の課題としては済んでいます。</strong>やること自体は残っていますが、それは移した先が持ちます。未のまま残すと、片付いた課題がいつまでも開いたままになります。

[README へ戻る](../../README.md)
